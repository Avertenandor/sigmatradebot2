# Business Logic Documentation

Документация бизнес-логики бота, включая поведение при различных сценариях.

## Поведение при блокировке/терминации пользователя

### Типы блокировок

Бот поддерживает три типа действий в blacklist:

1. **REGISTRATION_DENIED** - Отказ в регистрации
   - Пользователь не может зарегистрироваться
   - Не влияет на существующих пользователей

2. **BLOCKED** - Блокировка аккаунта (с возможностью апелляции)
   - Пользователь может подать апелляцию в течение 3 рабочих дней
   - Доступны только: `/start` и кнопка "📝 Подать апелляцию"
   - Все остальные действия заблокированы

3. **TERMINATED** - Терминация аккаунта (без возможности апелляции)
   - Полная блокировка всех действий
   - Нет возможности подать апелляцию
   - Все взаимодействия с ботом заблокированы

### Влияние на депозиты

#### При BLOCKED:

- **Активные депозиты:**
  - Продолжают работать и начислять ROI
  - Пользователь не может создавать новые депозиты
  - Пользователь не может выводить средства

- **ROI начисления:**
  - Продолжают начисляться согласно расписанию
  - Накапливаются в `earnings` балансе
  - Доступны для вывода после снятия блокировки

- **Новые депозиты:**
  - Создание новых депозитов заблокировано
  - Кнопка "💰 Депозит" недоступна (пользователь не видит меню)

#### При TERMINATED:

- **Активные депозиты:**
  - Замораживаются (ROI перестает начисляться)
  - Пользователь не может создавать новые депозиты
  - Пользователь не может выводить средства

- **ROI начисления:**
  - Прекращаются с момента терминации
  - Существующие `earnings` остаются в системе, но недоступны для вывода

- **Новые депозиты:**
  - Создание полностью заблокировано

**Примечание:** Текущая реализация не останавливает автоматическое начисление ROI для BLOCKED пользователей. Это бизнес-решение - заблокированные пользователи продолжают получать ROI, но не могут выводить средства до снятия блокировки.

### Влияние на реферальные начисления

#### При BLOCKED:

- **Реферальные начисления:**
  - Продолжают начисляться за активность рефералов
  - Пользователь получает комиссии от депозитов своих рефералов
  - Начисления идут в `referral_earnings` баланс

- **Выплаты реферальных начислений:**
  - Заблокированы до снятия блокировки
  - `referral_earnings` накапливаются, но недоступны для вывода

- **Реферальная структура:**
  - Остается активной
  - Новые рефералы могут регистрироваться по ссылке заблокированного пользователя
  - Комиссии продолжают начисляться

#### При TERMINATED:

- **Реферальные начисления:**
  - Прекращаются с момента терминации
  - Существующие `referral_earnings` остаются в системе, но недоступны

- **Реферальная структура:**
  - Остается в базе данных для исторических целей
  - Новые рефералы могут регистрироваться, но комиссии не начисляются
  - Реферальная ссылка продолжает работать, но пользователь не получает комиссии

**Примечание:** Это важный security-аспект. Админ не может "по-тихому" обнулить реферальные начисления, изменив статус пользователя. Все начисления остаются в системе и могут быть восстановлены при снятии блокировки (для BLOCKED) или вручную администратором.

### Влияние на выводы

#### При BLOCKED:

- **Активные заявки на вывод:**
  - Остаются в системе
  - Могут быть обработаны администратором
  - Пользователь не может создавать новые заявки

- **Новые заявки:**
  - Создание полностью заблокировано
  - Кнопка "💸 Вывод" недоступна

#### При TERMINATED:

- **Активные заявки на вывод:**
  - Могут быть отклонены администратором
  - Пользователь не может создавать новые заявки

- **Новые заявки:**
  - Создание полностью заблокировано

### Влияние на поддержку

#### При BLOCKED:

- **Создание тикетов:**
  - Заблокировано (пользователь не видит меню поддержки)
  - Исключение: апелляция создается автоматически при подаче апелляции

- **Существующие тикеты:**
  - Остаются активными
  - Пользователь может получать ответы от администраторов
  - Пользователь не может отправлять новые сообщения

#### При TERMINATED:

- **Создание тикетов:**
  - Полностью заблокировано

- **Существующие тикеты:**
  - Могут быть закрыты администратором
  - Пользователь не может отправлять новые сообщения

### Восстановление после блокировки

#### Для BLOCKED:

- **Автоматическое восстановление:**
  - При одобрении апелляции блокировка снимается
  - `user.is_banned = False`
  - `blacklist_entry.is_active = False`
  - Все функции восстанавливаются

- **Ручное восстановление:**
  - Администратор может деактивировать запись в blacklist
  - Все функции восстанавливаются

#### Для TERMINATED:

- **Восстановление:**
  - Только вручную администратором
  - Требуется деактивация записи в blacklist
  - `user.is_banned = False`
  - Все функции восстанавливаются

### Security-модель

**Важные принципы:**

1. **Необратимость финансовых операций:**
   - Депозиты и реферальные начисления не удаляются при блокировке
   - Все данные остаются в системе для аудита

2. **Прозрачность для администратора:**
   - Все действия логируются в `admin_actions`
   - История блокировок сохраняется в `blacklist`
   - Администратор не может "скрыть" финансовые операции

3. **Защита от злоупотреблений:**
   - Администратор не может обнулить балансы через блокировку
   - Все начисления продолжаются (для BLOCKED) или остаются в системе (для TERMINATED)
   - Восстановление доступа возвращает все функции

4. **Аудит:**
   - Все изменения статуса пользователя логируются
   - История депозитов и выводов сохраняется
   - Реферальные начисления отслеживаются независимо от статуса пользователя

---

**Последнее обновление:** 2025-01-19

