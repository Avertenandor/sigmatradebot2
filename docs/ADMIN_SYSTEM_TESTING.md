# Инструкция по тестированию системы управления админами

## Предварительные требования

1. Применить миграцию:
   ```bash
   alembic upgrade head
   ```

2. Убедиться, что бот запущен и работает

3. Иметь доступ к Telegram боту как существующий админ

## Тест 1: Аутентификация через мастер-ключ

### Шаги:
1. Отправить команду `/admin` в бот
2. Система должна запросить мастер-ключ
3. Ввести правильный мастер-ключ
4. Должна открыться админ-панель

### Ожидаемый результат:
- ✅ Запрос мастер-ключа
- ✅ Успешный вход при правильном ключе
- ✅ Открытие админ-панели

### Проверка в БД:
```sql
SELECT * FROM admin_sessions 
WHERE admin_id = <your_admin_id> 
ORDER BY created_at DESC 
LIMIT 1;
```
Должна быть активная сессия с `is_active = true`.

## Тест 2: Автоматический разлогин при бездействии

### Шаги:
1. Войти в админ-панель с мастер-ключом
2. Подождать 16 минут без действий
3. Попытаться выполнить любое действие в админ-панели

### Ожидаемый результат:
- ✅ Система запросит мастер-ключ снова
- ✅ Сообщение о неактивной сессии

### Проверка в БД:
```sql
SELECT * FROM admin_sessions 
WHERE admin_id = <your_admin_id> 
ORDER BY created_at DESC 
LIMIT 1;
```
Сессия должна быть деактивирована (`is_active = false`).

## Тест 3: Создание нового админа (super_admin)

### Шаги:
1. Войти как super_admin
2. Нажать "👥 Управление админами"
3. Нажать "➕ Добавить админа"
4. Ввести Telegram ID нового админа
5. Выбрать роль (1, 2 или 3)
6. Проверить, что мастер-ключ отправлен новому админу

### Ожидаемый результат:
- ✅ Успешное создание админа
- ✅ Мастер-ключ отправлен новому админу в Telegram
- ✅ Подтверждение создания

### Проверка в БД:
```sql
SELECT * FROM admins WHERE telegram_id = <new_admin_telegram_id>;
SELECT * FROM admin_actions WHERE action_type = 'ADMIN_CREATED' ORDER BY created_at DESC LIMIT 1;
```

## Тест 4: Список админов (super_admin)

### Шаги:
1. Войти как super_admin
2. Нажать "👥 Управление админами"
3. Нажать "📋 Список админов"

### Ожидаемый результат:
- ✅ Отображение списка всех админов
- ✅ Информация о ролях
- ✅ Информация о создателе (если есть)

## Тест 5: Удаление админа (super_admin)

### Шаги:
1. Войти как super_admin
2. Нажать "👥 Управление админами"
3. Нажать "🗑️ Удалить админа"
4. Ввести Telegram ID админа для удаления
5. Подтвердить удаление

### Ожидаемый результат:
- ✅ Успешное удаление админа
- ✅ Подтверждение удаления

### Проверка защиты:
- ❌ Нельзя удалить самого себя
- ❌ Нельзя удалить последнего super_admin

### Проверка в БД:
```sql
SELECT * FROM admins WHERE id = <deleted_admin_id>;
-- Должно быть NULL
SELECT * FROM admin_actions WHERE action_type = 'ADMIN_DELETED' ORDER BY created_at DESC LIMIT 1;
```

## Тест 6: Логирование действий

### Шаги:
1. Выполнить любое действие админа:
   - Создать/удалить админа
   - Заблокировать пользователя
   - Одобрить/отклонить вывод
   - Отправить рассылку

### Проверка в БД:
```sql
SELECT * FROM admin_actions 
ORDER BY created_at DESC 
LIMIT 10;
```

### Ожидаемый результат:
- ✅ Все действия логируются
- ✅ Правильный `admin_id`
- ✅ Правильный `action_type`
- ✅ Детали в `details` (JSON)

## Тест 7: Очистка сессий

### Шаги:
1. Создать несколько истекших сессий (вручную в БД или подождать)
2. Подождать запуска задачи очистки (каждые 5 минут)
3. Проверить логи или БД

### Проверка в БД:
```sql
SELECT COUNT(*) FROM admin_sessions 
WHERE is_active = true 
AND expires_at < NOW();
-- Должно быть 0 после очистки
```

### Ожидаемый результат:
- ✅ Истекшие сессии деактивированы
- ✅ Неактивные сессии (>15 минут) деактивированы

## Тест 8: Проверка прав доступа

### Шаги:
1. Войти как обычный админ (не super_admin)
2. Попытаться открыть "👥 Управление админами"

### Ожидаемый результат:
- ❌ Кнопка "👥 Управление админами" не отображается
- ❌ При попытке доступа - сообщение об отказе

## Тест 9: Проверка обновления активности сессии

### Шаги:
1. Войти в админ-панель
2. Выполнить несколько действий с интервалом < 15 минут
3. Проверить `last_activity` в БД

### Проверка в БД:
```sql
SELECT last_activity, NOW() - last_activity as time_since_activity 
FROM admin_sessions 
WHERE admin_id = <your_admin_id> 
AND is_active = true 
ORDER BY created_at DESC 
LIMIT 1;
```

### Ожидаемый результат:
- ✅ `last_activity` обновляется при каждом действии
- ✅ `time_since_activity` < 15 минут

## Тест 10: Проверка отправки мастер-ключа

### Шаги:
1. Создать нового админа
2. Проверить, что мастер-ключ отправлен в Telegram

### Ожидаемый результат:
- ✅ Новый админ получает сообщение с мастер-ключом
- ✅ Сообщение содержит инструкции по использованию

## Чек-лист тестирования

- [ ] Тест 1: Аутентификация через мастер-ключ
- [ ] Тест 2: Автоматический разлогин при бездействии
- [ ] Тест 3: Создание нового админа
- [ ] Тест 4: Список админов
- [ ] Тест 5: Удаление админа
- [ ] Тест 6: Логирование действий
- [ ] Тест 7: Очистка сессий
- [ ] Тест 8: Проверка прав доступа
- [ ] Тест 9: Обновление активности сессии
- [ ] Тест 10: Отправка мастер-ключа

## Известные ограничения

1. **Мастер-ключ**: Если мастер-ключ потерян, его нельзя восстановить. Нужно создать нового админа или сбросить мастер-ключ в БД (не рекомендуется).

2. **Сессии**: Сессии хранятся в БД и автоматически очищаются задачей каждые 5 минут.

3. **Логирование**: Логи действий хранятся в БД и не удаляются автоматически. Рекомендуется периодически архивировать старые логи.

## Устранение неполадок

### Проблема: Мастер-ключ не принимается
**Решение**: Проверить, что мастер-ключ введен правильно (без пробелов, регистр не важен для hex).

### Проблема: Сессия не обновляется
**Решение**: Проверить, что middleware `AdminAuthMiddleware` правильно применен к роутерам.

### Проблема: Кнопка "Управление админами" не отображается
**Решение**: Проверить, что админ имеет роль `super_admin` и `is_super_admin` правильно передается в `admin_keyboard()`.

### Проблема: Логирование не работает
**Решение**: Проверить, что таблица `admin_actions` создана (миграция применена) и `AdminLogService` правильно вызывается в handlers.

