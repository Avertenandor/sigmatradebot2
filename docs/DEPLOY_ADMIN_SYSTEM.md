# Деплой системы управления админами

## ⚠️ ВАЖНО: Перед деплоем

Система управления админами была реализована локально. Для деплоя на сервер необходимо выполнить следующие шаги.

## 📋 Чеклист перед деплоем

### 1. Проверка файлов

Убедитесь, что все новые файлы созданы:

```bash
# Проверить наличие новых файлов
ls -la app/models/admin_action.py
ls -la app/repositories/admin_action_repository.py
ls -la app/services/admin_log_service.py
ls -la bot/middlewares/admin_auth_middleware.py
ls -la bot/handlers/admin/admins.py
ls -la jobs/tasks/admin_session_cleanup.py
ls -la alembic/versions/20250113_000001_create_admin_actions_table.py
```

### 2. Проверка миграции

Убедитесь, что миграция создана:

```bash
# Проверить миграцию
cat alembic/versions/20250113_000001_create_admin_actions_table.py
```

### 3. Коммит изменений

```bash
# Проверить статус
git status

# Добавить все изменения
git add .

# Закоммитить
git commit -m "feat: Add admin management system with master key auth, logging, and auto-logout"

# Отправить на сервер (если используется git)
git push origin <your-branch>
```

## 🚀 Шаги деплоя на сервер

### Шаг 1: Подключение к серверу

```bash
# Через gcloud
gcloud compute ssh sigmatrade-20251108-210354 --zone=europe-north1-a

# Или через SSH
ssh konfu@34.88.234.78
```

### Шаг 2: Обновление кода на сервере

```bash
cd /opt/sigmatradebot

# Обновить код из репозитория
git pull origin <your-branch>

# Или если код уже на сервере, проверить изменения
git status
```

### Шаг 3: Применение миграции БД

**⚠️ КРИТИЧЕСКИ ВАЖНО: Применить миграцию перед перезапуском бота!**

```bash
cd /opt/sigmatradebot

# Вариант 1: Через Docker (если alembic в контейнере)
docker-compose -f docker-compose.python.yml exec bot alembic upgrade head

# Вариант 2: Локально (если alembic установлен на сервере)
alembic upgrade head

# Проверить, что миграция применена
alembic current
```

### Шаг 4: Перезапуск сервисов

```bash
cd /opt/sigmatradebot

# Пересобрать и перезапустить
docker-compose -f docker-compose.python.yml up -d --build

# Или просто перезапустить
docker-compose -f docker-compose.python.yml restart
```

### Шаг 5: Проверка логов

```bash
# Проверить логи бота
docker-compose -f docker-compose.python.yml logs -f bot | tail -100

# Проверить логи worker
docker-compose -f docker-compose.python.yml logs -f worker | tail -50

# Проверить логи scheduler
docker-compose -f docker-compose.python.yml logs -f scheduler | tail -50
```

### Шаг 6: Проверка миграции в БД

```bash
# Подключиться к БД
psql -h localhost -U botuser -d sigmatradebot

# Проверить, что таблица создана
\dt admin_actions

# Проверить структуру таблицы
\d admin_actions

# Выйти
\q
```

## ✅ Проверка после деплоя

### 1. Проверка таблицы admin_actions

```sql
-- В psql
SELECT * FROM admin_actions LIMIT 1;
```

Если таблица существует и пуста - это нормально (логи появятся после действий админов).

### 2. Тест входа в админ-панель

1. Откройте Telegram бота
2. Отправьте `/admin`
3. Система должна запросить мастер-ключ
4. Введите мастер-ключ
5. Должна открыться админ-панель

### 3. Проверка кнопки "Управление админами"

1. Войдите как super_admin
2. Проверьте, что кнопка "👥 Управление админами" отображается
3. Нажмите на неё
4. Должно открыться меню управления админами

### 4. Проверка логирования

1. Выполните любое действие админа (например, заблокируйте пользователя)
2. Проверьте в БД:

```sql
SELECT * FROM admin_actions ORDER BY created_at DESC LIMIT 5;
```

Должна появиться запись о действии.

## 🔧 Устранение проблем

### Проблема: Миграция не применяется

```bash
# Проверить текущую версию миграции
alembic current

# Проверить историю миграций
alembic history

# Применить миграцию вручную
alembic upgrade head
```

### Проблема: Бот не запускается

```bash
# Проверить логи на ошибки
docker-compose -f docker-compose.python.yml logs bot | grep -i error

# Проверить импорты
docker-compose -f docker-compose.python.yml exec bot python -c "from app.models.admin_action import AdminAction; print('OK')"
```

### Проблема: Middleware не работает

```bash
# Проверить, что middleware зарегистрирован
docker-compose -f docker-compose.python.yml exec bot python -c "from bot.middlewares.admin_auth_middleware import AdminAuthMiddleware; print('OK')"

# Проверить логи при входе в админ-панель
docker-compose -f docker-compose.python.yml logs bot | grep -i "admin.*auth\|master.*key"
```

## 📝 Быстрая команда для деплоя

Если все готово, можно использовать один скрипт:

```bash
cd /opt/sigmatradebot && \
git pull && \
alembic upgrade head && \
docker-compose -f docker-compose.python.yml up -d --build && \
docker-compose -f docker-compose.python.yml logs -f bot | tail -50
```

## ⚠️ Важные напоминания

1. **Миграция обязательна**: Без применения миграции система не будет работать
2. **Мастер-ключ**: Все админы должны будут ввести мастер-ключ при первом входе
3. **Логирование**: Начнет работать сразу после деплоя
4. **Очистка сессий**: Задача запустится автоматически через scheduler

## 📊 Мониторинг после деплоя

### Проверить активные сессии

```sql
SELECT * FROM admin_sessions WHERE is_active = true;
```

### Проверить логи действий

```sql
SELECT action_type, COUNT(*) as count 
FROM admin_actions 
GROUP BY action_type 
ORDER BY count DESC;
```

### Проверить задачу очистки

```bash
# Проверить логи scheduler
docker-compose -f docker-compose.python.yml logs scheduler | grep -i "admin.*session.*cleanup"
```

---

**После выполнения всех шагов система управления админами будет развернута и готова к использованию!** ✅

