# Security Runbook

Руководство по экстренным действиям в случае компрометации безопасности.

## Экстренная блокировка скомпрометированного админа

### Сценарий

Админ-аккаунт был скомпрометирован (Telegram украден, подозрительная активность, и т.д.).

### Пошаговая инструкция

#### Шаг 1: Подтверждение компрометации

**ВАЖНО:** Перед блокировкой необходимо связаться с человеком **вне Telegram** (телефон, email, личная встреча) и подтвердить, что аккаунт действительно скомпрометирован.

Если подтверждение получено:

#### Шаг 2: Выполнение экстренной блокировки

1. Войдите в админ-панель как `super_admin`:
   - Используйте команду `/admin`
   - Введите ваш мастер-ключ

2. Перейдите в меню "👥 Управление админами"

3. Нажмите кнопку "🛑 Экстренно заблокировать админа"

4. Введите Telegram ID скомпрометированного админа

5. Подтвердите действие

**Что происходит автоматически:**
- ✅ Админ удаляется из таблицы `admins`
- ✅ Все его сессии деактивируются
- ✅ Telegram ID добавляется в blacklist с типом `TERMINATED`
- ✅ Если админ был зарегистрирован как пользователь - ставится `user.is_banned = True`
- ✅ Все супер-администраторы получают уведомление

#### Шаг 3: Проверка в базе данных

Выполните следующие SQL запросы для проверки:

```sql
-- Проверка удаления админа
SELECT * FROM admins WHERE telegram_id = <TELEGRAM_ID>;
-- Результат должен быть пустым

-- Проверка blacklist
SELECT * FROM blacklist 
WHERE telegram_id = <TELEGRAM_ID> 
AND is_active = true 
AND action_type = 'terminated';
-- Должна быть одна активная запись с action_type = 'terminated'

-- Проверка деактивации сессий
SELECT * FROM admin_sessions 
WHERE admin_id = <ADMIN_ID> 
AND is_active = true;
-- Результат должен быть пустым (все сессии деактивированы)

-- Проверка бана пользователя (если был зарегистрирован)
SELECT id, telegram_id, is_banned FROM users 
WHERE telegram_id = <TELEGRAM_ID>;
-- is_banned должен быть true
```

#### Шаг 4: Уведомление команды

После выполнения блокировки все супер-администраторы автоматически получают уведомление в Telegram.

**Дополнительно:** Если нужно уведомить команду вручную, используйте скрипт:

```bash
python scripts/notify_admins.py --message "Админ <TELEGRAM_ID> был экстренно заблокирован"
```

### Восстановление доступа (если компрометация была ложной)

Если выяснилось, что компрометация была ложной:

1. Удалите запись из blacklist:
   ```sql
   UPDATE blacklist 
   SET is_active = false 
   WHERE telegram_id = <TELEGRAM_ID> 
   AND action_type = 'terminated';
   ```

2. Если пользователь был забанен, разбаньте его:
   ```sql
   UPDATE users 
   SET is_banned = false 
   WHERE telegram_id = <TELEGRAM_ID>;
   ```

3. Создайте админа заново через меню "👥 Управление админами" → "➕ Добавить админа"

4. Отправьте новый мастер-ключ админу через безопасный канал

### Логирование

Все действия экстренной блокировки логируются в таблицу `admin_actions` с типом `ADMIN_TERMINATED`.

Для просмотра логов:

```sql
SELECT * FROM admin_actions 
WHERE action_type = 'ADMIN_TERMINATED' 
ORDER BY created_at DESC 
LIMIT 10;
```

### Предотвращение компрометации

**Рекомендации:**

1. Используйте двухфакторную аутентификацию в Telegram
2. Регулярно меняйте мастер-ключи админов
3. Мониторьте логи админ-действий на подозрительную активность
4. Ограничьте количество супер-администраторов (минимум 2, максимум 5)
5. Регулярно проверяйте список активных админ-сессий

### Контакты для экстренных ситуаций

- Главный администратор: [указать контакт]
- Техническая поддержка: [указать контакт]
- Резервный доступ: [указать контакт]

---

**Последнее обновление:** 2025-01-19

