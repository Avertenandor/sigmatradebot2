# План исправления bot/handlers/start.py

## Проблема
Файл содержит множественные дубликаты обработчика `handle_show_password_again` (18+ экземпляров), что приводит к синтаксическим ошибкам.

## План действий

### Шаг 1: Завершить R1-12
- **Место**: строка 688-697
- **Действие**: Добавить `return` после `await state.clear()` и добавить `else` для случая когда кошелек привязан к другому пользователю

### Шаг 2: Удалить все дубликаты обработчика callback
- **Места**: строки 700-769, 809-878, и другие (всего 18+ экземпляров)
- **Действие**: Удалить все обработчики `@router.callback_query(F.data.startswith("show_password_"))` из середины файла

### Шаг 3: Добавить R1-19 в process_password_confirmation
- **Место**: после успешной регистрации (строка ~626)
- **Действие**: 
  - Сохранить plain password в Redis на 1 час
  - Добавить кнопку "🔑 Показать пароль ещё раз" в сообщение об успешной регистрации

### Шаг 4: Добавить обработчик callback в конец файла
- **Место**: после всех handlers регистрации (после `process_email`)
- **Действие**: Добавить один обработчик `handle_show_password_again` в конец файла

### Шаг 5: Проверка
- Проверить синтаксис: `python -m py_compile bot/handlers/start.py`
- Проверить линтер: `read_lints`
- Убедиться, что все handlers регистрации работают корректно

## Структура файла (после исправления)
1. Импорты
2. `cmd_start()` - обработчик /start
3. `process_wallet()` - обработка кошелька
4. `process_financial_password()` - обработка финпароля
5. `process_password_confirmation()` - подтверждение пароля и регистрация
6. `handle_contacts_choice()` - выбор контактов
7. `process_phone()` - обработка телефона
8. `process_email()` - обработка email
9. `handle_show_password_again()` - **ОДИН** обработчик callback в конце

