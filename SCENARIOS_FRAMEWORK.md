# SigmaTradeBot — Полный каркас ролей и сценариев

**Цель документа**: Систематизация всех бизнес-ролей и нормальных/аварийных сценариев работы с ботом для последующей проверки кода, тестов и выявления пробелов в функциональности.

**Последнее обновление:** 2025-01-28 - добавлены расширенные сценарии управления депозитами и ROI

---

## 📋 Основные разделы

* **R1** - Новый пользователь (регистрация)
* **R2** - Обычный пользователь
* **R3** - Пользователь-инвестор (депозиты)
* **R4** - Партнёр (рефералы)
* **R5** - Пользователь с ограничениями
* **R6** - Администратор (включая **R6-16–R6-40**: ROI Corridor & Deposit Management)
* **R7** - Фоновые процессы (включая **R7-7–R7-12**: Individual Reward Accrual)
* **R8** - Система уведомлений
* **R9** - Конкурентность и race conditions
* **R10** - Безопасность и аудит
* **R11** - Системная стабильность
* **R12** - Граничные случаи
* **R13** - Пользовательский UX
* **R14** - Мониторинг и алерты
* **R15** - Cross-role сценарии
* **R16** - Identity / multi-account
* **R17** - Product changes (включая **R17-6–R17-13**: Deposit Versioning)
* **R18** - Threat model / атаки

**🆕 Новые подразделы (2025-01-28):**
* **R6.4** - ROI Corridor Management (R6-16–R6-29) - динамическое управление доходностью
* **R6.5** - Deposit Management (R6-30–R6-40) - расширенное управление депозитами
* **R7.2** - Individual Reward Accrual (R7-7–R7-12) - индивидуальное начисление наград
* **R17.2** - Deposit Versioning (R17-6–R17-13) - версионирование условий депозитов

---

## 0. Роли пользователей (R1–R6)

Чтобы потом можно было свести в таблицу:

### R1 — Новый пользователь (гость / регистрирующийся)
Нет записи в `users` или только что регистрируется, депозитов ещё нет.

### R2 — Обычный пользователь с аккаунтом (без или с депозитами)
Запись в `users` есть, не заблокирован, видит полное меню:
`💰 Депозит`, `💸 Вывод`, `📦 Мои депозиты`, `👥 Рефералы`, `📊 Баланс`,
`💬 Поддержка`, `⚙️ Настройки`, `📖 Инструкции`, `📜 История`,
`✅ Пройти верификацию`, `🔑 Восстановить финпароль`.

### R3 — Пользователь-инвестор (есть хотя бы один депозит)
То же, что R2, но с активными депозитами и начислениями.

### R4 — Партнёр (есть реферальная активность)
Пользователь, у которого есть рефералы/реферальные начисления. Логика в `ReferralService`.

### R5 — Пользователь с ограничениями / санкциями

* pre-registration blacklist по телеграму или кошельку
* `BlacklistActionType.BLOCKED` + апелляции
* `BlacklistActionType.TERMINATED` (жёсткий бан)
* `earnings_blocked`, активный `finpass_recovery` и т.п.

### R6 — Администратор

* базовый `admin`
* расширенный `extended_admin`
* `super_admin` c управлением админами

**Примечание**: Роли R2–R5 могут накладываться (инвестор+партнёр+ограничения и т.д.), но для удобства сценарии разбиваем по доминирующей роли.

---

## 1. R1 — Новый пользователь (гость / регистрация)

### 1.1. Первый вход в бота

#### R1-1: /start без реферала, не в blacklist

* Telegram даёт апдейт `/start`
* Бот показывает приветствие с описанием и ссылкой на сайт `sigmatrade.org`
* Устанавливает состояние регистрации `waiting_for_wallet`
* Параллельно отправляет главное меню (для гостя — урезанное: `📖 Инструкции`, `💬 Поддержка`, `📝 Регистрация`)

#### R1-2: /start с параметром реферала

* Аналогично R1-1, но в `FSM state` кладётся `referrer_telegram_id`, чтобы потом привязать реферальную цепочку

#### R1-3: /start, но telegram_id уже в blacklist (registration_denied)

* `BlacklistService.is_blacklisted()` по telegram_id → true
* Сценарий: вместо запуска регистрации — сообщение «регистрация недоступна / обратитесь в поддержку», **нет** перехода в FSM регистрации

#### R1-4: /start при технической ошибке БД

* Неудачный доступ к БД (исключение при вытаскивании blacklist или сессии)
* Пользователю — «Системная ошибка, попробуйте позже / обратитесь в поддержку»
* В логах — `[ERROR]` + стектрейс

### 1.2. Кнопка «📖 Инструкции» (гость)

#### R1-5: Инструкции до регистрации

* Переход в handler `instructions` для неавторизованных
* Выводится:
  * базовое описание платформы
  * ссылка на сайт
  * напоминание, что для работы нужно пройти регистрацию

#### R1-6: Инструкции во время регистрации (FSM активно)

* Пользователь в состоянии `waiting_for_wallet` / `waiting_for_password`, нажимает «📖 Инструкции»
* Через `is_menu_button()` состояние сбрасывается, показывается главное меню, регистрация прерывается

### 1.3. Кнопка «💬 Поддержка» (гость)

#### R1-7: Создание обращения без регистрации

* Либо:
  * допускаем гостевые тикеты: создаётся тикет с пометкой `guest` и только telegram_id пользователя
  * либо отвечаем «Сначала пройдите регистрацию» (в коде нужно проверить текущую реализацию — сценарий всё равно должен существовать)

#### R1-8: Просмотр «📋 Мои обращения» у гостя

* Если гость — выводим «У вас нет обращений» либо принуждаем к регистрации

### 1.4. Кнопка «📝 Регистрация»

#### R1-9: Старт регистрации кнопкой

* Из урезанного меню гость жмёт «📝 Регистрация»
* `menu.py`:
  * обнуляет FSM
  * отправляет расширенное объяснение про адрес кошелька BSC, формат `0x...` 42 символа, ссылку на сайт
  * ставит состояние `RegistrationStates.waiting_for_wallet`

#### R1-10: Ввод кошелька — валидный

* Проверка формата и checksum адреса (валидатор BSC)
* Проверка rate-limit регистрации через `OperationRateLimiter`
* Проверка blacklist по адресу кошелька
* Создаётся `user`, связывается referral, переходим к шагу финпароля

#### R1-11: Ввод кошелька — невалидный формат

* Адрес не 42 символа / не `0x` / checksum не совпадает
* Ответ: «адрес некорректен, формат такой-то», повторный запрос

#### R1-12: Кошелёк уже привязан к существующему пользователю

* Поиск в `users` по `wallet_address`
* Варианты:
  * если это тот же telegram_id — предлагаем `/start` и используем старый аккаунт
  * если другой — выводим сообщение, что кошелёк занят, и стоп

#### R1-13: Кошелёк или telegram_id в blacklist (wallet-ban)

* `BlacklistService.is_blacklisted(telegram_id=None, wallet_address=addr)` → true
* Сообщение: регистрация запрещена, обращайтесь в поддержку

#### R1-14: Превышен лимит попыток регистрации

* `OperationRateLimiter.check_registration_limit()` вернул `allowed=False`
* Сообщение «Слишком много попыток регистрации, попробуйте позже»

#### R1-15: Пользователь во время шага кошелька нажимает любую кнопку из главного меню

* `is_menu_button()` → сброс FSM, показ главного меню; регистрация прервана

### 1.5. Финансовый пароль и контакты

#### R1-16: Выбор и ввод финпароля

* После валидации кошелька просим ввести пароль (`>=6` символов)
* Нормальный сценарий:
  * юзер вводит пароль → сообщение удаляется (безопасность)
  * пароль сохраняется в FSM
  * запрос подтверждения пароля
* Ошибки:
  * слишком короткий пароль → «минимум 6 символов», повтор ввода
  * юзер нажимает любую меню-кнопку → FSM очищается, переход в главное меню

#### R1-17: Подтверждение финпароля

* Совпал → регистрация коммитится, создаётся user + referral, хэш пароля пишется в БД
* Не совпал → сообщение «пароли не совпадают», возврат на шаг ввода
* Снова возможен выход в меню через кнопку

#### R1-18: Ввод контактов (телефон/e-mail)

* После успешной регистрации:
  * спрашиваем, хочет ли пользователь оставить контакты (`contacts_choice_keyboard`: «✅ Да, оставить контакты» / «⏭ Пропустить»)
* Сценарии:
  * соглашается → вводит телефон/e-mail, валидация, сохранение
  * пропускает → сразу главное меню

#### R1-19: Повторный показ финпароля

* В течение часа после регистрации финпароль можно ещё раз запросить (логика из CHANGELOG/TS версии)
* Кнопка типа «Показать пароль ещё раз» в успех-сообщении

---

## 2. R2 — Обычный пользователь (есть аккаунт, любые депозиты/рефы, не заблокирован)

Он видит **полное главное меню**.

### 2.1. Главное меню и навигация

#### R2-1: Кнопка «📊 Главное меню» / «⬅ Назад»

* В любом FSM состоянии:
  * state.clear()
  * подгружается актуальный `blacklist_entry`
  * строится `main_menu_reply_keyboard` с учётом admin-флага
  * отправляется приветствие и клавиатура

#### R2-2: «⚙️ Настройки» → settings_keyboard

* Пользователь попадает в подменю:
  * `👤 Мой профиль`
  * `💳 Мой кошелек`
  * `🔔 Настройки уведомлений`
  * `📝 Обновить контакты`
  * `📊 Главное меню`

#### R2-3: «📖 Инструкции» (после регистрации)

* Инструкции уже в контексте зарегистрированного пользователя (могут содержать инфу про депозиты, уровни и т.д.)
* Кнопка «📊 Главное меню» возвращает назад

#### R2-4: «📜 История»

* Переход в `transaction.py`:
  * если операций нет → «История пуста»
  * если есть → пагинация, фильтры по типам: депозиты, выводы, реферальные

### 2.2. Профиль / настройки

#### R2-5: «👤 Мой профиль»

* Показ:
  * баланс (доступный/в ожидании/выплаченный)
  * активные уровни депозитов
  * реферальная ссылка
  * статус верификации

#### R2-6: «💳 Мой кошелек»

* Статус кошелька:
  * текущий адрес
  * возможен ли запрос смены кошелька (через админов / wallet_change_request)

#### R2-7: «🔔 Настройки уведомлений»

* Три тумблера (депозиты, выводы, маркетинг)
* Сценарии:
  * включение/выключение каждого типа
  * возврат в главное меню

#### R2-8: «📝 Обновить контакты»

* Ввод нового телефона/e-mail
* Валидация, сохранение, подтверждающее сообщение

### 2.3. Верификация

#### R2-9: «✅ Пройти верификацию»

* Сценарии:
  * ещё не верифицирован:
    * запуск FSM верификации (ввод ФИО/страны/прочих данных — зависит от ТЗ)
    * опциональная ручная проверка админами → флаг `is_verified=True` в БД
    * после успеха сообщение и возможно автоматическое открытие части функциональности (вывод)
  * уже верифицирован:
    * ответ «вы уже верифицированы»

#### R2-10: Верификация при ошибках

* нехватка данных
* неверный формат документов
* превышен лимит попыток (rate-limit на верификацию)

---

## 3. R3 — Пользователь-инвестор (есть депозиты)

Тут основной упор на `💰 Депозит`, `📦 Мои депозиты`, `📊 Баланс` и `💸 Вывод`.

### 3.1. Кнопка «💰 Депозит» → deposit_keyboard

#### R3-1: Открытие меню депозитов

* Показывается `deposit_keyboard()` с уровнями 1–5 и статусами:
  * активен
  * доступен к покупке
  * заблокирован (нет предыдущего / нет партнёров)

#### R3-2: Пополнение доступного уровня

* Пользователь жмёт «💰 Пополнить Level N (...)»:
  * проверка бизнес-правил (предыдущие уровни, лимит по количеству депозитов и т.д.)
  * показ инструкции:
    * сколько USDT надо отправить
    * на какой адрес
    * в какой сети
  * создаётся `pending` депозит + ожидаемый объём
* **См. также:** R17-8 (привязка депозита к версии), R17-10 (отображение условий), R6-26 (настройка сумм уровней)

#### R3-3: Пополнение уже активного уровня

* Кнопка «✅ Level N (...) — Активен»:
  * либо запрет повторной покупки уровня
  * либо переход к пополнению (если архитектура позволяет несколько депозитов на уровне)

#### R3-4: Попытка купить заблокированный уровень

* Кнопка «🔒 Level N — Нет предыдущего/Нет партнёров/Недоступен»:
  * сообщение с причиной (нет Level N-1; недостаточно партнёров; другие ограничения)
  * предложение сначала выполнить условие (напр. пригласить X рефералов)

#### R3-5: Обработка входящих депозитов (фоновый сценарий)

* Blockchain-монитор находит `Transfer USDT` на системный кошелёк, сопоставляет с ожидаемым депозитом и подтверждает
* Если tx не найдена — остаётся в ожидании, запускается «умный поиск по истории» при ошибках
* После подтверждения депозита устанавливается `next_accrual_at` для первого начисления
* **См. также:** R7-7 (individual reward accrual), R7-9 (расчет next_accrual_at), R17-8 (привязка к версии)

#### R3-6: Ошибки депозитов

* пользователь отправил меньше суммы → депозит отклонён/обновлён на меньший уровень
* отправил на чужой адрес → депозит не находится → через `searchBlockchainForDeposit()` попытка восстановить
* истёк тайм-аут депозита → переводим в FAILED и уведомляем

### 3.2. «📦 Мои депозиты»

#### R3-7: Просмотр списка депозитов

* Если активных нет → «У вас нет активных депозитов, создайте через "💰 Депозит"»
* Если есть:
  * по каждому депозиту:
    * уровень, дата открытия, сумма
    * прогресс ROI (например, `x% из 500%`), статус «активен/закрыт», эмодзи статуса

#### R3-8: Завершённый депозит (ROI достигнут)

* `is_roi_completed=True` → статус «Закрыт (ROI 500%)»
* Пользователь видит, что повторные начисления по этому депозиту не идут

### 3.3. Баланс и вывод

#### R3-9: «📊 Баланс»

* Разбивка:
  * доступный баланс
  * в ожидании
  * выплачено
  * сумма по депозитам, рефералке и т.п.

#### R3-10: «💸 Вывод» → withdrawal_keyboard

* Показывается меню:
  * `💸 Вывести всю сумму`
  * `💵 Вывести указанную сумму`
  * `📜 История выводов`
  * `📊 Главное меню`

#### R3-11: «💸 Вывести всю сумму»

* Проверка:
  * user есть
  * `is_verified=True` (иначе сообщение «вывод недоступен до верификации»)
  * rate-limit по выводам (лимиты в час/день)
  * расчёт доступного баланса через `UserService.get_user_balance()` в короткой транзакции
  * минимальная сумма вывода
* Если всё ок:
  * сохраняется сумма в FSM
  * пользователя просят ввести финпароль

#### R3-12: «💵 Вывести указанную сумму»

* Аналогично, но:
  * пользователь вводит сумму
  * проверка:
    * число >0
    * не превышает `available_balance`
    * не меньше минимума
  * Далее — шаг финпароля

#### R3-13: Шаг финпароля при выводе

* Пароль верен → создаётся заявка на вывод, статус `PENDING`, ответ с ID заявки и текстом «на рассмотрении 1–24 часа»
* Пароль неверен:
  * сообщение об ошибке
  * опционально счётчик неверных попыток (блокировка после N?)

#### R3-14: «📜 История выводов»

* Если заявок нет → сообщение «История выводов пуста»
* Если есть:
  * список, каждая строка:
    * сумма, статус (`PENDING/CONFIRMED/REJECTED`), дата
  * пролистывание страниц по кнопкам/командам (если реализовано)

#### R3-15: Ошибки на выводе

* Нет сессии к БД / `session_factory` → «Системная ошибка, отправьте /start»
* Внутренние ошибки сервиса вывода → «Ошибка при создании заявки, попробуйте позже» + возврат в главное меню

---

## 4. R4 — Партнёр (реферальная логика)

Функционал на кнопке «👥 Рефералы» + `referral_keyboard`.

### 4.1. Меню рефералов

#### R4-1: Вход в реферальное меню

* `👥 Рефералы` → показ:
  * `👥 Мои рефералы`
  * `💰 Мой заработок`
  * `📊 Статистика рефералов`
  * `📊 Главное меню`

### 4.2. «👥 Мои рефералы»

#### R4-2: Нет ни одного реферала

* `ReferralRepository.get_by_referrer()` возвращает пустой список
* Сообщение: «У вас пока нет рефералов»

#### R4-3: Есть рефералы (по уровням)

* Вывод:
  * список Level 1 с датой регистрации и суммой заработанного на каждом
  * возможно, переключение уровня (1–3) с пагинацией

#### R4-4: Пагинация длинных списков

* При большом числе рефералов используется `page`/`limit` в `get_referrals_by_level`
* Пользователь перелистывает страницы кнопками/командами

### 4.3. «💰 Мой заработок»

#### R4-5: Просмотр суммарного заработка

* `ReferralEarningRepository` считает total по unpaid/paid
* Показываются:
  * общий заработок
  * сколько ещё не выплачено
  * сколько переведено на кошелёк

#### R4-6: Нулевой заработок

* Сообщение «У вас пока нет реферальных начислений»

### 4.4. «📊 Статистика рефералов»

#### R4-7: Статистика по уровням

* `ReferralRepository.count_by_level()` даёт числа по каждому уровню
* Пользователь видит:
  * сколько рефералов уровня 1/2/3
  * возможно, конверсию в проценты

#### R4-8: Ошибки/edge-кейсы

* Реферальная петля (если кто-то пытается создать циклическую цепочку) — ловится в `create_referral_relationships` и возвращает ошибку «Нельзя создать циклическую реферальную цепочку» (это для регистрации, но сценарий важно сохранить)

---

## 5. R5 — Пользователь с ограничениями / санкциями

### 5.1. Blacklist до регистрации

#### R5-1: Телеграм в blacklist (registration_denied)

* Любая попытка `/start` или «📝 Регистрация»:
  * проверка `BlacklistService.is_blacklisted(telegram_id=...)`
  * ответ «Регистрация недоступна» + контакт поддержки
  * FSM регистрации не запускается

#### R5-2: Кошелёк в blacklist

* На шаге ввода кошелька — см. R1-13

### 5.2. BLOCKED после регистрации

#### R5-3: Меню заблокированного пользователя

* При построении `main_menu_reply_keyboard`:
  * если есть активный `Blacklist` с `action_type=BLOCKED`, то пользователю показывается **только** кнопка «📝 Подать апелляцию»

#### R5-4: Нажатие «📝 Подать апелляцию»

* Проверки в `appeal.start_appeal`:
  * нет активной записи в blacklist → «нет активной блокировки»
  * action_type ≠ BLOCKED → «апелляция только для заблокированных аккаунтов»
  * истёк срок апелляции (3 дня) → «срок подачи истёк»
  * уже есть активная апелляция → показываем статус и дату, просим ждать
  * превышен лимит открытых апелляций → ошибка и просьба ждать
  * в остальных случаях — запускаем FSM, просим текст апелляции

#### R5-5: Ввод текста апелляции

* Если текст < 20 символов → «слишком короткий, минимум 20»
* Иначе:
  * создаётся запись в `appeals`
  * создаётся тикет в системе поддержки
  * статус `PENDING`, пользователю подтверждение

### 5.3. TERMINATED

#### R5-6: Пользователь с `BlacklistActionType.TERMINATED`

* `BanMiddleware` полностью глотает апдейты такого пользователя, хэндлеры не вызываются (поведение описано в сервисах blacklist и middleware; фактически для пользователя бот выглядит «мёртвым»)

### 5.4. Ограничения по финпаролю и выплатам

#### R5-7: Активный запрос восстановления финпароля

* При попытке запустить восстановление:
  * если есть `pending` запрос → «у вас уже есть активный запрос, дождитесь решения» + главное меню

#### R5-8: Запрос одобрен, но ещё не использован новый пароль

* `FinpassRecoveryService.has_active_recovery()` → true:
  * сообщение, что новый пароль уже отправлен
  * выплаты заблокированы до первого успешного вывода с новым паролем

#### R5-9: Создание нового запроса восстановления

* Пользователь проходит ветку:
  * «🔑 Восстановить финпароль» → предупреждение → «❌ Отмена / 📊 Главное меню» либо ввод причины
  * слишком короткая причина (<10) → просим расписать подробнее
  * нормальная причина → создаётся заявка, блокируются выплаты, улетает уведомление админам

---

## 6. R6 — Администратор (admin / extended / super_admin)

Основные кнопки админ-панели берём из `admin_keyboard()`:
`📊 Статистика`, `👥 Управление пользователями`, `💸 Заявки на вывод`,
`📢 Рассылка`, `🆘 Техподдержка`, `🔐 Управление кошельком`,
`🚫 Управление черным списком`, `⚙️ Настроить уровни депозитов`,
плюс `👥 Управление админами` только для super_admin.

### 6.1. Вход и сессии

#### R6-1: /admin → запрос мастер-ключа

* Команда `/admin`:
  * если юзер имеет флаг `is_admin` — просим ввести мастер-ключ
  * иначе → «эта функция доступна только администраторам»

#### R6-2: Ввод мастер-ключа (успех)

* `AdminService.login(...)`:
  * создаёт/обновляет `AdminSession` (Redis/БД)
  * пишет лог
  * FSM состояние сбрасывается
  * показывается админ-панель

#### R6-3: Ввод мастер-ключа (ошибка)

* Неверный ключ → сообщение «Ошибка аутентификации / попробуйте ещё раз»
* Несколько неверных попыток подряд → возможно, запись в `admin_actions` и ограничение (см. тесты системы)

#### R6-4: Авто-разлогин при бездействии

* Если `last_activity` > 15 минут:
  * при любом действии в админ-панели → запрос мастер-ключа повторно

#### R6-5: Очистка сессий

* Фоновая задача `admin_session_cleanup` снимает старые сессии
* Сценарий: админ спустя день пытается нажать кнопку в панели → сессия истекла, его перекидывает на повторную аутентификацию

### 6.2. Админ-панель (базовый admin)

#### R6-6: «📊 Статистика»

* Показ:
  * количество пользователей, депозитов, суммарный объём, реферальные выплаты и т.п.

#### R6-7: «👥 Управление пользователями» → admin_users_keyboard

* Кнопки:
  * `🔍 Найти пользователя`
  * `👥 Список пользователей`
  * `🚫 Заблокировать пользователя`
  * `⚠️ Терминировать аккаунт`
  * `👑 Админ-панель`
* Сценарии:
  * поиск по username/ID
  * просмотр карточки пользователя
  * бан (создание записи в blacklist с `BLOCKED`, либо `TERMINATED`)
  * разбан (деактивация записи)

#### R6-8: «💸 Заявки на вывод» → admin_withdrawals_keyboard

* Кнопки:
  * `⏳ Ожидающие выводы`
  * `✅ Одобренные выводы`
  * `❌ Отклоненные выводы`
  * `👑 Админ-панель`
* Сценарии:
  * просмотр списка `PENDING`
  * выбор заявки → подтверждение:
    * при одобрении:
      * попытка отправки USDT через blockchain-сервис
      * при успехе статус `CONFIRMED`, лог в `admin_actions`, уведомление пользователю
      * при ошибке — запись в retry/payment-DLQ
    * при отказе:
      * статус `REJECTED`, возврат суммы на баланс пользователя, уведомление

#### R6-9: «📢 Рассылка»

* Запуск сценария массовой рассылки:
  * ввод текста/медиа
  * проверка cooldown (например, 15 минут между рассылками), лимита 15 сообщений/сек
  * отображение статистики: отправлено/ошибок
  * ошибки → попадание в retry-систему уведомлений

#### R6-10: «🆘 Техподдержка»

* Просмотр очереди тикетов поддержки (особенно апелляций)
* Переход к связанной переписке с пользователем

#### R6-11: «🔐 Управление кошельком» → admin_wallet_keyboard

* Статус системных кошельков (балансы, активные ключи)
* Сценарии:
  * добавление/обновление ключа/seed
  * удаление ключа
  * проверки доступа (только extended/super_admin)

#### R6-12: «🚫 Управление черным списком» → admin_blacklist_keyboard

* `➕ Добавить в blacklist`, `🗑️ Удалить из blacklist`, `📝 Редактировать тексты`, `👑 Админ-панель`
* Сценарии:
  * бан по telegram_id
  * бан по кошельку (включая wallet-only бан)
  * редактирование шаблонов текстов для блокировки / апелляции

#### R6-13: «⚙️ Настроить уровни депозитов» → admin_deposit_settings_keyboard

* Возможность менять:
  * суммы по уровням
  * лимиты
  * описание
  * включение/выключение уровня

### 6.3. Super_admin-специфика

#### R6-14: «👥 Управление админами» → admin_management_keyboard

* Доступно только при `is_super_admin=True`
* Кнопки:
  * `➕ Добавить админа`
  * `📋 Список админов`
  * `🗑️ Удалить админа`
  * `🛑 Экстренно заблокировать админа`
  * `👑 Админ-панель`
* Сценарии:
  * создание админа с ролью (admin/extended/super_admin), отправка мастер-ключа в Telegram
  * просмотр списка админов, их ролей и создателя
  * удаление админа (с защитой от удаления себя и последнего super_admin)
  * экстренная блокировка админа (создаёт запись в blacklist/TERMINATED и вырубает все его сессии)

#### R6-15: Аудит действий админов

* Любое действие админа (создание/удаление админа, бан пользователя, одобрение вывода, рассылка) логируется в `admin_actions`
* Сценарий просмотра через SQL/интерфейс

### 6.4. ROI Corridor Management (динамическое управление доходностью)

#### R6-16: Вход в меню управления коридорами доходности

* **Кнопка:** `💰 Коридоры доходности` (из меню "Управление депозитами")
* **FSM State:** - (без состояния, прямой показ меню)
* **Входные данные:** нет
* **Валидация:**
  * проверка `is_admin=True` через middleware
  * проверка активной admin session
* **Бизнес-логика:**
  * показ объяснения двух режимов начисления:
    * **Custom** - каждый пользователь получает случайный процент из коридора ежедневно (weighted к нижним значениям)
    * **Поровну** - все пользователи получают одинаковый фиксированный процент
  * объяснение применения изменений:
    * **Текущая сессия** - изменения к ближайшему начислению (в течение периода)
    * **Следующая сессия** - изменения через одно начисление
  * показ клавиатуры с опциями:
    * `⚙️ Настроить коридоры` - старт настройки коридора для уровня
    * `📊 Текущие настройки` - просмотр всех текущих коридоров (уровни 1-5)
    * `📜 История изменений` - просмотр истории изменений коридоров
    * `⏱ Настроить период начисления` - изменение периода (1-24 часа)
    * `💵 Настроить суммы уровней` - изменение сумм с версионированием
    * `◀️ Назад в управление депозитами` - возврат
* **Обновление БД:** нет
* **Уведомления:** нет
* **Логирование:** вход в меню
* **Обработка ошибок:**
  * не админ → сообщение "Эта функция доступна только администраторам"
* **Edge cases:**
  * админ без активной сессии → редирект на ввод мастер-ключа
* **Связанные сценарии:** R6-17, R6-18, R6-25, R6-26, R6-27, R6-30
* **Handler:** `bot/handlers/admin/roi_corridor.py:show_roi_corridor_menu()`
* **Клавиатура:** `admin_roi_corridor_menu_keyboard()`

#### R6-17: Просмотр текущих настроек всех коридоров

* **Кнопка:** `📊 Текущие настройки` (из меню коридоров)
* **FSM State:** - (без состояния)
* **Входные данные:** нет
* **Валидация:** проверка `is_admin=True`
* **Бизнес-логика:**
  * для каждого уровня 1-5:
    * вызов `RoiCorridorService.get_corridor_config(level)`
    * получение из GlobalSettings JSON:
      * `LEVEL_{N}_ROI_MODE` (custom/equal)
      * `LEVEL_{N}_ROI_MIN`, `LEVEL_{N}_ROI_MAX` (для custom)
      * `LEVEL_{N}_ROI_FIXED` (для equal)
    * форматирование отображения:
      * emoji уровня (1️⃣-5️⃣)
      * режим (Custom/Поровну)
      * значения (коридор или фиксированный процент)
  * получение `REWARD_ACCRUAL_PERIOD_HOURS` из GlobalSettings
  * показ периода начисления
* **Обновление БД:** нет (только чтение)
* **Уведомления:** нет
* **Логирование:** просмотр настроек админом
* **Обработка ошибок:**
  * ошибка чтения GlobalSettings → "Системная ошибка"
  * отсутствие настроек для уровня → использование defaults (custom, 0.8%-10%)
* **Edge cases:**
  * уровень не настроен → показ defaults
  * некорректные значения в JSON → fallback на безопасные defaults
* **Связанные сценарии:** R6-16, R6-18
* **Handler:** `bot/handlers/admin/roi_corridor.py:show_current_settings()`
* **Сервис:** `RoiCorridorService.get_corridor_config()`

#### R6-18: Выбор уровня для настройки коридора

* **Кнопка:** `⚙️ Настроить коридоры` (из меню коридоров)
* **FSM State:** `AdminRoiCorridorStates.selecting_level`
* **Входные данные:** выбор уровня из клавиатуры (1-5)
* **Валидация:**
  * проверка is_admin
  * извлечение номера уровня из текста кнопки (split + int)
  * проверка диапазона: 1 <= level <= 5
* **Бизнес-логика:**
  * установка FSM состояния `selecting_level`
  * показ клавиатуры с кнопками:
    * `Уровень 1`, `Уровень 2`, `Уровень 3`, `Уровень 4`, `Уровень 5`
    * `👑 Админ-панель` - экстренный выход
    * `◀️ Отмена` - возврат в меню коридоров
  * при выборе уровня:
    * сохранение `level` в state.data
    * переход в состояние `selecting_mode`
    * показ клавиатуры выбора режима
* **Обновление БД:** нет
* **Уведомления:** нет
* **Логирование:** выбор уровня для настройки
* **Обработка ошибок:**
  * неверный формат уровня → сообщение "Неверный уровень", повторный запрос
  * нажатие `👑 Админ-панель` → `clear_state_preserve_admin_token()`, вызов `handle_admin_panel_button()`
  * нажатие `◀️ Отмена` → clear state, возврат в меню коридоров
* **Edge cases:**
  * ввод текста вместо кнопки → обработка через try-except, повторный запрос
* **Связанные сценарии:** R6-16, R6-19, R6-20
* **Handler:** `bot/handlers/admin/roi_corridor.py:process_level_selection()`

#### R6-19: Выбор режима "Custom" и ввод коридора

* **Кнопка:** `🎲 Custom` (из меню выбора режима)
* **FSM State:** `AdminRoiCorridorStates.selecting_mode` → `entering_min` → `entering_max`
* **Входные данные:**
  1. Выбор режима "Custom"
  2. Ввод минимального процента (Decimal)
  3. Ввод максимального процента (Decimal)
* **Валидация:**
  * минимум >= 0
  * максимум >= 0
  * максимум > минимум
  * предупреждение если min < 0.5% (слишком низко)
  * предупреждение если max > 20% (слишком высоко)
* **Бизнес-логика:**
  * **Шаг 1/4:** Выбор режима
    * сохранение `mode="custom"`, `mode_text="Custom (случайный из коридора)"` в state
    * переход в `entering_min`
    * показ запроса минимума с примером ("0.8")
  * **Шаг 2/4:** Ввод минимума
    * парсинг Decimal из текста
    * проверка >= 0
    * сохранение `roi_min` в state (как float для JSON serialization)
    * переход в `entering_max`
    * показ запроса максимума с текущим минимумом
  * **Шаг 3/4:** Ввод максимума
    * парсинг Decimal
    * проверка >= 0
    * проверка max > min
    * сохранение `roi_max` в state
    * переход в `selecting_applies_to`
    * показ клавиатуры "Когда применить?"
* **Обновление БД:** нет (только в state)
* **Уведомления:** нет
* **Логирование:**
  * лог выбора режима
  * лог ввода min/max значений
* **Обработка ошибок:**
  * неверный формат числа → "Неверный формат. Введите число (например: `0.8`)", повторный запрос
  * max <= min → "Максимум должен быть больше минимума", повторный запрос max
  * отрицательные значения → "Проценты не могут быть отрицательными", повтор
* **Edge cases:**
  * ввод с пробелами/лишними символами → strip() перед парсингом
  * очень большие числа (>1000%) → допускается, но предупреждение
* **Связанные сценарии:** R6-18 (предыдущий), R6-21 (следующий), R6-23 (подтверждение)
* **Handler:** `bot/handlers/admin/roi_corridor.py:process_mode_selection()`, `process_min_input()`, `process_max_input()`

#### R6-20: Выбор режима "Поровну" и ввод фиксированного процента

* **Кнопка:** `📊 Поровну` (из меню выбора режима)
* **FSM State:** `AdminRoiCorridorStates.selecting_mode` → `entering_fixed`
* **Входные данные:**
  1. Выбор режима "Поровну"
  2. Ввод фиксированного процента (Decimal)
* **Валидация:**
  * процент >= 0
  * предупреждение если < 0.5% или > 20%
* **Бизнес-логика:**
  * **Шаг 1/3:** Выбор режима
    * сохранение `mode="equal"`, `mode_text="Поровну (фиксированный для всех)"` в state
    * переход в `entering_fixed`
    * показ запроса фиксированного процента с примером ("5.5")
  * **Шаг 2/3:** Ввод процента
    * парсинг Decimal из текста
    * проверка >= 0
    * сохранение `roi_fixed` в state (как float)
    * переход в `selecting_applies_to`
    * показ клавиатуры "Когда применить?"
* **Обновление БД:** нет (только в state)
* **Уведомления:** нет
* **Логирование:** лог выбора режима и ввода фиксированного процента
* **Обработка ошибок:**
  * неверный формат → "Неверный формат. Введите число", повтор
  * отрицательное значение → ошибка, повтор
* **Edge cases:**
  * 0% процент → допускается технически, но странно (warning в логах)
  * очень большой процент (>50%) → допускается, но предупреждение
* **Связанные сценарии:** R6-18, R6-21, R6-23
* **Handler:** `bot/handlers/admin/roi_corridor.py:process_mode_selection()`, `process_fixed_input()`

#### R6-21: Выбор применения изменений (текущая/следующая сессия)

* **Кнопка:** `Применить к текущей сессии` или `Применить к следующей сессии`
* **FSM State:** `AdminRoiCorridorStates.selecting_applies_to` → `entering_reason`
* **Входные данные:** выбор сессии из клавиатуры
* **Валидация:**
  * проверка что текст содержит "текущей" или "следующей"
  * если нет - повторный запрос
* **Бизнес-логика:**
  * парсинг выбора:
    * "текущей" → `applies_to="current"`, `applies_text="текущей сессии (ближайшее начисление)"`
    * "следующей" → `applies_to="next"`, `applies_text="следующей сессии (через одно начисление)"`
  * сохранение в state
  * переход в `entering_reason`
  * показ запроса причины изменения (опционально)
* **Семантика применения:**
  * **current:** изменения записываются в `GlobalSettings.roi_settings["LEVEL_{N}_ROI_*"]`
    * применяются к следующему запуску reward accrual task (в течение периода)
    * все активные депозиты получат новые rates при их следующем начислении
  * **next:** изменения записываются в `GlobalSettings.roi_settings["LEVEL_{N}_ROI_*_NEXT"]`
    * не применяются сразу
    * при следующем запуске reward task вызывается `apply_next_session_settings()`
    * `*_NEXT` значения копируются в основные `*`, затем удаляются
    * применяются только через одно начисление
* **Обновление БД:** нет (сохранение в state для последующего применения)
* **Уведомления:** нет (на этом шаге)
* **Логирование:** выбор применения
* **Обработка ошибок:**
  * неверный выбор → повторный запрос с клавиатурой
  * нажатие `👑 Админ-панель` → clear state, выход в админ-панель
  * нажатие `◀️ Отмена` → clear state, возврат в меню коридоров
* **Edge cases:**
  * частичное совпадение текста ("текущ" в "текущая") → обработка через `in` проверку
* **Связанные сценарии:** R6-19 или R6-20 (предыдущие), R6-22 (следующий), R6-28 (применение next)
* **Handler:** `bot/handlers/admin/roi_corridor.py:process_applies_to()`
* **Клавиатура:** `admin_roi_applies_to_keyboard()`

#### R6-22: Ввод причины изменения (опциональный шаг)

* **Кнопка:** нет (текстовый ввод)
* **FSM State:** `AdminRoiCorridorStates.entering_reason` → показ подтверждения
* **Входные данные:** текст причины или "Пропустить"
* **Валидация:**
  * если текст = "Пропустить" или "skip" (case-insensitive) → reason = None
  * иначе → сохранение текста как есть (без минимальной длины)
* **Бизнес-логика:**
  * сохранение `reason` в state (может быть None)
  * немедленный переход к показу подтверждения через `show_confirmation()`
  * причина используется для:
    * логирования в `deposit_corridor_history.reason`
    * показа в истории изменений
    * прозрачности для других админов
* **Обновление БД:** нет (сохранение в state)
* **Уведомления:** нет
* **Логирование:** ввод причины (текст или skip)
* **Обработка ошибок:**
  * любой текст допустим (включая emoji, спецсимволы)
  * если нет текста (None от Telegram) → трактуется как пропуск
* **Edge cases:**
  * очень длинный текст (>500 символов) → допускается, но может обрезаться в UI
  * нажатие кнопки меню во время ввода → FSM сбрасывается через `is_menu_button()`
* **Связанные сценарии:** R6-21 (предыдущий), R6-23 (следующий - подтверждение)
* **Handler:** `bot/handlers/admin/roi_corridor.py:process_reason_input()`
* **Примеры причин:**
  * "Экстренное снижение доходности"
  * "Плановое повышение для привлечения пользователей"
  * "Корректировка после анализа финансов"

#### R6-23: Подтверждение изменений коридора с валидацией

* **Кнопка:** `✅ Да, применить` или `❌ Нет, отменить`
* **FSM State:** `AdminRoiCorridorStates.confirming` → clear state
* **Входные данные:** подтверждение (Да/Нет)
* **Валидация:**
  * проверка наличия всех данных в state: level, mode, values, applies_to
  * проверка наличия admin_id в handler data
  * вызов `RoiCorridorService.validate_corridor_settings()` для Custom режима
* **Бизнес-логика:**
  * **Показ подтверждения** (вызывается из R6-22):
    * формирование summary всех настроек:
      * Уровень: X
      * Режим: Custom/Поровну
      * Значение: коридор или фиксированный
      * Применить к: текущей/следующей сессии
      * Причина: (если указана)
    * получение warnings от validate_corridor_settings():
      * если roi_min < 0.5% → warning "Очень низкий минимум"
      * если roi_max > 20% → warning "Очень высокий максимум"
      * если (max - min) < 1% → warning "Узкий коридор"
    * добавление warnings в текст с "⚠️ Требуется подтверждение!"
    * показ клавиатуры: `✅ Да, применить` / `❌ Нет, отменить`
  * **Обработка подтверждения:**
    * если "Нет" или "отменить" → clear state, сообщение "Изменения отменены"
    * если "Да" или "применить":
      * конвертация float → Decimal для всех процентов
      * вызов `RoiCorridorService.set_corridor()`:
        * валидация параметров на уровне сервиса
        * создание записи в `deposit_corridor_history`
        * применение настроек в GlobalSettings:
          * если `applies_to="current"`:
            * `LEVEL_{N}_ROI_MODE = mode`
            * `LEVEL_{N}_ROI_MIN/MAX = values` (для custom)
            * `LEVEL_{N}_ROI_FIXED = value` (для equal)
          * если `applies_to="next"`:
            * `LEVEL_{N}_ROI_MODE_NEXT = mode`
            * `LEVEL_{N}_ROI_MIN/MAX_NEXT = values`
        * commit транзакции
      * при успехе:
        * сообщение "✅ Настройки успешно применены!"
        * вызов `_notify_other_admins()` для уведомления extended_admin'ов
        * логирование события с деталями
      * при ошибке:
        * сообщение "❌ Ошибка: {error}"
        * rollback транзакции
* **Обновление БД:**
  * `GlobalSettings.roi_settings` (JSON field) - обновление ключей
  * `deposit_corridor_history` - новая запись (INSERT)
* **Уведомления:**
  * всем extended_admin через `send_telegram_message()`
  * текст: уровень, режим, значение, применено к, изменил (admin ID)
  * исключая админа-инициатора
* **Логирование:**
  * INFO: "Corridor settings updated" с extra: level, mode, applies_to, admin_id
  * при ошибке отправки уведомления: ERROR с деталями
* **Обработка ошибок:**
  * ошибка валидации в сервисе → возврат (False, error_message)
  * ошибка commit → exception, rollback, сообщение пользователю
  * admin_id не найден → "Ошибка: admin_id не найден"
  * потеря данных в state → "Ошибка: данные потеряны"
* **Edge cases:**
  * одновременная настройка разных уровней разными админами → нет конфликта (разные ключи в JSON)
  * настройка того же уровня двумя админами → последний выигрывает (no locking)
  * applies_to="next" + повторная настройка до применения → перезапись _NEXT значений
* **Связанные сценарии:** R6-21, R6-22, R6-24 (уведомления), R6-27 (история)
* **Handler:** `bot/handlers/admin/roi_corridor.py:show_confirmation()`, `process_confirmation()`
* **Сервис:** `RoiCorridorService.set_corridor()`, `validate_corridor_settings()`

#### R6-24: Уведомление других админов об изменении коридора

* **Кнопка:** нет (автоматический процесс)
* **FSM State:** - (фоновая операция)
* **Входные данные:** admin_id, level, mode_text, config_text, applies_text
* **Валидация:** нет
* **Бизнес-логика:**
  * получение списка всех extended_admin через `AdminRepository.get_extended_admins()`
  * фильтрация: исключить админа-инициатора (admin.id != admin_id)
  * для каждого админа:
    * формирование текста уведомления:
      * "🔔 Изменены настройки коридора доходности"
      * Уровень: X
      * Режим: Custom/Поровну
      * Значение: коридор/фиксированный
      * Применено к: текущей/следующей
      * Изменил: Admin ID Y
    * попытка отправки через `send_telegram_message(telegram_id, text)`
    * при ошибке:
      * логирование ERROR с admin.id и текстом ошибки
      * продолжение со следующим админом (не блокирует процесс)
* **Обновление БД:** нет
* **Уведомления:** да (основная функция)
* **Логирование:**
  * INFO: попытка уведомления N админов
  * ERROR: неудача отправки конкретному админу с причиной
* **Обработка ошибок:**
  * TelegramAPIError (rate limit, bot blocked) → логирование, skip
  * ошибка получения списка админов → логирование, весь процесс пропускается
  * любая ошибка не блокирует основной процесс настройки
* **Edge cases:**
  * админ заблокировал бота → ошибка 403, логируется, продолжаем
  * telegram_id админа изменился → не найдет, логируется
  * нет других админов (единственный admin) → список пустой, ничего не отправляется
* **Связанные сценарии:** R6-23 (триггер), R8-2 (обработка bot blocked)
* **Handler:** `bot/handlers/admin/roi_corridor.py:_notify_other_admins()`

#### R6-25: Настройка периода начисления наград

* **Кнопка:** `⏱ Настроить период начисления` (из меню коридоров)
* **FSM State:** `AdminRoiCorridorStates.setting_period` → `confirming_period`
* **Входные данные:**
  1. Новый период в часах (int, 1-24)
  2. Подтверждение (Да/Нет)
* **Валидация:**
  * период - целое число
  * 1 <= период <= 24
  * наличие admin_id
* **Бизнес-логика:**
  * **Шаг 1:** Показ текущего периода
    * получение `REWARD_ACCRUAL_PERIOD_HOURS` из GlobalSettings через `get_accrual_period_hours()`
    * переход в состояние `setting_period`
    * показ инструкции:
      * текущий период
      * запрос нового (1-24)
      * пример: 6 для каждые 6 часов
      * важно: период применяется индивидуально для каждого пользователя
  * **Шаг 2:** Обработка ввода
    * парсинг int
    * проверка диапазона 1-24
    * сохранение `new_period_hours` в state
    * переход в `confirming_period`
    * показ подтверждения:
      * "Новый период: X часов"
      * Warning: "Изменения применятся к следующему циклу для всех депозитов"
      * клавиатура: Да/Нет
  * **Шаг 3:** Подтверждение
    * если "Нет" → clear state, "Изменения отменены"
    * если "Да":
      * вызов `RoiCorridorService.set_accrual_period_hours(hours, admin_id)`
      * сохранение в `GlobalSettings.roi_settings["REWARD_ACCRUAL_PERIOD_HOURS"]`
      * commit
      * сообщение "✅ Период начисления обновлён!"
      * вызов `_notify_other_admins_period()` для уведомления
      * логирование с hours и admin_id
* **Обновление БД:**
  * `GlobalSettings.roi_settings["REWARD_ACCRUAL_PERIOD_HOURS"]` = новое значение
* **Уведомления:**
  * всем extended_admin: "🔔 Изменен период начисления", новый период, изменил (admin ID)
* **Логирование:**
  * INFO: "Accrual period updated" с hours и admin_id
  * ERROR: при ошибках уведомления
* **Обработка ошибок:**
  * не число → "Неверный формат. Введите целое число от 1 до 24"
  * вне диапазона → "Период должен быть от 1 до 24 часов"
  * потеря данных (hours/admin_id) → "Ошибка: данные потеряны"
* **Edge cases:**
  * очень короткий период (1 час) → допускается, может создать высокую нагрузку
  * изменение периода не сбрасывает `next_accrual_at` существующих депозитов
  * каждый депозит продолжает по своему расписанию
  * новый период применится при расчете следующего `next_accrual_at` для каждого депозита
* **Связанные сценарии:** R6-16, R7-7 (использование периода), R7-8 (расчет next_accrual_at)
* **Handler:** `bot/handlers/admin/roi_corridor.py:start_period_setup()`, `process_period_input()`, `process_period_confirmation()`
* **Сервис:** `RoiCorridorService.set_accrual_period_hours()`, `get_accrual_period_hours()`

#### R6-26: Настройка сумм уровней с версионированием

* **Кнопка:** `💵 Настроить суммы уровней` (из меню коридоров)
* **FSM State:** `AdminRoiCorridorStates.selecting_level_amount` → `setting_level_amount` → `confirming_level_amount`
* **Входные данные:**
  1. Выбор уровня (1-5)
  2. Новая сумма (Decimal)
  3. Подтверждение
* **Валидация:**
  * уровень: 1-5
  * сумма > 0
  * наличие admin_id
* **Бизнес-логика:**
  * **Шаг 1:** Выбор уровня
    * установка состояния `selecting_level_amount`
    * показ клавиатуры с уровнями 1-5
  * **Шаг 2:** Обработка выбора уровня
    * парсинг номера уровня из текста кнопки
    * получение `current_version` через `DepositLevelVersionRepository.get_current_version(level)`
    * если есть версия → показ текущей суммы
    * если нет → "Не настроен"
    * сохранение `level`, `current_amount` в state
    * переход в `setting_level_amount`
    * запрос новой суммы с примером
  * **Шаг 3:** Ввод новой суммы
    * парсинг Decimal
    * проверка > 0
    * сохранение `new_amount` в state
    * переход в `confirming_level_amount`
    * показ подтверждения:
      * Уровень, текущая сумма, новая сумма
      * **ВАЖНОЕ WARNING:**
        * "Будет создана новая версия уровня"
        * "Старые депозиты продолжат работать на прежних условиях"
        * "Новые депозиты потребуют новую сумму"
  * **Шаг 4:** Подтверждение
    * если "Нет" → отмена
    * если "Да":
      * вызов `RoiCorridorService.set_level_amount(level, amount, admin_id)`
      * **КРИТИЧЕСКАЯ ОПЕРАЦИЯ - версионирование:**
        * получение current_version.version
        * расчет new_version = current_version.version + 1
        * копирование roi_percent, roi_cap_percent, is_active из current
        * получение актуального corridor config для roi_percent
        * создание новой записи `deposit_level_versions`:
          * level_number = level
          * amount = новая сумма
          * roi_percent = из corridor config (fixed) или текущей версии
          * roi_cap_percent = из current (обычно 500)
          * version = new_version
          * is_active = из current
          * effective_from = NOW()
          * created_by_admin_id = admin_id
        * commit
      * сообщение "✅ Сумма успешно обновлена! Изменения для новых депозитов"
      * возврат в меню коридоров
* **Обновление БД:**
  * `deposit_level_versions` - INSERT новой записи (новая версия)
  * старая версия остается в БД без изменений
* **Уведомления:** опционально (в текущей реализации нет, но можно добавить)
* **Логирование:**
  * INFO: "Level amount updated" с level, amount, version, admin_id
* **Обработка ошибок:**
  * неверный формат суммы → повтор ввода
  * сумма <= 0 → "Must be positive", повтор
  * ошибка создания версии → rollback, сообщение об ошибке
* **Edge cases:**
  * изменение суммы на ту же → технически допускается (создается новая версия)
  * нет current_version (уровень не инициализирован) → version = 1
  * привязка депозитов: новые депозиты получат `deposit_version_id` новой версии
* **Связанные сценарии:** R6-16, R17-2 (создание версии), R17-3 (привязка депозита к версии), R17-5 (гарантии)
* **Handler:** `bot/handlers/admin/roi_corridor.py:start_amount_setup()`, `process_level_amount_selection()`, `process_amount_input()`, `process_amount_confirmation()`
* **Сервис:** `RoiCorridorService.set_level_amount()`
* **Репозиторий:** `DepositLevelVersionRepository.create()`

#### R6-27: Просмотр истории изменений коридоров по уровню

* **Кнопка:** `📜 История изменений` (из меню коридоров)
* **FSM State:** `AdminRoiCorridorStates.viewing_history_level` → clear state
* **Входные данные:** выбор уровня (1-5)
* **Валидация:**
  * is_admin=True
  * уровень 1-5
* **Бизнес-логика:**
  * **Шаг 1:** Запрос уровня
    * установка состояния `viewing_history_level`
    * показ клавиатуры с уровнями 1-5
  * **Шаг 2:** Обработка выбора
    * парсинг номера уровня
    * получение истории: `DepositCorridorHistoryRepository.get_history_for_level(level, limit=20)`
    * если история пуста:
      * сообщение "История изменений для уровня X пуста"
      * возврат в меню коридоров
    * если есть записи:
      * для каждой записи (показ первых 10):
        * дата/время изменения (`changed_at`)
        * режим (custom/equal)
        * значение (min-max или fixed)
        * применено к (current/next)
        * изменил:
          * получение admin через `AdminRepository.get_by_id(changed_by_admin_id)`
          * формат: "@username (ID: telegram_id)" или "Admin ID: X" или "Система"
        * причина (если есть)
      * если записей > 10: "... и еще X записей"
      * возврат в меню коридоров
      * clear state
* **Обновление БД:** нет (только чтение)
* **Уведомления:** нет
* **Логирование:** просмотр истории админом
* **Обработка ошибок:**
  * неверный уровень → повторный запрос
  * нажатие `👑 Админ-панель` → clear state, выход
  * нажатие `◀️ Отмена` → clear state, возврат в меню коридоров
  * ошибка чтения истории → exception, сообщение "Системная ошибка"
* **Edge cases:**
  * изменения сделаны системой (changed_by_admin_id=None) → показ "Система"
  * админ удален из БД → показ "Admin ID: X" без username
  * очень длинная причина → может обрезаться в Telegram (ограничение длины сообщения)
* **Связанные сценарии:** R6-23 (создание записи истории), R6-16
* **Handler:** `bot/handlers/admin/roi_corridor.py:start_history_view()`, `show_level_history()`
* **Репозиторий:** `DepositCorridorHistoryRepository.get_history_for_level()`
* **Модель:** `DepositCorridorHistory` (таблица с полями: id, level, mode, roi_min, roi_max, roi_fixed, changed_at, changed_by_admin_id, applies_to, reason)

#### R6-28: Применение настроек "следующей сессии" (фоновая операция)

* **Кнопка:** нет (автоматический процесс)
* **FSM State:** - (фоновая задача)
* **Входные данные:** нет
* **Валидация:** нет
* **Триггер:**
  * вызывается в начале reward accrual task (scheduled job)
  * перед обработкой депозитов проверяются pending "next" настройки
* **Бизнес-логика:**
  * вызов `RoiCorridorService.apply_next_session_settings()`
  * для каждого уровня 1-5:
    * проверка наличия `LEVEL_{N}_ROI_MODE_NEXT` в GlobalSettings.roi_settings
    * если есть:
      * копирование mode в `LEVEL_{N}_ROI_MODE`
      * если mode="custom":
        * копирование `LEVEL_{N}_ROI_MIN_NEXT` → `LEVEL_{N}_ROI_MIN`
        * копирование `LEVEL_{N}_ROI_MAX_NEXT` → `LEVEL_{N}_ROI_MAX`
      * если mode="equal":
        * копирование `LEVEL_{N}_ROI_FIXED_NEXT` → `LEVEL_{N}_ROI_FIXED`
      * удаление всех `*_NEXT` ключей для этого уровня:
        * `LEVEL_{N}_ROI_MODE_NEXT`
        * `LEVEL_{N}_ROI_MIN_NEXT`
        * `LEVEL_{N}_ROI_MAX_NEXT`
        * `LEVEL_{N}_ROI_FIXED_NEXT`
      * логирование INFO: "Applied next session settings" с level и mode
  * commit изменений GlobalSettings
* **Обновление БД:**
  * `GlobalSettings.roi_settings` (JSON) - обновление и удаление ключей
* **Уведомления:** нет (админы уже были уведомлены при настройке)
* **Логирование:**
  * INFO для каждого уровня: "Applied next session settings" с level, mode
  * DEBUG: детали копирования значений
* **Обработка ошибок:**
  * ошибка чтения/записи GlobalSettings → exception, логирование CRITICAL
  * отсутствие *_NEXT ключей → нормально, просто skip уровня
  * некорректное значение в *_NEXT → логирование WARNING, использование defaults
* **Edge cases:**
  * применение в момент когда админ настраивает новый "next" → race condition unlikely (разные операции)
  * повторное применение → безопасно (ключи уже удалены, ничего не происходит)
  * partial application (только mode_next без values) → incomplete config, использование текущих values
* **Связанные сценарии:** R6-21 (настройка с applies_to="next"), R7-7 (использование примененных настроек)
* **Вызов:** из `jobs/tasks/reward_accrual_task.py` перед `RewardService.calculate_individual_rewards()`
* **Сервис:** `RoiCorridorService.apply_next_session_settings()`

#### R6-29: Обработка ошибок при настройке коридоров

* **Кнопка:** различные (контекстные ошибки)
* **FSM State:** любое состояние AdminRoiCorridorStates
* **Сценарии ошибок:**
  * **Невалидный формат числа при вводе:**
    * триггер: Exception при `Decimal(message.text.strip())`
    * реакция: "❌ Неверный формат. Введите число (например: `X`)"
    * поведение: повторный запрос ввода, FSM state не меняется
  * **max <= min при настройке Custom:**
    * триггер: проверка в `process_max_input()`: `roi_max <= roi_min`
    * реакция: "❌ Максимум (Y%) должен быть больше минимума (X%)"
    * поведение: повторный запрос максимума, state = `entering_max`
  * **Отрицательные значения:**
    * триггер: `roi_value < 0` при парсинге
    * реакция: "❌ Проценты не могут быть отрицательными"
    * поведение: повторный запрос
  * **Период вне диапазона (< 1 или > 24):**
    * триггер: проверка в `process_period_input()`
    * реакция: "❌ Неверный формат. Введите целое число от 1 до 24"
    * поведение: повторный запрос
  * **Потеря данных в FSM state:**
    * триггер: `state.get_data()` не содержит нужных ключей (level, mode, values)
    * реакция: "❌ Ошибка: данные потеряны"
    * поведение: `clear_state_preserve_admin_token()`, возврат в меню коридоров
  * **admin_id не найден в handler data:**
    * триггер: `data.get("admin_id")` возвращает None
    * реакция: "❌ Ошибка: admin_id не найден"
    * поведение: clear state, может потребоваться re-login
  * **Ошибка валидации в сервисе:**
    * триггер: `set_corridor()` возвращает (False, error_message)
    * примеры: "Для режима Custom требуются min и max", "Минимум должен быть меньше максимума"
    * реакция: показ error_message пользователю
    * поведение: возврат в меню коридоров
  * **Ошибка commit транзакции:**
    * триггер: exception при `session.commit()` в сервисе
    * реакция: rollback, логирование ERROR, сообщение "Системная ошибка"
    * поведение: clear state, возврат в меню
  * **Ошибка создания версии уровня:**
    * триггер: exception в `DepositLevelVersionRepository.create()`
    * примеры: нарушение constraints, ошибка БД
    * реакция: rollback, сообщение "Ошибка создания версии"
    * поведение: clear state
* **Обновление БД:** зависит от типа ошибки (может быть rollback)
* **Уведомления:** нет (при ошибках не уведомляем других админов)
* **Логирование:**
  * WARNING: невалидные данные с деталями (что ввел админ)
  * ERROR: системные ошибки с stack trace
* **Обработка ошибок:** каждая ошибка обрабатывается gracefully без краша бота
* **Edge cases:**
  * нажатие кнопки меню во время FSM → `is_menu_button()` сбрасывает состояние
  * timeout сессии во время ввода → потребуется повторный логин
  * одновременное редактирование двумя админами → последний перезаписывает (no optimistic locking)
* **Связанные сценарии:** R6-18, R6-19, R6-20, R6-21, R6-23, R6-25, R6-26
* **Handler:** все функции в `bot/handlers/admin/roi_corridor.py`

### 6.5. Deposit Management (расширенное управление депозитами)

#### R6-30: Вход в меню управления депозитами

* **Кнопка:** `💰 Управление депозитами` (из админ-панели)
* **FSM State:** - (без состояния)
* **Входные данные:** нет
* **Валидация:** is_admin=True
* **Бизнес-логика:**
  * показ главного меню управления депозитами
  * клавиатура с опциями:
    * `📊 Статистика по депозитам` → R6-31
    * `🔍 Найти депозиты пользователя` → R6-32
    * `📋 Pending депозиты` → R6-33
    * `📈 ROI статистика` → R6-38
    * `⚙️ Управление уровнями` → R6-34
    * `💰 Коридоры доходности` → R6-16
    * `◀️ Назад в админ-панель` → admin panel
* **Обновление БД:** нет
* **Уведомления:** нет
* **Логирование:** вход в меню
* **Обработка ошибок:**
  * не админ → "Эта функция доступна только администраторам"
* **Edge cases:** нет
* **Связанные сценарии:** R6-6 (админ-панель), R6-16, R6-31-R6-38
* **Handler:** `bot/handlers/admin/deposit_management.py:show_deposit_management_menu()`
* **Клавиатура:** `admin_deposit_management_keyboard()`

#### R6-31: Просмотр общей статистики депозитов

* **Кнопка:** `📊 Статистика по депозитам`
* **FSM State:** - (без состояния)
* **Входные данные:** нет
* **Валидация:** is_admin=True
* **Бизнес-логика:**
  * **Общая статистика** (один SQL запрос с агрегациями):
    * `COUNT(*)` → всего депозитов
    * `COUNT(*) WHERE status=CONFIRMED` → активных
    * `COUNT(*) WHERE is_roi_completed=True` → завершенных
    * `COUNT(*) WHERE status=PENDING` → pending
  * **Статистика по уровням** (5 отдельных запросов или один с GROUP BY):
    * для level 1-5:
      * `COUNT(*) WHERE level=N AND status=CONFIRMED` → количество
      * `SUM(amount) WHERE level=N AND status=CONFIRMED` → общая сумма
  * расчет grand total = сумма всех level totals
  * форматирование сообщения:
    * общая информация (4 цифры)
    * по уровням с emoji (1️⃣-5️⃣): "Уровень X: Y депозитов (Z USDT)"
    * итоговая сумма
  * показ с клавиатурой меню управления депозитами
* **Обновление БД:** нет (read-only)
* **Уведомления:** нет
* **Логирование:** просмотр статистики админом
* **Обработка ошибок:**
  * ошибка SQL запроса → exception, "Ошибка получения статистики"
  * нет депозитов → показ нулей (0 / 0.00 USDT)
* **Edge cases:**
  * очень большие числа (>1M deposits) → потенциально медленный запрос
  * negative amounts (data corruption) → показываются как есть (warning в логах)
* **Связанные сценарии:** R6-30, R6-38 (ROI статистика - более детальная)
* **Handler:** `bot/handlers/admin/deposit_management.py:show_deposit_statistics()`
* **SQL:** использует `func.count()`, `func.sum()` с фильтрами
* **Форматирование:** `format_usdt()` для сумм

#### R6-32: Поиск депозитов конкретного пользователя

* **Кнопка:** `🔍 Найти депозиты пользователя`
* **FSM State:** `AdminDepositManagementStates.searching_user_deposits` → clear state
* **Входные данные:** Telegram ID пользователя (int)
* **Валидация:**
  * is_admin=True
  * telegram_id - целое число
  * пользователь существует в БД
* **Бизнес-логика:**
  * **Шаг 1:** Запрос ID
    * установка состояния `searching_user_deposits`
    * показ инструкции: "Введите Telegram ID пользователя"
    * клавиатура с `❌ Отмена`
  * **Шаг 2:** Обработка ID
    * парсинг int из текста (strip + int())
    * поиск user: `UserRepository.get_by(telegram_id=...)`
    * если не найден:
      * сообщение "❌ Пользователь с ID `X` не найден"
      * clear state, возврат в меню
    * если найден:
      * получение депозитов: `DepositRepository.find_by(user_id=user.id)`
      * если нет депозитов:
        * "ℹ️ У пользователя `X` нет депозитов"
        * clear state, возврат
      * если есть:
        * форматирование списка депозитов:
          * заголовок: "📋 Депозиты пользователя X", username
          * для каждого депозита:
            * emoji статуса (⏳ pending, ✅ confirmed, ❌ failed)
            * уровень + сумма
            * ID депозита (code block для копирования)
            * статус + ROI прогресс:
              * если confirmed + !roi_completed: "(ROI: X.X%)" - процент от cap
              * если roi_completed: "(ROI завершён)"
            * заработано: roi_paid_amount
            * дата создания
        * clear state, возврат в меню
* **Обновление БД:** нет (read-only)
* **Уведомления:** нет
* **Логирование:**
  * INFO: поиск депозитов админом для user_id X
* **Обработка ошибок:**
  * ValueError при парсинге int → "Неверный формат! Введите числовой Telegram ID"
  * нажатие `❌ Отмена` → clear state, возврат
  * нажатие кнопки меню → `is_menu_button()` → clear state
  * ошибка БД → exception, "Системная ошибка"
* **Edge cases:**
  * очень много депозитов (>100) → все показываются (может быть длинное сообщение)
  * депозит без user (data corruption) → skip с логированием
  * roi_cap_amount = 0 → показ "ROI: N/A"
* **Связанные сценарии:** R6-30, R6-33 (pending), R3-7 (пользовательский просмотр)
* **Handler:** `bot/handlers/admin/deposit_management.py:start_search_user_deposits()`, `process_user_id_for_deposits()`
* **Репозитории:** `UserRepository`, `DepositRepository`

#### R6-33: Просмотр всех pending депозитов

* **Кнопка:** `📋 Pending депозиты`
* **FSM State:** - (без состояния)
* **Входные данные:** нет
* **Валидация:** is_admin=True
* **Бизнес-логика:**
  * получение всех pending депозитов: `DepositRepository.find_by(status=TransactionStatus.PENDING.value)`
  * если нет pending:
    * сообщение "ℹ️ Нет pending депозитов"
    * возврат в меню
  * если есть:
    * показ первых 10 депозитов (ограничение для читаемости):
      * для каждого:
        * 🆔 Deposit ID (code block)
        * 👤 User: telegram_id (@username или N/A)
        * 📊 Уровень: X
        * 💰 Сумма: formatted USDT
        * 📅 Дата: YYYY-MM-DD HH:MM
        * 🔗 TX: первые 16 символов tx_hash (code block) или "нет hash"
    * если pending_deposits > 10:
      * "... и ещё N депозитов"
    * возврат в меню управления депозитами
* **Обновление БД:** нет (read-only)
* **Уведомления:** нет
* **Логирование:** просмотр pending списка админом
* **Обработка ошибок:**
  * ошибка SQL → exception, "Ошибка получения данных"
  * депозит без user (orphan) → показ "User: Unknown"
* **Edge cases:**
  * очень старые pending (>24h) → показываются, но скоро будут timeout (R7-3)
  * pending без tx_hash (еще не был отправлен blockchain tx) → показ "TX: нет"
  * множество pending (>100) → может указывать на проблему с blockchain monitor
* **Связанные сценарии:** R6-30, R3-5 (создание pending), R7-3 (timeout pending)
* **Handler:** `bot/handlers/admin/deposit_management.py:show_pending_deposits()`
* **Мониторинг:** если pending > 10 → возможная проблема с инфраструктурой

#### R6-34: Управление уровнями депозитов (enable/disable)

* **Кнопка:** `⚙️ Управление уровнями` (из меню управления депозитами)
* **FSM State:** - (без состояния для показа списка)
* **Входные данные:** нет (только отображение)
* **Валидация:** is_admin=True
* **Бизнес-логика:**
  * получение `GlobalSettings.max_open_deposit_level` через `GlobalSettingsRepository.get_settings()`
  * показ максимального открытого уровня
  * для каждого уровня 1-5:
    * получение `current_version`: `DepositLevelVersionRepository.get_current_version(level_num)`
    * определение статуса:
      * если нет версии: "⚠️ Не настроен", amount = "N/A"
      * если есть версия:
        * если `is_active=True`: "✅ Активен"
        * если `is_active=False`: "❌ Отключен"
        * amount = formatted USDT
    * форматирование строки: emoji + Уровень + amount + статус
  * показ кнопок:
    * `Уровень 1` ... `Уровень 5` - для детального управления
    * `🔢 Изм. макс. уровень` → R6-35
    * `◀️ Назад в управление депозитами`
* **Обновление БД:** нет (read-only)
* **Уведомления:** нет
* **Логирование:** просмотр списка уровней
* **Обработка ошибок:**
  * ошибка чтения GlobalSettings → "Системная ошибка"
  * ошибка чтения версий → показ "⚠️ Не настроен" для проблемных уровней
* **Edge cases:**
  * нет ни одной версии ни для одного уровня → все показываются как "Не настроен"
  * max_open_deposit_level > 5 или < 1 → показ как есть (некорректная конфигурация)
* **Связанные сценарии:** R6-30, R6-35, R6-36
* **Handler:** `bot/handlers/admin/deposit_management.py:show_levels_management()`
* **Клавиатура:** `admin_deposit_levels_keyboard()`

#### R6-35: Изменение максимального открытого уровня

* **Кнопка:** `🔢 Изм. макс. уровень` (из меню управления уровнями)
* **FSM State:** `AdminDepositManagementStates.setting_max_level` → возврат в меню
* **Входные данные:** новое значение max level (int, 1-5)
* **Валидация:**
  * is_admin=True
  * значение - целое число
  * 1 <= значение <= 5
* **Бизнес-логика:**
  * **Шаг 1:** Показ текущего max level
    * получение `settings.max_open_deposit_level` из GlobalSettings
    * установка FSM состояния `setting_max_level`
    * показ инструкции:
      * текущее значение
      * запрос нового (1-5)
      * объяснение: "Пользователи смогут открывать депозиты только до этого уровня включительно"
    * клавиатура: `❌ Отмена`
  * **Шаг 2:** Обработка ввода
    * парсинг int
    * проверка диапазона 1-5
    * получение admin info для логирования: `AdminRepository.get_by_id(admin_id)`
    * сохранение: `GlobalSettingsRepository.update_settings(max_open_deposit_level=new_max)`
    * commit
    * логирование INFO: "Max open deposit level changed to X by admin Y"
    * сообщение "✅ Максимальный уровень успешно изменён на X"
    * clear state
    * возврат в `show_levels_management()`
* **Обновление БД:**
  * `GlobalSettings.max_open_deposit_level` = новое значение
* **Уведомления:** опционально (в текущей реализации нет, но логично добавить)
* **Логирование:**
  * INFO: изменение с admin info
* **Обработка ошибок:**
  * не число или вне диапазона → "Неверный формат. Введите число от 1 до 5"
  * нажатие `❌ Отмена` → clear state, возврат в меню
  * ошибка commit → exception, rollback, сообщение об ошибке
* **Edge cases:**
  * установка max_level < текущего максимального активного уровня пользователей → допускается
    * существующие депозиты высоких уровней продолжают работать
    * новые депозиты высоких уровней нельзя создать
  * установка max_level = 1 → только Level 1 доступен для новых депозитов
* **Связанные сценарии:** R6-34, R3-4 (проверка доступности уровня при создании депозита)
* **Handler:** `bot/handlers/admin/deposit_management.py:start_max_level_change()`, `process_max_level_change()`

#### R6-36: Просмотр детальной информации об уровне

* **Кнопка:** `Уровень X` (где X = 1-5, из меню управления уровнями)
* **FSM State:** `AdminDepositManagementStates.managing_level`
* **Входные данные:** выбор уровня из кнопки
* **Валидация:**
  * is_admin=True
  * извлечение level из текста: `int(message.text.split()[1])`
  * проверка 1 <= level <= 5
* **Бизнес-логика:**
  * парсинг номера уровня из текста кнопки ("Уровень 2" → 2)
  * получение `current_version`: `DepositLevelVersionRepository.get_current_version(level)`
  * если нет версии:
    * сообщение "⚠️ Уровень X не настроен в системе"
    * возврат в меню уровней
  * если есть версия:
    * сохранение `managing_level=level` в state
    * установка FSM `managing_level`
    * формирование информации:
      * Сумма: formatted amount
      * Статус: ✅ Активен или ❌ Отключен (из `is_active`)
      * ROI: X% (из `roi_percent`)
      * ROI Cap: Y% (из `roi_cap_percent`)
    * показ клавиатуры с действиями:
      * `✅ Включить уровень` (если is_active=False)
      * `❌ Отключить уровень` (если is_active=True)
      * `💰 Настроить коридор доходности` → R6-39
      * `◀️ Назад к уровням`
* **Обновление БД:** нет (только чтение)
* **Уведомления:** нет
* **Логирование:** просмотр деталей уровня
* **Обработка ошибок:**
  * неверный формат уровня → "Неверный формат уровня"
  * ValueError/IndexError при split → обработка, сообщение об ошибке
* **Edge cases:**
  * уровень не настроен (нет версии) → показ предупреждения
  * клавиатура динамическая: кнопка зависит от is_active
* **Связанные сценарии:** R6-34, R6-37 (включение/отключение), R6-39 (настройка коридора)
* **Handler:** `bot/handlers/admin/deposit_management.py:show_level_actions()`, `show_level_actions_for_level()`
* **Клавиатура:** `admin_deposit_level_actions_keyboard(level, is_active)` - динамическая генерация

#### R6-37: Включение или отключение уровня депозитов

* **Кнопка:** `✅ Включить уровень` или `❌ Отключить уровень` (из R6-36)
* **FSM State:** `AdminDepositManagementStates.managing_level` → `confirming_level_status` → clear
* **Входные данные:**
  1. Действие (enable/disable)
  2. Подтверждение (Да/Нет)
* **Валидация:**
  * is_admin=True
  * managing_level существует в state
  * current_version существует в БД
  * подтверждение = "Да" (явное)
* **Бизнес-логика:**
  * **Шаг 1:** Запрос подтверждения
    * определение action на основе кнопки:
      * "Включить" → `target_status="enable"`, `status_text="ВКЛЮЧИТЬ"`
      * "Отключить" → `target_status="disable"`, `status_text="ОТКЛЮЧИТЬ"`
    * сохранение `level_action`, `level_current_active` в state
    * переход в `confirming_level_status`
    * показ warning:
      * "Вы хотите {ВКЛЮЧИТЬ/ОТКЛЮЧИТЬ} уровень X?"
      * объяснение последствий:
        * при включении: пользователи смогут создавать новые депозиты
        * при отключении: новые депозиты нельзя, существующие работают
      * запрос подтверждения (Да/Нет)
    * клавиатура: `❌ Отмена`
  * **Шаг 2:** Обработка подтверждения
    * нормализация текста: strip + lower
    * если не "да"/"yes"/"✅ да" → трактуется как отмена
    * если "да":
      * получение level, action из state
      * получение current_version
      * применение изменения:
        * если action="enable": `current_version.is_active = True`, `status_msg="✅ Уровень X включён!"`
        * если action="disable": `current_version.is_active = False`, `status_msg="❌ Уровень X отключён!"`
      * commit
      * сообщение с результатом
      * уведомление других админов:
        * "🔔 Изменён статус уровня депозитов"
        * Уровень, статус (включён/отключён), изменил (admin ID)
      * clear state
      * возврат в меню уровней
* **Обновление БД:**
  * `deposit_level_versions.is_active` - UPDATE для current_version
* **Уведомления:**
  * всем extended_admin через `send_telegram_message()`
  * исключая инициатора
* **Логирование:**
  * INFO: "Level X enabled/disabled by admin Y"
  * ERROR: при ошибках уведомления
* **Обработка ошибок:**
  * отмена → "Действие отменено", возврат
  * нажатие кнопок навигации → обработка, возврат
  * level/action не найдены в state → "Ошибка: данные уровня не найдены"
  * current_version не найден в БД → "Ошибка: уровень не найден"
* **Edge cases:**
  * отключение последнего активного уровня → допускается (админ знает что делает)
  * включение уровня когда max_open_deposit_level < level → технически включен, но пользователи не смогут создать (ограничение max_open)
  * существующие активные депозиты отключенного уровня:
    * продолжают работать нормально
    * ROI начисления идут
    * withdrawals разрешены
    * только новые депозиты блокируются
* **Связанные сценарии:** R6-36, R6-34, R17-2 (temporary disable в документе)
* **Handler:** `bot/handlers/admin/deposit_management.py:process_level_action()`, `confirm_level_status_change()`

#### R6-38: Просмотр ROI статистики с детальной аналитикой

* **Кнопка:** `📈 ROI статистика` (из меню управления депозитами)
* **FSM State:** - (без состояния)
* **Входные данные:** нет
* **Валидация:** is_admin=True
* **Бизнес-логика:**
  * для каждого уровня 1-5:
    * SQL запрос активных депозитов:
      * `WHERE level=X AND status=CONFIRMED AND is_roi_completed=False`
    * если нет депозитов → skip уровня
    * если есть:
      * расчет агрегированных метрик:
        * `total_deposits = COUNT(*)`
        * `total_paid = SUM(roi_paid_amount)`
        * `total_cap = SUM(roi_cap_amount)`
        * `avg_progress = (total_paid / total_cap * 100)` - средний процент выполнения
      * поиск депозитов близких к завершению:
        * фильтр: `(roi_paid_amount / roi_cap_amount * 100) > 80`
        * подсчет количества
      * форматирование вывода:
        * emoji уровня (1️⃣-5️⃣)
        * Активных: X
        * Выплачено: formatted total_paid USDT
        * Средний прогресс: Y%
        * если есть близкие к завершению: "🔥 Близки к завершению: Z"
  * если нет активных депозитов ни на одном уровне:
    * "ℹ️ Нет активных депозитов с незавершённым ROI"
  * возврат в меню управления депозитами
* **Обновление БД:** нет (read-only, аналитика)
* **Уведомления:** нет
* **Логирование:** просмотр ROI статистики админом
* **Обработка ошибок:**
  * ошибка SQL → exception, "Системная ошибка"
  * division by zero (total_cap=0) → обработка: avg_progress = 0
* **Edge cases:**
  * все депозиты с roi_paid_amount = 0 → avg_progress = 0%, нормально для новых депозитов
  * депозит с roi_cap_amount = 0 (data corruption) → skip при расчетах
  * очень много депозитов близких к завершению (>50) → может указывать на высокий коридор или долгий период работы
* **Связанные сценарии:** R6-31 (общая статистика), R6-30, R7-9 (завершение ROI)
* **Handler:** `bot/handlers/admin/deposit_management.py:show_roi_statistics()`
* **Метрики:** полезны для планирования выплат и анализа доходности платформы

#### R6-39: Интеграция настройки коридора из меню уровня

* **Кнопка:** `💰 Настроить коридор доходности` (внутри управления уровнем R6-36)
* **FSM State:** `AdminDepositManagementStates.managing_level` → `AdminRoiCorridorStates.selecting_mode`
* **Входные данные:** нет (level уже в state)
* **Валидация:**
  * is_admin=True
  * managing_level существует в state
* **Бизнес-логика:**
  * получение `managing_level` из state.data
  * очистка admin state (preserve admin_token): `clear_state_preserve_admin_token(state)`
  * вызов `show_level_roi_config(message, session, state, level, from_level_management=True, **data)`
  * внутри `show_level_roi_config`:
    * получение текущих ROI настроек для уровня через `RoiCorridorService.get_corridor_config(level)`
    * получение `accrual_period_hours`
    * форматирование display:
      * режим (custom/equal)
      * коридор (min-max) или фиксированный
      * период начисления
    * сохранение `level`, `from_level_management=True` в state
    * установка FSM `AdminRoiCorridorStates.selecting_mode`
    * показ клавиатуры выбора режима (R6-19/R6-20)
  * после завершения настройки коридора (в конце R6-23):
    * проверка флага `from_level_management` в state
    * если True:
      * вызов `show_level_actions_for_level()` для возврата в меню уровня
      * НЕ возврат в ROI Corridor menu
    * если False:
      * стандартный возврат в ROI Corridor menu
* **Обновление БД:** через R6-23 (настройка коридора)
* **Уведомления:** через R6-24 (после применения настроек)
* **Логирование:**
  * INFO: "ROI corridor configuration accessed from level management"
* **Обработка ошибок:**
  * managing_level не найден в state → показ error, возврат в deposit management
* **Edge cases:**
  * переключение контекста между двумя меню (deposit management ↔ roi corridor)
  * важность флага from_level_management для правильного возврата
  * circular imports prevention: lazy import в конце функции
* **Связанные сценарии:** R6-36 (триггер), R6-19-R6-23 (процесс настройки), R6-34 (возврат)
* **Handler:** `bot/handlers/admin/deposit_management.py:process_level_action()` (перехват кнопки), `bot/handlers/admin/roi_corridor.py:show_level_roi_config()`

#### R6-40: Обработка навигационных кнопок "Назад" в различных контекстах

* **Кнопки:**
  * `◀️ Назад в админ-панель` (из deposit management, roi corridor)
  * `◀️ Назад к уровням` (из меню конкретного уровня)
  * `◀️ Назад в управление депозитами` (из roi corridor menu)
  * `👑 Админ-панель` (emergency exit из любого места)
* **FSM State:** любое admin state → clear state
* **Входные данные:** нажатие кнопки
* **Валидация:** is_admin=True
* **Бизнес-логика:**
  * **универсальная обработка навигации:**
    * во всех случаях: `clear_state_preserve_admin_token(state)`
      * очищает FSM state
      * НЕ трогает admin session (master key token)
      * сохраняет admin_id в контексте
  * **маршрутизация по тексту кнопки:**
    * "Назад в админ-панель" → `admin_panel_handler(message, state, **data)`
      * показ главного админ-меню
    * "Назад к уровням" → `show_levels_management(message, session, **data)`
      * возврат в список уровней
    * "Назад в управление депозитами" → `show_deposit_management_menu(message, session, **data)`
      * возврат в deposit management menu
    * "👑 Админ-панель" → `handle_admin_panel_button(message, session, **data)`
      * emergency exit из любого FSM state
      * всегда доступно для админа
* **Обновление БД:** нет
* **Уведомления:** нет
* **Логирование:** навигационное действие админа
* **Обработка ошибок:**
  * всегда graceful: даже если целевой handler падает, state очищен
* **Edge cases:**
  * нажатие "Назад" из уже главного меню → no-op или повторный показ
  * нажатие "Назад" во время ввода данных → прерывание FSM flow, потеря введенных данных (ожидаемо)
  * множественные быстрые нажатия "Назад" → rate limiting на уровне middleware предотвращает спам
* **Связанные сценарии:** все admin сценарии R6-16 до R6-39
* **Handler:** различные функции в `deposit_management.py`, `roi_corridor.py`
* **Утилита:** `bot/utils/admin_utils.py:clear_state_preserve_admin_token()` - критическая функция для сохранения admin session при навигации

---

## 7. R7 — Фоновые процессы и автоматика

### 7.1. Ежедневные начисления

#### R7-1: Ежедневное начисление ROI (2% в день) [LEGACY]

* **ВАЖНО:** Это legacy система с фиксированным 2% для session-based начислений
* **Актуальная система:** См. R7-7 (Individual Reward Accrual) с динамическими ROI corridors
* Фоновая задача запускается по расписанию (например, в 00:00 UTC)
* Проходит по всем активным депозитам с `is_roi_completed=False` и `status=CONFIRMED`
* Рассчитывает 2% от суммы депозита, начисляет на `available_balance`
* Проверяет достижение лимита 500%:
  * если `total_earned >= deposit_amount * 5` → устанавливает `is_roi_completed=True`
* Логирует все начисления в `transactions` с типом `ROI_EARNING`
* Отправляет уведомления пользователям (если включены настройки депозитных уведомлений)
* При ошибках — retry механизм с экспоненциальной задержкой

### 7.2. Реферальные начисления

#### R7-2: Автоматическое начисление реферальных бонусов при подтверждении депозита

* При смене статуса депозита с `PENDING` на `CONFIRMED` (триггер из blockchain service)
* `ReferralService.process_deposit_referral_earnings()`:
  * находит реферальную цепочку пользователя через `get_referral_chain(user_id, depth=3)`
  * начисляет комиссии:
    * 3% рефереру уровня 1
    * 2% рефереру уровня 2
    * 5% рефереру уровня 3
  * создаёт записи в `referral_earnings` с `is_paid=False`
  * атомарно обновляет балансы рефереров
* Отправляет уведомления каждому рефереру о новом заработке (если включены реферальные уведомления)
* Логирует в `transactions` с типом `REFERRAL_EARNING`

### 7.3. Таймауты и очистка

#### R7-3: Автоматический таймаут ожидающих депозитов

* Фоновая задача `deposit_timeout_checker` запускается каждый час
* Выбирает депозиты в статусе `PENDING` с `created_at < NOW() - INTERVAL '24 hours'`
* Для каждого:
  * проверяет, нет ли всё-таки транзакции в блокчейне (последняя попытка через `searchBlockchainForDeposit`)
  * если не найдена:
    * переводит в статус `EXPIRED`
    * логирует событие
    * отправляет уведомление пользователю: «Депозит не был подтверждён в течение 24 часов, создайте новый депозит»
* Статистика по истёкшим депозитам сохраняется для анализа админами

### 7.4. Blockchain error handling и retry

#### R7-4: Повторная обработка failed транзакций вывода

* Фоновая задача `withdrawal_retry_processor` проверяет withdrawal requests в статусе `PROCESSING` или с флагом `retry_needed`
* При blockchain ошибках (network timeout, nonce conflict, insufficient gas, RPC errors):
  * повторная попытка отправки с экспоненциальной задержкой: 1 мин → 5 мин → 15 мин → 1 час → 4 часа
  * каждая попытка логируется в `withdrawal_retry_log` с деталями ошибки
  * корректировка gas price при необходимости (если ошибка связана с gas)
* После N неудачных попыток (например, 5):
  * эскалация в Dead Letter Queue (DLQ) для ручной обработки админом
  * изменение статуса на `REQUIRES_MANUAL_REVIEW`
  * критическое уведомление админу через Telegram с деталями проблемы
  * средства остаются зарезервированными до решения проблемы

#### R7-5: Обработка blockchain node недоступности

* При запросах к BSC node возвращается timeout, connection error, или HTTP 5xx
* `BlockchainService.execute_with_failover()`:
  * автоматически переключается на следующий RPC endpoint из списка fallback узлов
  * пробует до 3 разных узлов последовательно
* Если все узлы из списка недоступны:
  * временная приостановка обработки новых депозитов/выводов
  * установка флага `blockchain_maintenance_mode=True`
  * критическое уведомление всем админам о проблеме с инфраструктурой
  * пользователям при попытке операций: "Временное техническое обслуживание, попробуйте через 10-15 минут"
* Фоновая задача `node_health_monitor`:
  * каждые 30 секунд пытается восстановить соединение со всеми узлами
  * проверяет `eth_blockNumber` для валидации работоспособности
  * при восстановлении — снятие maintenance mode и уведомление админов
* Все проверки и переключения логируются в health check систему

#### R7-6: Обнаружение и обработка "застрявших" транзакций

* Фоновая задача `stuck_transaction_monitor` запускается каждые 5 минут
* Выбирает withdrawal transactions в статусе `PROCESSING` старше 15 минут
* Для каждой транзакции проверка в блокчейне через `eth_getTransactionReceipt`:
  * **Транзакция pending (в mempool)**:
    * проверка текущего gas price в сети
    * если наш gas price < рекомендуемый → speed-up через replacement transaction с higher gas
    * отправка новой tx с тем же nonce, но увеличенным gas на 20%
  * **Транзакция failed (reverted)**:
    * анализ причины revert из receipt
    * пометка withdrawal как `FAILED`
    * возврат средств на баланс пользователя через `UserService.adjust_balance()`
    * уведомление пользователю и админу о причине отказа
  * **Транзакция dropped (nonce пропущен)**:
    * повторная отправка с корректным nonce
    * использование `eth_getTransactionCount` для получения актуального nonce
* Все проблемные транзакции уведомляются админам для ручного контроля и аудита
* Критическая метрика: если >3 транзакций застряли одновременно → алерт о системной проблеме

### 7.2. Individual Reward Accrual System (новая система начисления)

#### R7-7: Индивидуальное начисление наград с использованием ROI corridors

* **Триггер:** Scheduled task каждые N минут (период настраивается, например каждые 5 минут)
* **Отличие от R7-1:** Использует динамические ROI corridors вместо фиксированных 2% в день
* **Процесс:**
  1. **Получение времени:** `current_time = datetime.now(UTC)`
  2. **Выборка депозитов для обработки:**
     * SQL: `SELECT * FROM deposits WHERE next_accrual_at <= current_time AND is_roi_completed = False AND status = CONFIRMED`
     * индекс на `next_accrual_at` для производительности
     * обрабатываются все депозиты с пришедшим временем начисления
  3. **Для каждого депозита:**
     * **Получение corridor config:** `RoiCorridorService.get_corridor_config(deposit.level)`
     * **Определение rate:**
       * если `mode="custom"`: генерация случайного rate через `generate_rate_from_corridor(min, max)` (R7-8)
       * если `mode="equal"`: использование fixed rate
     * **Расчет reward amount:** `reward = (deposit.amount * rate) / 100`
     * **Проверка ROI cap:**
       * `roi_remaining = deposit.roi_cap_amount - deposit.roi_paid_amount`
       * если `reward > roi_remaining`: `reward = roi_remaining`
     * **Валидация:** если `reward <= 0` → skip этот депозит
     * **Создание записи DepositReward:**
       * `reward_session_id = None` (не привязан к сессии)
       * `deposit_id = deposit.id`
       * `user_id = deposit.user_id`
       * `amount = reward`
       * `accrued_at = current_time`
     * **Обновление deposit:**
       * `roi_paid_amount += reward`
       * расчет `next_accrual_at = current_time + timedelta(hours=period_hours)` (R7-9)
     * **Начисление реферальных бонусов:**
       * вызов `ReferralService.process_roi_referral_rewards(reward, user_id)`
       * начисление процентов рефереру (если есть)
     * **Проверка завершения ROI:**
       * если `roi_paid_amount >= roi_cap_amount`:
         * `is_roi_completed = True`
         * `completed_at = current_time`
         * вызов `_send_roi_completed_notification(deposit)` (R7-10)
  4. **Commit транзакции** после обработки всех депозитов
  5. **Логирование результатов:** количество обработанных депозитов, total accrued amount
* **Обновление БД:**
  * `deposits` - UPDATE: `roi_paid_amount`, `next_accrual_at`, `is_roi_completed`, `completed_at`
  * `deposit_rewards` - INSERT для каждого начисления
  * `users` - UPDATE: `available_balance` (через реферальные бонусы)
* **Уведомления:** уведомление о завершении ROI при `is_roi_completed=True`
* **Логирование:**
  * INFO: "Individual rewards calculated for X deposits, total Y USDT"
  * DEBUG: детали каждого начисления (deposit_id, rate, amount)
  * ERROR: ошибки обработки отдельных депозитов
* **Обработка ошибок:**
  * ошибка получения corridor config → skip deposit, логирование ERROR
  * ошибка создания reward → skip deposit, логирование, продолжение с next
  * ошибка обновления deposit → skip deposit, логирование
  * ошибка отправки уведомления → логирование, не блокирует процесс
  * **важно:** try-catch вокруг каждого депозита, чтобы ошибка одного не влияла на остальные
* **Concurrency:**
  * использование database-level locking (SELECT FOR UPDATE) при чтении депозитов для предотвращения двойного начисления
  * идемпотентность: повторное выполнение задачи безопасно (проверка `next_accrual_at`)
* **Edge cases:**
  * депозит с `roi_paid_amount` очень близким к cap → cap точно до копейки
  * corridor config не настроен для уровня → использование defaults (custom, 0.8%-10%)
  * period_hours изменен админом во время выполнения задачи → новые депозиты получат новый период при next calculation
  * депозит создан с `deposit_version` → использование roi_percent из версии (приоритет над corridor)
* **Связанные сценарии:** R7-1 (legacy session-based), R6-16 до R6-29 (настройка corridors), R7-8 (генерация rate), R7-10 (завершение ROI), R17-4 (использование версии)
* **Задача:** `jobs/tasks/individual_reward_accrual_task.py` (scheduled)
* **Сервис:** `RewardService.calculate_individual_rewards()`

#### R7-8: Генерация случайного rate из коридора с weighted distribution

* **Алгоритм:** `RoiCorridorService.generate_rate_from_corridor(roi_min: Decimal, roi_max: Decimal) -> Decimal`
* **Цель:** создать случайный ROI rate в диапазоне [min, max] с bias к нижним значениям (экономия для платформы)
* **Логика weighted random:**
  1. **Расчет диапазона:** `rate_range = roi_max - roi_min`
  2. **Разделение на thirds:**
     * `lower_bound = roi_min`
     * `lower_third_end = roi_min + (rate_range / 3)`
     * `middle_third_end = roi_min + (2 * rate_range / 3)`
     * `upper_bound = roi_max`
  3. **Случайный выбор с вероятностями:**
     * генерация `rand_val = random.random()` (0.0 до 1.0)
     * если `rand_val < 0.6` (60% шанс):
       * генерация в lower third: `rate = uniform(lower_bound, lower_third_end)`
     * иначе если `rand_val < 0.9` (30% шанс):
       * генерация в middle third: `rate = uniform(lower_third_end, middle_third_end)`
     * иначе (10% шанс):
       * генерация в upper third: `rate = uniform(middle_third_end, upper_bound)`
  4. **Квантизация:** округление до 2 decimal places: `rate = round(rate, 2)`
  5. **Возврат:** Decimal value
* **Пример распределения для коридора 0.8% - 10%:**
  * rate_range = 9.2%
  * lower third: 0.8% - 3.87% (60% случаев)
  * middle third: 3.87% - 6.93% (30% случаев)
  * upper third: 6.93% - 10% (10% случаев)
  * **ожидаемый средний rate:** ~3.5% (смещен к нижней границе)
* **Обоснование weighted подхода:**
  * платформа экономит средства на большинстве начислений
  * пользователи получают "сюрприз" при высоком rate (gamification)
  * compliance с обещанным коридором (все rates валидны)
* **Обработка edge cases:**
  * `roi_min == roi_max` (нет диапазона) → возврат roi_min без случайности
  * очень узкий коридор (range < 0.01%) → все равно работает, но мало вариативности
  * отрицательные значения → не должно происходить (валидация в R6-19/R6-20)
* **Связанные сценарии:** R7-7 (использование), R6-19 (настройка custom corridor)
* **Сервис:** `RoiCorridorService.generate_rate_from_corridor()`
* **Важность:** ключевой компонент для индивидуального начисления, влияет на финансы платформы

#### R7-9: Расчет next_accrual_at для депозита

* **Метод:** `RoiCorridorService.calculate_next_accrual_time(deposit_created_at: datetime) -> datetime`
* **Использование:**
  * при создании нового депозита (установка initial `next_accrual_at`)
  * после каждого индивидуального начисления (обновление `next_accrual_at`)
* **Процесс:**
  1. **Получение периода:** `period_hours = get_accrual_period_hours()` из GlobalSettings
  2. **Если период не настроен:** использование default = 24 часа
  3. **Расчет:** `next_time = deposit_created_at + timedelta(hours=period_hours)`
  4. **Возврат:** datetime объект (timezone-aware UTC)
* **Примеры:**
  * период = 24 часа, deposit created = 2025-01-24 12:00:00
    * next_accrual_at = 2025-01-25 12:00:00
  * период = 6 часов:
    * first accrual: +6h
    * second accrual: +12h
    * third accrual: +18h
    * ...
* **Важные детали:**
  * каждый депозит имеет **индивидуальное** расписание, зависящее от времени создания
  * изменение периода админом НЕ сбрасывает `next_accrual_at` существующих депозитов
  * новый период применяется при расчете следующего `next_accrual_at` для каждого депозита
* **Edge cases:**
  * период изменен с 24h на 1h во время работы:
    * старые депозиты: продолжают по старому расписанию до следующего начисления
    * при следующем начислении: новый `next_accrual_at` будет через 1h
  * депозит создан в 23:59 с периодом 24h → начисление в 23:59 следующего дня
  * очень короткий период (1h) → может создать высокую нагрузку на задачу (админ предупрежден)
* **Связанные сценарии:** R7-7 (использование при начислении), R6-25 (настройка периода), R3-5 (установка при создании депозита)
* **Сервис:** `RoiCorridorService.calculate_next_accrual_time()`
* **Конфигурация:** `GlobalSettings.roi_settings["REWARD_ACCRUAL_PERIOD_HOURS"]`

#### R7-10: Уведомление о завершении ROI (500% cap достигнут)

* **Триггер:** `is_roi_completed = True` установлено для депозита
* **Место вызова:** в конце обработки депозита в R7-7, когда `roi_paid_amount >= roi_cap_amount`
* **Метод:** `RewardService._send_roi_completed_notification(deposit: Deposit)`
* **Процесс:**
  1. **Получение пользователя:** `user = deposit.user` (через relationship или repository)
  2. **Формирование текста уведомления:**
     * заголовок: "🎉 Обязательства системы выполнены в объеме 500%!"
     * детали:
       * Уровень депозита: X
       * Сумма депозита: Y USDT
       * Всего заработано: Z USDT (должно быть ровно deposit.amount * 5)
     * важные сообщения:
       * "⚠️ Ваш текущий депозит деактивирован системой"
       * "Начисления по этому депозиту остановлены"
       * "✅ Вы можете открыть новый депозит для продолжения заработка"
     * призыв к действию: кнопка "💰 Открыть новый депозит" (inline keyboard)
  3. **Отправка:** `send_telegram_message(user.telegram_id, text, reply_markup=keyboard)`
  4. **Обработка результата:**
     * успех → логирование INFO: "ROI completion notification sent to user X"
     * TelegramAPIError (bot blocked, user deleted) → логирование WARNING, не блокирует процесс
     * другие ошибки → логирование ERROR
* **Обновление БД:** нет (только отправка уведомления)
* **Логирование:**
  * INFO: успешная отправка
  * WARNING: bot blocked by user
  * ERROR: неожиданные ошибки
* **Обработка ошибок:**
  * user не найден (deposit.user = None, orphan deposit) → логирование ERROR, skip уведомления
  * telegram_id не валиден → логирование ERROR
  * TelegramAPIError 403 (bot blocked) → логирование, skip (пользователь не хочет уведомлений)
  * TelegramAPIError 429 (rate limit) → retry через notification queue (R8-1)
  * любая ошибка НЕ блокирует основной процесс начисления наград
* **Edge cases:**
  * пользователь удалил свой Telegram аккаунт → 400 error от API, логируется
  * пользователь заблокировал бота → 403 error, нормально
  * уведомление не доставлено → депозит уже помечен как completed, пользователь увидит в UI
  * ROI завершен точно при 500.00% (не 499.99% или 500.01%) благодаря cap logic в R7-7
* **Связанные сценарии:** R7-7 (триггер), R3-7 (пользователь видит completed депозит), R8-2 (обработка bot blocked)
* **Метод:** `RewardService._send_roi_completed_notification()`
* **UI:** после клика на "Открыть новый депозит" → redirect в меню создания депозита (R3-2)

#### R7-11: Legacy RewardSession system (параллельная система)

* **Описание:** Старая система начисления с фиксированными rates по сессиям
* **Отличие от R7-7:** rates устанавливаются глобально для всех депозитов уровня в период сессии
* **Таблица:** `reward_sessions` с полями:
  * `session_name` - название (например, "Weekly Session 2025-W04")
  * `start_date`, `end_date` - временной диапазон
  * `reward_rate_level_1` ... `reward_rate_level_5` - фиксированные rates (Decimal)
* **Процесс:** `RewardService.calculate_rewards_for_session(session_id)`
  1. Получение сессии из БД
  2. Для каждого уровня:
     * получение `reward_rate` из `reward_rate_level_X`
     * если rate == 0:
       * попытка получения `deposit_version.roi_percent` как fallback (R17-4)
       * если нет версии → skip уровня
     * если rate > 0:
       * выборка депозитов: `level=X AND created_at BETWEEN start_date AND end_date AND status=CONFIRMED`
       * для каждого депозита:
         * расчет reward = (amount * rate) / 100
         * проверка ROI cap
         * создание `DepositReward` с `reward_session_id=session.id`
         * обновление `roi_paid_amount`
  3. Commit
* **Приоритет rates:**
  * **RewardSession rate** > corridor rate (если session rate != 0)
  * используется для промо-акций, бонусных периодов
  * пример: обычно corridor 0.8%-10%, но в промо-сессию все получают фиксированные 15%
* **Связь с Individual Accrual:**
  * обе системы могут работать параллельно
  * Individual accrual (R7-7) для regular daily операций
  * RewardSession для специальных периодов (промо, events)
  * conflict resolution: если есть session rate для периода → он в приоритете
* **Edge cases:**
  * перекрывающиеся сессии → последняя созданная session перекрывает предыдущую (no explicit handling)
  * сессия создана с rate=0 для всех уровней → no rewards для этой сессии
  * депозит создан до session start, но все еще active → получит session rate если попадает в период
* **Связанные сценарии:** R7-7 (новая система), R17-4 (fallback на версию)
* **Сервис:** `RewardService.calculate_rewards_for_session()`
* **Использование:** редко, только для специальных событий

#### R7-12: Интеграция двух систем начисления (Individual + Session)

* **Проблема:** Как reconcile две системы начисления, работающих параллельно?
* **Решение - приоритеты:**
  1. **RewardSession rate (если != 0)** → высший приоритет
  2. **deposit_version.roi_percent** → средний приоритет (для версионированных депозитов)
  3. **ROI corridor config** → низший приоритет (default для новых депозитов)
* **Логика в `RewardService.calculate_rewards_for_session()`:**
  * при расчете reward для депозита:
    1. проверка: есть ли активная RewardSession для этого периода?
    2. если есть:
       * получение `reward_rate` из session
       * если `reward_rate > 0`: использование session rate
       * если `reward_rate == 0`: переход к шагу 3
    3. если нет session или rate == 0:
       * проверка: есть ли `deposit.deposit_version`?
       * если есть: использование `deposit_version.roi_percent`
       * если нет: использование corridor config через `RoiCorridorService`
* **Примеры сценариев:**
  * **Сценарий A:** Обычный день, нет active session
    * Individual accrual задача выполняется каждые 6 часов
    * для каждого депозита: rate из corridor config
    * результат: случайные rates с weighted distribution
  * **Сценарий B:** Промо-неделя, active session с rate=15% для Level 1-3
    * Individual accrual пропускается (или использует session rates)
    * Session-based accrual выполняется в конце недели
    * все депозиты Level 1-3 в период недели получают 15% ровно
    * Level 4-5: используют corridor config (т.к. session rate=0)
  * **Сценарий C:** Депозит создан с deposit_version (версионированный Level)
    * нет active session
    * Individual accrual: использует `deposit_version.roi_percent` вместо corridor
    * result: консистентные rates для старых депозитов (гарантия R17-5)
* **Конфликты и resolution:**
  * депозит попадает в две session'ы одновременно → последняя session (по created_at) перекрывает
  * session rate отличается от corridor rate → session в приоритете (by design)
  * изменение corridor во время session → не влияет на session rates
* **Обновление БД:**
  * `deposit_rewards.reward_session_id`:
    * `NOT NULL` → создано через session-based accrual
    * `NULL` → создано через individual accrual
* **Edge cases:**
  * создание session с overlapping периодом с individual accrual:
    * рекомендуется: остановить individual accrual task на период session
    * или: individual accrual проверяет наличие session перед обработкой
  * удаление session после создания rewards → rewards остаются (soft delete session)
* **Связанные сценарии:** R7-7 (individual), R7-11 (session), R17-4 (версии)
* **Конфигурация:** решение о том, какую систему использовать - на уровне операций (manual trigger)
* **Best practice:** Individual accrual для daily operations, Session для events

---

## 8. R8 — Система уведомлений и коммуникации

### 8.1. Обработка ошибок доставки

#### R8-1: Обработка ошибок отправки уведомлений (Telegram API rate limits)

* При отправке уведомления пользователю Telegram API возвращает 429 (Too Many Requests)
* `NotificationService.send_notification()`:
  * перехватывает TelegramAPIError с кодом 429
  * помещает уведомление в Redis queue для retry с типом и приоритетом
  * извлекает `retry_after` из ответа API (обычно 30-60 секунд)
* Retry механизм с exponential backoff:
  * попытка 1: через `retry_after` секунд (из API)
  * попытка 2: через 2 минуты
  * попытка 3: через 10 минут
  * попытка 4: через 1 час
  * попытка 5: через 4 часа
* После всех неудачных попыток:
  * пометка в `notification_log` как `status=FAILED`
  * для критических уведомлений (вывод одобрен, депозит подтверждён, блокировка аккаунта):
    * эскалация в Dead Letter Queue
    * уведомление админам о невозможности доставки
    * возможность ручной повторной отправки через админ-панель
  * для некритических (маркетинговые рассылки):
    * просто логируются как failed без эскалации
* Статистика по failed notifications в дашборде для мониторинга проблем с API

#### R8-2: Обработка заблокированного бота пользователем

* Пользователь заблокировал бота через Telegram (bot blocked by user)
* При попытке отправить уведомление получаем Telegram error 403 "Forbidden: bot was blocked by the user"
* `NotificationService`:
  * перехватывает исключение
  * устанавливает флаг `bot_blocked=True` в таблице `users`
  * устанавливает `bot_blocked_at=NOW()`
  * отключает все дальнейшие попытки отправки уведомлений этому юзеру (проверка перед отправкой)
  * логирует событие в `user_activity_log`
* Если пользователь разблокирует бота и отправит `/start` или любое сообщение:
  * обработчик проверяет флаг `bot_blocked=True`
  * сброс флага `bot_blocked=False`, `bot_blocked_at=NULL`
  * возобновление уведомлений
  * опционально: проверка накопленных важных уведомлений за период блокировки
  * если есть критические (финансовые) → отправка сводки "Пока вас не было..."
* Метрика: процент пользователей с `bot_blocked=True` для анализа оттока

#### R8-3: Система приоритетов уведомлений при перегрузке

* При высокой нагрузке на систему уведомлений (например, массовая рассылка + одновременные ROI начисления)
* Очередь уведомлений (Redis sorted set) сортируется по приоритету + timestamp:
  * **P0 (критические, score=1000)**: 
    * финансовые операции: депозит подтверждён, вывод одобрен/отклонён
    * блокировки/разблокировки аккаунта
    * восстановление финпароля
  * **P1 (высокие, score=500)**:
    * ежедневные ROI начисления
    * реферальные заработки
    * административные действия (бан, изменение админом)
  * **P2 (нормальные, score=100)**:
    * ответы службы поддержки
    * изменения настроек/профиля
    * апелляции обработаны
  * **P3 (низкие, score=10)**:
    * маркетинговые рассылки
    * информационные сообщения
    * напоминания
* Worker процесс `notification_dispatcher`:
  * обрабатывает очередь в порядке приоритета
  * соблюдает rate limit: 30 msg/sec (Telegram Bot API limit)
  * при достижении лимита:
    * P0-P1 продолжают обрабатываться с задержками
    * P2 откладываются на 5-15 минут
    * P3 могут откладываться на несколько часов
* Мониторинг размера очереди по каждому приоритету
* Алерт админам если P0 очередь > 100 сообщений

---

## 9. R9 — Конкурентность и race conditions

### 9.1. Защита от double spending

#### R9-1: Одновременные попытки вывода средств (double spending protection)

* Сценарий атаки/ошибки:
  * пользователь быстро нажимает кнопку "Вывести всю сумму" несколько раз подряд
  * или открывает бота в двух устройствах/браузерах и одновременно инициирует вывод
  * или эксплуатирует race condition в API
* Защита через database-level locks в `WithdrawalService.create_withdrawal()`:
  * `SELECT * FROM users WHERE id = :user_id FOR UPDATE` - блокировка записи юзера
  * атомарная проверка внутри транзакции: `available_balance >= withdrawal_amount`
  * первая транзакция успешно:
    * создаёт запись в `withdrawals`
    * уменьшает `available_balance` через `UserService.adjust_balance()`
    * коммитит транзакцию
  * вторая параллельная транзакция:
    * ждёт освобождения lock
    * после получения доступа видит уже обновлённый баланс
    * проверка `available_balance >= withdrawal_amount` → False
    * возвращает ошибку `insufficient funds`
    * пользователю: "Недостаточно средств для вывода"
* Дополнительная защита через FSM state:
  * при начале процесса вывода устанавливается `WithdrawalStates.entering_amount` или `processing`
  * повторная попытка начать вывод проверяет текущий state
  * если уже в процессе вывода → блокирование с сообщением "У вас уже есть активная заявка на вывод"
* Логирование всех concurrent withdrawal attempts в `security_log` для:
  * аудита подозрительного поведения
  * выявления abuse/attack patterns
  * возможного temporary ban при слишком частых попытках

#### R9-2: Race condition при одновременном начислении ROI и выводе

* Критический сценарий:
  * фоновая задача запускает mass ROI distribution в 00:00 UTC
  * одновременно пользователь в timezone другой страны запрашивает вывод всего баланса
  * проблема: баланс может быть рассчитан до начисления ROI, но withdrawal создан после
  * результат: пользователь выводит меньше, чем должен был
* Решение через transaction isolation level:
  * обе операции (ROI distribution и withdrawal creation) выполняются в транзакциях с `REPEATABLE READ` или `SERIALIZABLE` isolation
  * использование pessimistic locking:
    * `SELECT * FROM users WHERE id = :user_id FOR UPDATE NOWAIT`
    * при конфликте немедленное получение исключения вместо ожидания
  * при обнаружении конфликта в withdrawal service:
    * откат транзакции
    * задержка 1-2 секунды (random jitter для предотвращения thundering herd)
    * повторная попытка с обновлёнными данными
  * пользователь видит актуальный баланс с учётом только что начисленного ROI
* Альтернативный подход для критических периодов:
  * временная "мягкая блокировка" выводов на время mass ROI distribution (00:00-00:05)
  * пользователям показывается: "Идёт начисление ежедневного дохода, попробуйте через минуту"
  * это предотвращает race conditions и упрощает логику
* Мониторинг:
  * метрики количества serialization failures
  * алерт если > 10 конфликтов в минуту

#### R9-3: Конкурентная обработка одного депозита blockchain monitor'ом

* Сценарий при horizontal scaling:
  * несколько экземпляров blockchain monitor service одновременно сканируют блокчейн
  * все экземпляры обнаруживают одну и ту же новую транзакцию депозита
  * риски:
    * дублирование начислений на баланс (2x, 3x от суммы депозита)
    * двойное/тройное начисление реферальных бонусов
    * критическая финансовая потеря для платформы
* Защита через distributed lock (Redis):
  * перед началом обработки депозита:
    * попытка установить lock: `SET processing:deposit:{tx_hash} {worker_id} NX EX 60`
    * `NX` - только если ключа не существует
    * `EX 60` - автоматическое удаление через 60 секунд (защита от deadlock)
  * только один worker процесс получает lock → он обрабатывает депозит
  * остальные worker'ы видят, что lock уже установлен → skip этой транзакции
  * после успешной обработки worker удаляет lock: `DEL processing:deposit:{tx_hash}`
* Database-level protection как fallback:
  * уникальный constraint `UNIQUE(blockchain_tx_hash)` на таблице `deposits`
  * если два worker'а каким-то образом прошли Redis lock:
    * первый успешно создаёт запись в БД
    * второй получает `IntegrityError` / `UniqueViolation`
    * graceful handling исключения без краша
* Idempotency в реферальных начислениях:
  * перед созданием `referral_earning`:
    * проверка: `SELECT * FROM referral_earnings WHERE deposit_id = :deposit_id AND referrer_id = :referrer_id`
    * если уже существует → skip начисления
  * или уникальный constraint `UNIQUE(deposit_id, referrer_id, referral_level)`
* Логирование и мониторинг:
  * каждый случай "lock already exists" логируется как INFO
  * каждый случай `UniqueViolation` логируется как WARNING с алертом
  * метрика: если >5 UniqueViolation в минуту → возможна проблема с Redis locks

---

## 10. R10 — Безопасность и аудит

### 10.1. Fraud detection

#### R10-1: Обнаружение и блокировка подозрительной активности (fraud detection)

* Мониторинг паттернов поведения пользователей через доступные данные:
  * множественные регистрации с одного и того же wallet address pattern (похожие адреса)
  * регистрация с использованием временных/одноразовых email сервисов при вводе контактов
  * подозрительная реферальная активность:
    * все рефералы зарегистрированы за 1 день
    * все рефералы используют похожие username паттерны
    * реферальная цепочка без реальной активности (нет депозитов)
  * попытки вывода сразу после первого депозита без достаточного времени для накопления ROI
  * паттерн "deposit confirmed → immediate maximum withdrawal attempt в течение часа"
  * множественные неудачные попытки ввода финпароля (>5 за 10 минут)
  * аномальная частота операций (>20 действий в минуту)
* `FraudDetectionService` анализирует события в реальном времени:
  * подсчёт risk score на основе множественных факторов (0-100)
  * факторы риска:
    * новый аккаунт (<24 часа) с крупным депозитом (+20 points)
    * нет email/телефона в контактах (+10 points)
    * подозрительная реферальная структура (+30 points)
    * быстрый вывод после депозита (+25 points)
    * множественные неудачные финпароли (+15 points)
  * при превышении порога (risk_score > 80):
    * автоматическая пометка аккаунта как `suspicious=True` в БД
    * блокировка выводов до ручной проверки админом (`withdrawal_blocked=True`)
    * создание алерта в админ-панель с детальным breakdown risk score
    * уведомление всех extended_admin и super_admin
* Админ получает детальный отчёт:
  * timeline всех действий пользователя
  * список всех связанных аккаунтов (по рефералам, похожим wallet addresses)
  * evidence для каждого фактора риска
  * рекомендуемое действие: review/unblock или ban
* Админ может:
  * разблокировать пользователя (`suspicious=False, withdrawal_blocked=False`)
  * запросить дополнительную верификацию
  * забанить через blacklist

#### R10-2: Аудит всех финансовых операций и reconciliation

* Ежедневная фоновая задача `financial_reconciliation` в 01:00 UTC (после ROI distribution)
* Сверка финансовых данных по формуле:
  ```
  Total System Balance = 
    SUM(confirmed_deposits) 
    - SUM(confirmed_withdrawals) 
    - SUM(paid_referral_earnings)
  
  Should Equal:
    SUM(users.available_balance) 
    + SUM(users.pending_balance) 
    + SUM(pending_withdrawals.amount)
  ```
* Детальные проверки:
  * сумма всех `deposits.amount` WHERE `status=CONFIRMED` 
  * минус сумма всех `withdrawals.amount` WHERE `status=CONFIRMED`
  * минус сумма всех `referral_earnings.amount` WHERE `is_paid=True`
  * результат должен равняться сумме всех балансов пользователей + pending операции
* Проверка реферальных начислений:
  * для каждого confirmed deposit должны быть соответствующие referral_earnings
  * сумма реферальных для депозита = 3% + 2% + 5% = 10% от суммы депозита (если есть 3 уровня)
  * нет "сирот" earnings без связанного депозита
* При обнаружении расхождения (discrepancy > 0.01 USDT):
  * **критический алерт** всем super_admin через Telegram
  * автоматическая **блокировка всех выводов** (`system_maintenance_mode=True`)
  * создание детального `reconciliation_report`:
    * snapshot всех балансов на момент проверки
    * breakdown по каждому типу транзакции
    * список подозрительных операций за последние 24 часа
    * diff между expected и actual балансами
  * логирование в `audit_log` с `severity=CRITICAL`
  * email уведомление на корпоративную почту (если настроено)
* Хранение ежедневных snapshots:
  * таблица `daily_balance_snapshots`: дата, total_deposits, total_withdrawals, total_user_balances
  * позволяет отследить когда именно появилось расхождение
  * исторический анализ для выявления систематических проблем

#### R10-3: Защита от admin account compromise

* Обнаружение подозрительных административных действий:
  * массовые операции за короткий период:
    * >10 ban операций за минуту
    * >50 одобрений выводов за минуту
    * >20 изменений в blacklist за минуту
  * попытка удалить или заблокировать других super_admin'ов
  * изменение критичных настроек системы:
    * deposit level суммы или лимиты
    * реферальные комиссии
    * system wallet addresses
  * создание нового super_admin аккаунта
  * попытка доступа к wallet management без extended прав
* При обнаружении подозрительной активности через `AdminActivityMonitor`:
  * **немедленное требование повторной аутентификации**:
    * форсированный logout текущей сессии
    * запрос master key заново
    * challenge-response для подтверждения легитимности
  * **уведомление всех других super_admin через Telegram**:
    * "⚠️ SECURITY ALERT: Admin @username совершает подозрительные массовые операции"
    * детали: тип операций, количество, временной период
    * кнопки: "Confirm Legitimate" / "Emergency Block Admin"
  * **временная блокировка критичных операций** на 5-10 минут:
    * нельзя создавать/удалять админов
    * нельзя менять system settings
    * нельзя одобрять выводы >$1000
    * можно только просматривать данные
  * **детальное логирование** в `admin_actions`:
    * каждое действие с полным контекстом
    * timestamp с миллисекундами
    * session_id для трассировки
    * все параметры операции (JSON)
* Дополнительные защитные меры:
  * Rate limiting на административные операции:
    * max 5 ban/unban операций в минуту
    * max 20 withdrawal approvals в минуту
    * cooldown 30 секунд между изменениями критичных настроек
  * Обязательная причина (reason) для:
    * ban/terminate пользователей (minimum 20 символов)
    * rejection withdrawal requests
    * создание нового админа
  * Audit trail:
    * каждое admin действие сохраняется с возможностью rollback
    * "кто, что, когда, почему" для compliance
* Система 2FA для критических операций (рекомендация для production):
  * при создании super_admin
  * при изменении wallet keys/seeds
  * при массовых операциях (>10 за раз)
  * опционально: integration с authenticator apps

---

## 11. R11 — Системная стабильность и disaster recovery

### 11.1. Database failures

#### R11-1: Обработка полного падения PostgreSQL

* Критический сценарий: PostgreSQL становится недоступен
  * server crash или restart
  * network partition между приложением и БД
  * planned maintenance без proper notification
  * disk full на БД сервере
* Все операции с БД начинают возвращать connection errors, timeouts, или refused connections
* Система реагирует через `DatabaseMiddleware` и exception handlers:
  * перехват всех DB exceptions: `OperationalError`, `InterfaceError`, `DatabaseError`
  * graceful response пользователям:
    * "⚠️ Технические работы, сервис временно недоступен"
    * "Ваши средства в безопасности, все операции будут обработаны после восстановления"
    * "Попробуйте через 5-10 минут"
  * все фоновые задачи переходят в режим ожидания:
    * `roi_distribution_job` → pause с сохранением checkpoint
    * `blockchain_monitor` → graceful shutdown с сохранением last processed block
    * `withdrawal_processor` → pause, pending withdrawals остаются в очереди
  * критические данные сохраняются в Redis как temporary fallback:
    * pending withdrawal requests с полным контекстом
    * незавершённые депозиты с expected amounts
    * user FSM states для восстановления сессий
  * health check endpoint возвращает `503 Service Unavailable` с details
  * алерты отправляются:
    * всем super_admin через Telegram: "🚨 CRITICAL: PostgreSQL недоступен"
    * в monitoring систему (Prometheus/Grafana если интегрирована)
    * email на tech team (если настроено)
* Автоматические попытки восстановления:
  * каждые 30 секунд проверка доступности БД через `SELECT 1`
  * exponential backoff для предотвращения перегрузки при восстановлении
  * max 50 попыток за 30 минут
* При восстановлении PostgreSQL:
  * автоматическая проверка целостности:
    * `PRAGMA integrity_check` или PostgreSQL equivalent
    * проверка всех critical tables на corruption
    * валидация последних N транзакций
  * восстановление операций из Redis fallback:
    * pending withdrawals восстанавливаются в БД
    * user states восстанавливаются для продолжения сессий
    * проверка на дубликаты перед восстановлением
  * постепенное возобновление сервисов (circuit breaker pattern):
    * **фаза 1 (0-5 мин)**: read-only режим, только просмотр данных
    * **фаза 2 (5-15 мин)**: разрешены операции пользователей (депозиты, выводы)
    * **фаза 3 (15+ мин)**: full operations, включая фоновые задачи
  * уведомление пользователей: "✅ Технические работы завершены, все сервисы восстановлены"
  * post-mortem отчёт для админов с деталями outage

#### R11-2: Катастрофический сбой blockchain node + все fallbacks

* Worst-case сценарий: все RPC endpoints недоступны одновременно
  * DDoS атака на Binance Smart Chain
  * глобальный network outage провайдеров
  * chain halt или consensus failure
  * все публичные и приватные RPC nodes down
* Невозможность выполнить blockchain операции:
  * не можем мониторить входящие депозиты
  * не можем отправлять USDT withdrawals
  * не можем проверять балансы системных кошельков
  * не можем получать актуальный gas price
* Система переходит в "Limited Operations Mode":
  * флаг `blockchain_maintenance_mode=True` в system config
  * **все новые депозиты** автоматически:
    * создаются со статусом `PENDING_NETWORK_RECOVERY`
    * пользователю объяснение: "Депозит будет обработан автоматически после восстановления сети BSC"
    * инструкции остаются актуальными (адрес, сумма)
    * храним expected_amount и user_id для последующей обработки
  * **все попытки вывода** блокируются:
    * при нажатии кнопки "Вывод": 
    * "⚠️ Временная приостановка выводов из-за проблем с сетью Binance Smart Chain"
    * "Ваши средства в безопасности, выводы будут доступны после восстановления"
    * "Следите за обновлениями в нашем канале"
  * **read-only операции** продолжают работать:
    * просмотр баланса (из БД)
    * просмотр истории транзакций
    * просмотр рефералов и статистики
    * все меню и навигация
  * **ROI начисления** продолжаются:
    * ежедневный job запускается по расписанию
    * начисления фиксируются только в БД
    * без blockchain operations
    * помечаются флагом `blockchain_pending=True` для отслеживания
* Recovery plan при восстановлении сети:
  * автоматическое обнаружение восстановления:
    * `node_health_monitor` детектирует доступность RPC
    * последовательная проверка всех endpoints
    * валидация через `eth_blockNumber` и `eth_syncing`
  * **batch processing** всех pending депозитов:
    * собираем все deposits со статусом `PENDING_NETWORK_RECOVERY`
    * для каждого запускаем `searchBlockchainForDeposit()`
    * параллельная обработка (pool of workers) с rate limiting
    * found → CONFIRMED, not found → остаётся PENDING с новым timeout
  * **ручная проверка накопленных withdrawals**:
    * админы получают список всех withdrawal requests за период outage
    * детальный review перед массовым исполнением (fraud check)
    * batch approval через админ-панель
    * приоритизация по сумме: мелкие (<$100) автоматически, крупные вручную
  * **reconciliation** всех операций за период outage:
    * полная сверка балансов до и после
    * проверка всех ROI начислений за пропущенные дни
    * верификация всех реферальных начислений
    * detailed audit report для compliance
* Уведомления пользователям:
  * при начале outage: рассылка "⚠️ Временные проблемы с BSC сетью"
  * при восстановлении: "✅ Сеть восстановлена, обработка депозитов запущена"
  * индивидуальные уведомления при подтверждении каждого депозита

#### R11-3: Redis полностью недоступен (cache + sessions + locks потеряны)

* Критический сценарий: Redis server crash или недоступен
* Потеря всех данных в Redis:
  * **distributed locks** для предотвращения duplicate processing
  * **кэшированные данные**: user profiles, deposit levels, system settings
  * **notification queue** с pending уведомлениями
  * **rate limiting counters** (registration, withdrawal, admin operations)
  * **FSM states** пользователей (текущие wizard steps)
  * **admin sessions** (требуется re-authentication)
* Graceful degradation - система продолжает работать без Redis:
  * **fallback на PostgreSQL advisory locks**:
    * `pg_try_advisory_lock()` вместо Redis SETNX
    * медленнее (DB roundtrip), но надёжно
    * риск: higher DB load, но предотвращает duplicate processing
  * **FSM states в PostgreSQL**:
    * отдельная таблица `user_fsm_states` с columns: user_id, state, data
    * каждое изменение state пишется в БД
    * при получении update проверяем текущий state из БД
    * overhead: +1 DB query на каждый handler, но работоспособность сохранена
  * **rate limiting становится менее точным**:
    * вместо global counter (Redis) используем per-process counters (memory)
    * менее точно, но предотвращает большинство abuse
    * например: 100 registrations/hour globally → ~10-20/hour per process
  * **notification queue на PostgreSQL**:
    * таблица `notification_queue_fallback`
    * worker процесс делает polling каждые 5 секунд вместо Redis pub/sub
    * медленнее доставка (задержка до 5 сек), но уведомления не теряются
  * **кэш отсутствует**:
    * все данные читаются напрямую из PostgreSQL
    * нет cache hit → каждый запрос идёт в БД
    * используем aggressive DB connection pooling
    * включаем query result caching на уровне SQLAlchemy (short TTL)
* Performance impact при работе без Redis:
  * увеличение latency операций: 2-5x (от ~50ms до ~200ms)
  * снижение throughput: 50-70% (меньше concurrent users)
  * увеличение DB load: +200-300% queries per second
  * но система остаётся функциональной и не теряет данные
* Автоматические попытки восстановления Redis:
  * каждые 15 секунд проверка доступности: `PING` command
  * мониторинг health check в отдельном thread
  * алерты админам о недоступности
* При восстановлении Redis:
  * постепенный переход обратно:
    * **прогрев кэша** critical данных:
      * загрузка всех users в Redis (по частям, batch 1000)
      * загрузка deposit_levels, system_settings
      * прогрев занимает 2-5 минут для 100k+ users
    * **восстановление notification queue**:
      * копирование pending notifications из PostgreSQL fallback table в Redis
      * удаление из fallback после успешного копирования
    * **миграция FSM states**:
      * копирование active states из PostgreSQL в Redis
      * cleanup старых states (>24 часа)
    * **переход на distributed locks**:
      * постепенная миграция с PostgreSQL locks на Redis
      * проверка отсутствия conflicts
  * переключение систем происходит gracefully:
    * dual-write короткий период (пишем в оба места)
    * затем отключаем PostgreSQL fallback
    * monitoring метрик для подтверждения стабильности
  * уведомление админов: "✅ Redis восстановлен, система вернулась к нормальной производительности"

---

## 12. R12 — Граничные случаи и edge cases

### 12.1. Timing edge cases

#### R12-1: Пользователь достигает 500% ROI ровно в момент создания withdrawal

* Критический race condition сценарий:
  * депозит User A на $100, уже начислено $490 (490% ROI)
  * в 00:00:00 UTC запускается ежедневное начисление ROI (+$10, итого 500%)
  * в 00:00:01 UTC пользователь нажимает "Вывести всю сумму"
  * в 00:00:02 ROI job обрабатывает этот депозит
* Проблема требует корректной обработки трёх аспектов:
  * финальное начисление ROI должно пройти полностью
  * депозит должен корректно закрыться (`is_roi_completed=True`)
  * withdrawal должен учитывать это последнее начисление в расчёте баланса
* Решение через proper transaction ordering и isolation:
  * **ROI distribution job**:
    * использует `SELECT * FROM users WHERE id IN (...) FOR UPDATE` для блокировки
    * обрабатывает пользователей последовательно или в батчах
    * внутри транзакции: начисление → обновление депозита → commit
  * **Withdrawal creation**:
    * также использует `SELECT * FROM users WHERE id = :id FOR UPDATE`
    * расчёт available_balance внутри той же транзакции
    * создание withdrawal request → декремент баланса → commit
  * **Serialization гарантирует**:
    * кто первый захватил lock на user record - тот выполняется
    * второй ждёт освобождения lock
    * после получения lock видит уже обновлённое состояние
* Возможные outcomes:
  * **ROI first**: начисление $10 → баланс $500 → затем withdrawal на $500 → успех
  * **Withdrawal first**: вывод на $490 → баланс $0 → затем ROI хочет начислить → депозит помечен completed, но начисление идёт → баланс $10
* Дополнительная логика в ROI job:
  * после начисления проверка: `if total_earned >= deposit_amount * 5`
  * установка `is_roi_completed=True`, `completed_at=NOW()`
  * прекращение дальнейших начислений по этому депозиту
* Тестовый сценарий для E2E тестов:
  * создать депозит $100
  * manually установить `total_earned = $490`
  * симулировать одновременный запуск ROI job и withdrawal request
  * проверить: нет duplicate начислений, нет потери средств, корректное закрытие депозита

#### R12-2: Реферальная цепочка с удалёнными/заблокированными пользователями

* Сложный сценарий с частично недоступной реферальной цепочкой:
  * **User A (active)** → пригласил User B
  * **User B (blocked)** → пригласил User C  
  * **User C (terminated)** → пригласил User D
  * **User D (active)** → делает депозит $100
* Критические вопросы для бизнес-логики:
  * Кто должен получить реферальные начисления?
  * Пропускать ли заблокированных в цепочке?
  * Считать ли terminated пользователей в depth расчёте?
  * Как обрабатывать gaps в цепочке?
* Варианты бизнес-политики:
  * **Вариант 1 (строгий)**: пропускаем всех non-active
    * User C terminated → skip, не начисляем
    * User B blocked → skip, не начисляем  
    * User A active → получает бонус за level 3 (проход через gaps)
    * итого: только 5% для User A, 0% для B и C
  * **Вариант 2 (мягкий)**: blocked получают, но не могут вывести
    * User C terminated → skip полностью
    * User B blocked → начисление идёт, но `earnings_blocked=True`
    * User A active → получает бонус за level 3
    * итого: 2% для B (заблокирован), 5% для A
  * **Вариант 3 (compromise)**: начисляем всем, но помечаем
    * все получают начисления независимо от статуса
    * blocked/terminated пользователи имеют флаг `cannot_withdraw=True`
    * позволяет восстановить earnings если пользователь разблокирован
* Реализация в `ReferralService.process_deposit_referral_earnings()`:
  * получение цепочки через `get_referral_chain(user_id, depth=3)`
  * для каждого уровня проверка:
    ```python
    if referrer.is_terminated:
        continue  # skip terminated полностью
    
    if referrer.is_blocked:
        # согласно выбранной политике
        if POLICY == "strict":
            continue
        elif POLICY == "soft":
            create_earning(blocked=True)
    ```
  * обработка gaps: если пропущен level 2, считаем level 3 от level 1
* Документирование в коде:
  * комментарий в начале функции с объяснением политики
  * константа `REFERRAL_POLICY = "soft"` в config
  * unit тесты для всех комбинаций статусов
* Логирование edge cases:
  * каждый случай skipped referrer логируется с причиной
  * метрики: сколько earnings пропущено из-за блокировок
  * аудит для возможной компенсации при разблокировке

#### R12-3: Депозит на точно минимальную сумму с учётом gas fees

* Граничный случай с blockchain fees и округлением:
  * пользователь должен отправить ровно $10.00 USDT для Level 1
  * отправляет transaction с amount = 10.000000 USDT (6 decimals)
  * blockchain может:
    * округлить из-за внутренних расчётов
    * вычесть tiny amount как internal fee некоторых bridges
    * precision loss при конвертации units
  * реально на system wallet приходит: 9.999998 USDT
* Проблема: строгое сравнение `received == expected` отклонит депозит
* Решение через tolerance-based comparison в `BlockchainService.process_confirmed_deposit()`:
  * определение acceptable tolerance:
    * для мелких сумм (<$100): ±$0.01 (1 цент)
    * для средних ($100-$1000): ±$0.10  
    * для крупных (>$1000): ±$1.00
  * проверка с tolerance:
    ```python
    expected = deposit.expected_amount
    tolerance = 0.01 if expected < 100 else (0.10 if expected < 1000 else 1.00)
    
    if abs(received - expected) <= tolerance:
        # Accept deposit
        actual_level = determine_level(received)
    elif received < (expected - tolerance):
        # Insufficient amount
        deposit.status = FAILED
        reason = "Insufficient amount received"
    elif received > (expected + tolerance):
        # Overpayment - accept and credit bonus
        actual_level = determine_level(received)
        bonus = received - expected
    ```
* Логика обработки различных случаев:
  * **В пределах tolerance** (9.99 - 10.01 для $10):
    * депозит принимается на запланированный level
    * начисление полной суммы level ($10)
    * пользователь не видит разницы
  * **Меньше tolerance** (< 9.99):
    * статус `FAILED` или `PARTIAL`
    * уведомление: "Получено недостаточно средств: $9.95 вместо $10.00"
    * опции: вернуть (минус fees) или предложить доплатить
  * **Больше tolerance** (> 10.01):
    * два варианта политики:
      * **вариант A**: принять как депозит более высокого level (если $20+)
      * **вариант B**: принять level 1, остаток = bonus на баланс
    * пример: отправили $25 вместо $10 → засчитываем Level 2 ($20) + $5 на баланс
* Edge case: пользователь отправил сумму между уровнями:
  * отправил $15, между Level 1 ($10) и Level 2 ($20)
  * логика: принимаем как Level 1, остаток $5 возвращаем или зачисляем на баланс
  * или: предлагаем доплатить до Level 2
* Логирование всех amount mismatches:
  * таблица `deposit_amount_discrepancies`
  * columns: deposit_id, expected, received, diff, resolution
  * регулярный аудит для выявления систематических проблем
  * если >10% депозитов имеют discrepancy → проблема с instructions или fees
* Тестовые сценарии:
  * точная сумма: 10.000000
  * чуть меньше: 9.999998 (в tolerance)
  * недостаточно: 9.95 (вне tolerance)
  * чуть больше: 10.000005 (в tolerance)
  * значительно больше: 25.00 (upgrade level)
  * между уровнями: 15.00 (partial level)

---

## 13. R13 — Пользовательский UX и необычные сценарии

### 13.1. Повторные взаимодействия

#### R13-1: Пользователь удаляет бота и пытается зарегистрироваться заново

* Сценарий жизненного цикла:
  * пользователь успешно прошёл полную регистрацию
  * имеет активные депозиты на $100 и баланс $50
  * удаляет бота из Telegram (действие "Delete Chat" только скрывает чат локально, НЕ удаляет данные)
  * через неделю нажимает `/start` снова или переходит по новой реферальной ссылке друга
* Корректное поведение системы:
  * handler `/start` проверяет существование user по `telegram_id`
  * `UserRepository.get_by_telegram_id()` находит существующую запись
  * НЕ создаёт дубликат аккаунта (защита через unique constraint)
  * показывает персонализированное приветствие:
    * "С возвращением, @username! 👋"
    * "Ваш баланс: $50.00"
    * "Активные депозиты: 1"
  * восстанавливает FSM контекст:
    * если был активный state старше 24 часов → очищает (считаем abandoned)
    * если недавний active state → может восстановить или предложить продолжить
  * показывает актуальное главное меню с полным функционалом
  * сбрасывает флаг `bot_blocked=False` если был установлен ранее
  * логирует событие "user_returned" для аналитики retention
* Edge case: /start с параметром реферала для существующего user
  * URL: `t.me/bot?start=ref123456`
  * user уже зарегистрирован с другим referrer
  * правильное поведение:
    * игнорируем новый `referrer_id`, сохраняем оригинальную реферальную цепочку
    * показываем сообщение: "Вы уже зарегистрированы. Реферальная ссылка не применяется к существующим аккаунтам"
    * или молча игнорируем параметр и показываем обычное "С возвращением"
  * защита от накрутки рефералов (пользователь не может перерегистрироваться)
* Особый случай: user был terminated
  * при попытке `/start` проверка blacklist показывает `TERMINATED`
  * `BanMiddleware` блокирует все updates
  * пользователь не получает ответа (бот выглядит недоступным)
  * это правильное поведение для terminated accounts

#### R13-2: Множественные быстрые нажатия на одну кнопку (button spam)

* Проблемный сценарий:
  * пользователь (намеренно или случайно) быстро кликает одну кнопку
  * например: "📊 Баланс" 10 раз за 1 секунду
  * или циклический спам: "💰 Депозит" → "Level 1" → "⬅ Назад" → "Level 1" → повторить
  * или "💸 Вывести всю сумму" 5 раз подряд
* Негативные последствия без защиты:
  * перегрузка бота множественными идентичными запросами к БД
  * race conditions в FSM state (multiple handlers пытаются изменить state)
  * дублирующиеся сообщения в чате (10 копий сообщения о балансе)
  * возможные duplicate операции (multiple withdrawal requests)
  * плохой UX для пользователя (спам в чате)
  * потенциальный DDoS вектор атаки
* Многоуровневая защита:
  * **Level 1: Telegram built-in throttling**
    * Telegram API сам ограничивает rate updates от одного пользователя
    * но недостаточно для aggressive clicking
  * **Level 2: Handler-level rate limiting через Redis**
    * middleware перед каждым handler:
      ```python
      key = f"user:{telegram_id}:last_action:{callback_data}"
      last_time = redis.get(key)
      now = time.time()
      
      if last_time and (now - float(last_time)) < COOLDOWN:
          # Ignore duplicate - too soon
          return
      
      redis.setex(key, COOLDOWN, str(now))
      # Process handler
      ```
    * COOLDOWN: 500ms для обычных действий, 2-3 секунды для финансовых
    * при срабатывании throttle:
      * молча игнорируем повторные клики (no response)
      * или answer callback query с alert: "⏳ Подождите немного"
  * **Level 3: FSM-based protection**
    * проверка перед обработкой action:
      ```python
      processing_key = f"processing:{user_id}"
      if redis.exists(processing_key):
          # Already processing an action
          return
      
      redis.setex(processing_key, 5, "1")  # Lock for 5 seconds
      try:
          # Process handler
      finally:
          redis.delete(processing_key)
      ```
    * предотвращает concurrent processing одного user
  * **Level 4: Idempotency keys для критических операций**
    * при создании withdrawal/deposit генерируем idempotency_key
    * храним в Redis на 24 часа
    * повторные попытки с тем же key → возвращаем cached result
* Специальная обработка для спамеров:
  * если user превышает глобальный threshold (>50 actions в минуту):
    * временная мягкая блокировка на 5-10 минут
    * сообщение: "Вы слишком быстро нажимаете кнопки, подождите немного"
    * логирование в security_log для выявления abuse patterns
    * при систематическом спаме → review и возможный ban
* User-friendly feedback:
  * при первом срабатывании throttle: "⏳ Пожалуйста, не нажимайте кнопки слишком часто"
  * при последующих: молча игнорируем (не спамим сообщениями)
  * для долгих операций: показываем индикатор "⏳ Обработка..."

#### R13-3: Пользователь меняет язык Telegram во время использования бота

* Сценарий изменения языковых настроек:
  * бот изначально общается с пользователем на русском (default)
  * пользователь в настройках Telegram меняет язык интерфейса на английский
  * возвращается в бота и отправляет сообщение или нажимает кнопку
* Ожидания пользователя vs текущая реализация:
  * **ожидание**: бот автоматически переключится на английский
  * **текущее**: бот продолжает отвечать на русском (single language bot)
* Решение для будущей i18n поддержки (если потребуется):
  * **Middleware для определения языка**:
    ```python
    class LanguageMiddleware:
        async def __call__(self, handler, event, data):
            user = event.from_user
            telegram_lang = user.language_code  # 'ru', 'en', 'es', etc.
            
            # Get user from DB
            db_user = await user_repo.get_by_telegram_id(user.id)
            
            if db_user and db_user.preferred_language != telegram_lang:
                # Language changed
                await user_repo.update_language(db_user.id, telegram_lang)
                
            # Set locale for this request
            data["locale"] = telegram_lang or "ru"
            return await handler(event, data)
    ```
  * **Структура i18n файлов**:
    * `locales/ru/messages.yaml`
    * `locales/en/messages.yaml`
    * все текстовые строки вынесены из кода в locale files
  * **Использование в handlers**:
    ```python
    async def balance_handler(message, locale):
        text = get_text("balance.message", locale, 
                       available=user.available_balance,
                       pending=user.pending_balance)
        await message.answer(text)
    ```
  * **Клавиатуры также локализуются**:
    * кнопки берут текст из locale files
    * callback_data остаётся на английском (для кода)
* Текущее состояние проекта:
  * бот работает только на русском языке
  * `user.language_code` может записываться для будущего
  * этот сценарий важен для roadmap если планируется международная экспансия
* Альтернативное решение без полной i18n:
  * кнопка в настройках "🌐 Язык / Language"
  * пользователь вручную выбирает: Русский / English
  * флаг `user.preferred_language` в БД
  * минимальный набор переводов критичных сообщений (финансовые операции)

---

## 14. R14 — Мониторинг, алерты и observability

### 14.1. Финансовые метрики и аномалии

#### R14-1: Обнаружение аномалий в финансовых метриках

* Система непрерывного мониторинга ключевых финансовых показателей (KPI):
  * **Withdrawal metrics**:
    * резкий рост pending withdrawals: >100 заявок за час против обычных 10-20
    * необычно высокая сумма одобренных выводов: $50k+ за час против обычных $5k
    * spike в rejected withdrawals: >20% rejection rate против обычных 2-5%
  * **Deposit metrics**:
    * аномально низкое количество новых депозитов: <5 за день против обычных 50+
    * необычные суммы: много депозитов на максимальный level ($300) одновременно
    * географическая аномалия: все депозиты из одной timezone (suspicious pattern)
  * **Balance metrics**:
    * дисбаланс: total withdrawals превышают total deposits за последние 24 часа
    * резкий рост system liabilities (сумма всех user balances)
    * negative trend в platform reserves
  * **Referral metrics**:
    * spike в реферальных начислениях (возможная накрутка)
    * аномальная конверсия: много регистраций по рефералам, но нет депозитов
* `MetricsMonitor` service (фоновая задача каждые 5 минут):
  * сбор current metrics через агрегирующие SQL queries
  * загрузка historical baseline:
    * скользящее среднее за последние 7 дней
    * медиана и стандартное отклонение за 30 дней
    * учёт сезонности: выходные vs будни, праздники, time zones
  * statistical anomaly detection:
    * расчёт z-score для каждой метрики
    * `z = (current - mean) / std_deviation`
    * если `|z| > 3` (>3 стандартных отклонений) → аномалия
  * pattern recognition через ML (optional advanced):
    * LSTM model для предсказания expected values
    * deviation от prediction → потенциальная аномалия
* При обнаружении аномалии:
  * **Immediate alert** всем super_admin через Telegram:
    * "🚨 ANOMALY DETECTED: Withdrawal requests spike"
    * "Current: 150 requests/hour, Expected: 15±5"
    * "Deviation: +900%, Z-score: 8.5"
  * **Автоматический snapshot** текущего состояния:
    * экспорт всех pending withdrawals в CSV
    * список top 10 users по activity за последний час
    * blockchain wallet balances
    * database backup timestamp
  * **Рекомендуемые действия** в алерте:
    * "Recommended: Review pending withdrawals manually"
    * "Consider: Temporary pause of auto-approvals"
    * "Action: Investigate user #12345 (suspicious activity)"
  * **Real-time dashboard**:
    * ссылка на Grafana/admin panel с live графиками
    * drill-down для детального анализа
    * historical comparison charts
* Автоматические protective actions:
  * если anomaly severity > CRITICAL:
    * временная приостановка auto-approvals для withdrawals >$1000
    * требование двух super_admin для одобрения крупных выводов
    * усиленный fraud detection для новых операций
  * логирование всех decisions в audit trail
* False positive reduction:
  * whitelist expected anomalies: marketing campaign launches, bonus programs
  * admin может "acknowledge" алерт: "Это ожидаемо, проводим акцию"
  * machine learning model учится на feedback

#### R14-2: Мониторинг health всех критичных сервисов

* Централизованная система health checks для infrastructure и application services:
  * **PostgreSQL health**:
    * connection pool status: active/idle connections, wait time
    * slow query detection: queries >1 second в slow log
    * database size и disk space available (alert если <20% free)
    * replication lag (если настроен slave)
    * transaction rate и locks
  * **Redis health**:
    * memory usage: used_memory vs maxmemory (alert если >90%)
    * eviction rate: сколько keys было evicted (alert если >0)
    * connection count: current vs max
    * hit rate: cache effectiveness (warning если <70%)
    * persistence status: last RDB/AOF save
  * **Blockchain nodes health**:
    * RPC endpoint latency для каждого node (<500ms = good, >2s = bad)
    * sync status: `eth_syncing` должен быть false (synced)
    * block height comparison: наш node vs network (lag <10 blocks)
    * peer count: количество connected peers (>5 = healthy)
    * failed requests rate: % requests с errors (<5%)
  * **Telegram Bot API health**:
    * webhook status: активен и получает updates
    * message delivery rate: % successful sends (>95%)
    * API response latency (<1s median)
    * rate limit proximity: сколько до лимита 30 msg/sec
  * **Background jobs health**:
    * `roi_distribution_job`: last successful run (должен быть <25 часов)
    * `blockchain_monitor`: processing delay (blocks behind < 3)
    * `withdrawal_processor`: queue size (pending < 50)
    * `notification_sender`: queue size и delivery rate
    * все jobs: success rate >95%, average duration tracking
* Unified health check endpoint `GET /health`:
  * возвращает JSON с overall status:
    ```json
    {
      "status": "healthy|degraded|unhealthy",
      "timestamp": "2025-01-24T12:34:56Z",
      "version": "2.0.0",
      "uptime": "5d 12h 34m",
      "components": {
        "postgresql": {
          "status": "healthy",
          "latency_ms": 5,
          "details": {
            "connections_active": 12,
            "connections_idle": 18,
            "disk_free_percent": 45
          }
        },
        "redis": {
          "status": "healthy",
          "latency_ms": 2,
          "details": {
            "memory_used_percent": 65,
            "hit_rate_percent": 87
          }
        },
        "blockchain_bsc": {
          "status": "degraded",
          "latency_ms": 1500,
          "details": {
            "block_height": 12345678,
            "sync_status": "synced",
            "reason": "High latency on primary RPC"
          }
        }
      },
      "resource_usage": {
        "cpu_percent": 35,
        "memory_percent": 68,
        "disk_percent": 42
      }
    }
    ```
  * HTTP status codes:
    * 200 OK: все компоненты healthy
    * 503 Service Unavailable: хотя бы один critical component unhealthy
    * 429 Too Many Requests: если maintenance mode
* Автоматический self-healing:
  * **Component degradation detected**:
    * попытка graceful restart компонента
    * например: Redis high memory → trigger `MEMORY PURGE` для expired keys
    * PostgreSQL slow queries → kill long-running transactions (>5 min)
  * **Critical failure detected**:
    * automatic failover на backup компонент
    * например: primary RPC node down → переключение на secondary
    * notification админам о failover
  * **Escalation при failure auto-recovery**:
    * после 3 неудачных попыток restart → эскалация
    * алерт всем super_admin: manual intervention required
    * system переходит в degraded mode с ограниченным функционалом
* External monitoring integration:
  * **Prometheus metrics export**:
    * endpoint `/metrics` в формате Prometheus
    * custom metrics: `sigmabot_withdrawals_total`, `sigmabot_deposits_confirmed`, etc.
    * standard metrics: HTTP request duration, DB query latency, etc.
  * **Grafana dashboards**:
    * "Financial Operations" dashboard: deposits, withdrawals, балансы
    * "System Health" dashboard: CPU, memory, disk, DB, Redis
    * "User Activity" dashboard: registrations, active users, sessions
  * **Alerting rules** в Prometheus/Grafana:
    * `sigmabot_withdrawals_pending > 100` → alert super_admin
    * `sigmabot_db_connection_errors_total > 10` → critical alert
    * `sigmabot_blockchain_rpc_latency > 2000` → warning
* Periodic health reports:
  * ежедневный digest в 09:00 для всех админов
  * weekly report с трендами и рекомендациями
  * monthly compliance report для аудита

#### R14-3: Лог-агрегация и анализ ошибок

* Централизованная система сбора и анализа логов:
  * **Structured logging** (JSON format) для всех компонентов:
    ```json
    {
      "timestamp": "2025-01-24T12:34:56.789Z",
      "level": "ERROR",
      "service": "withdrawal_service",
      "trace_id": "abc123-def456",
      "user_id": 12345,
      "telegram_id": 67890,
      "message": "Failed to send USDT transaction",
      "error": {
        "type": "InsufficientGasError",
        "message": "Gas price too low",
        "stack_trace": "..."
      },
      "context": {
        "withdrawal_id": 999,
        "amount": 100.0,
        "tx_hash": null
      }
    }
    ```
  * **Log levels** со strict usage policy:
    * `DEBUG`: детальная информация для разработки (выключено в production)
    * `INFO`: важные события: user registered, deposit confirmed, withdrawal approved
    * `WARNING`: потенциальные проблемы: high latency, retry triggered, threshold approaching
    * `ERROR`: ошибки требующие attention: failed transaction, DB error, API error
    * `CRITICAL`: критические ошибки требующие immediate action: service down, data corruption
  * **Обязательные поля** в каждом log entry:
    * `timestamp`: ISO 8601 с milliseconds и timezone
    * `level`: log level
    * `service`: имя сервиса/модуля
    * `message`: человекочитаемое описание
    * `trace_id`: unique ID для трассировки request через систему
    * опционально: `user_id`, `telegram_id`, `session_id`, `request_id`
* Log aggregation stack:
  * **Collection**: все контейнеры/сервисы пишут в stdout
  * **Shipping**: FluentD/Filebeat собирает логи и отправляет в storage
  * **Storage**: Elasticsearch для searchable хранилища (или Loki для lightweight)
  * **Visualization**: Kibana/Grafana для поиска и анализа
* Автоматический анализ error patterns через `ErrorAnalyzer` service:
  * **Error grouping**:
    * группировка одинаковых ошибок по `error.type` и `message` fingerprint
    * подсчёт frequency для каждой группы
    * example: "InsufficientGasError occurred 45 times in last 10 minutes"
  * **Frequency-based alerting**:
    * threshold rules: если одна ошибка >10 раз за минуту → WARNING
    * если >50 раз за минуту → CRITICAL alert
    * if same user causing errors >20 times → potential abuse
  * **Cascade failure detection**:
    * correlation между ошибками: DB error → cascade withdrawal failures
    * temporal correlation: все ошибки начались после deployment
    * dependency analysis: Redis down → все services reporting cache errors
  * **Error impact assessment**:
    * сколько users affected
    * financial impact: сумма failed transactions
    * severity scoring: critical path vs edge case
* Smart alerting system (anti-spam):
  * **Grouping**: не шлём 100 алертов для 100 одинаковых ошибок
    * вместо этого: "⚠️ 15 withdrawal failures in last 5 minutes due to blockchain timeout"
  * **Severity-based routing**:
    * CRITICAL → instant Telegram notification всем super_admin
    * ERROR → email digest каждые 15 минут (batched)
    * WARNING → daily summary report
  * **Rate limiting**:
    * maximum 5 Telegram alerts per час для non-critical
    * unlimited для CRITICAL (но с deduplication)
  * **Alert fatigue prevention**:
    * deduplication window: same alert не чаще раз в 10 минут
    * auto-resolution: если ошибка прекратилась >30 минут назад → "Resolved" notification
    * snooze option: админ может временно отключить конкретный alert
* Searchable log storage и retention:
  * **Retention policy** по levels:
    * DEBUG: не сохраняем в production (или 1 день для troubleshooting)
    * INFO: 7 дней hot (быстрый доступ), 30 дней warm (slower access)
    * WARNING/ERROR/CRITICAL: 90 дней hot, 1 год cold archive
  * **Full-text search capabilities**:
    * поиск по любым полям: user_id, error type, time range
    * example query: "все errors от user 12345 за последние 24 часа"
    * regex support для сложных patterns
  * **Distributed tracing**:
    * `trace_id` позволяет проследить полный lifecycle request:
      * user clicks "Withdraw" → handler invoked → FSM updated → DB query → blockchain call → notification sent
    * view all logs for single trace_id в timeline
    * помогает debugging сложных multi-service issues
* Log analysis для business intelligence:
  * most common errors за неделю
  * user journey analysis: какие действия приводят к errors
  * performance bottlenecks: slow operations identification
  * security events: suspicious patterns, брутфорс попытки

---

## 15. R15 — Cross-role / Cross-status сценарии (пересечения ролей)

**Критическая важность**: Самые опасные баги появляются на стыках ролей и статусов, где логика может конфликтовать.

### 15.1. Блокировки с активными операциями

#### R15-1: Блокировка пользователя с активными депозитами и pending-выводами

* Критический сценарий требующий четкой политики:
  * пользователь имеет:
    * активные депозиты с незавершенным ROI (например, 200% из 500%)
    * pending withdrawal на $500
    * scheduled ROI начисления на завтра
    * реферальная цепочка с активными рефералами
  * админ блокирует пользователя (`BlacklistActionType.BLOCKED`)
* Что происходит с каждым аспектом:
  * **ROI начисления**:
    * вариант A (строгий): полная остановка всех начислений
    * вариант B (мягкий): продолжаем начисления на баланс, но блокируем вывод
    * вариант C (гибридный): завершаем текущий период (до конца месяца), потом стоп
  * **Pending withdrawals**:
    * вариант A: автоматический reject всех pending с возвратом на баланс
    * вариант B: админ решает для каждой заявки индивидуально
    * вариант C: "заморозка" в специальный статус `FROZEN` до разблокировки
  * **Реферальные начисления**:
    * от активности рефералов заблокированного: продолжаем или стоп?
    * заблокированный как реферер в чужих цепочках: пропускаем или начисляем?
  * **Доступ к истории и данным**:
    * показываем ли read-only view балансов и транзакций?
    * или полный blackout кроме кнопки "Подать апелляцию"?
* Рекомендуемая политика (требует утверждения):
  * ROI: остановка новых начислений
  * Withdrawals: перевод в `FROZEN`, решение после review
  * Referral: продолжаем начисления (это деньги рефереров, не нарушителя)
  * History: read-only доступ сохраняется
* Код должен явно проверять blacklist перед каждой операцией
* Логирование всех "заблокированных" действий для аудита

#### R15-2: Переключение BLOCKED → TERMINATED

* Эскалация санкций после расследования:
  * пользователь был `BLOCKED` (мягкая блокировка)
  * подал апелляцию, но расследование показало серьезное нарушение
  * super_admin переводит в `TERMINATED` (жесткий бан)
* Что происходит при переключении:
  * **Активные апелляции**:
    * автоматический reject всех appeals с причиной "Account terminated"
    * закрытие всех связанных support tickets
  * **BanMiddleware поведение**:
    * теперь глотает все updates полностью
    * пользователь видит "мертвый" бот (нет ответов)
  * **Фоновые задачи**:
    * ROI distribution: skip этого пользователя
    * referral earnings: пересчет цепочек с исключением terminated
    * notification queue: очистка всех pending уведомлений
  * **Финансовые операции**:
    * все pending withdrawals → автоматический `REJECTED`
    * средства остаются на балансе (возможная конфискация или возврат по решению legal)
  * **Видимость для пользователя**:
    * бот полностью не отвечает (как будто удален)
    * никаких объяснений или возможности апелляции
* Transition должен быть атомарным:
  * одна транзакция обновляет blacklist entry
  * триггеры или background job обрабатывают все последствия
* Обязательное логирование причины перехода для legal compliance

#### R15-3: Активный finpass_recovery + параллельный запрос вывода

* Race condition между восстановлением пароля и попыткой вывода:
  * пользователь инициировал finpass recovery
  * запрос находится в статусе `PENDING` (ждет одобрения админа)
  * пользователь пытается создать withdrawal (вспомнил старый пароль? атака?)
* Правила блокировки:
  * **Все новые withdrawal requests блокируются**:
    * проверка `FinpassRecoveryService.has_active_recovery(user_id)`
    * если true → отказ с сообщением:
      * "У вас есть активный запрос на восстановление финансового пароля"
      * "Выводы временно заблокированы до решения по запросу"
      * "Обратитесь в поддержку если это ошибка"
  * **Существующие PENDING withdrawals**:
    * вариант A: автоматическая заморозка в статус `ON_HOLD`
    * вариант B: остаются в PENDING, но админ видит warning при approve
  * **После одобрения нового пароля**:
    * первый успешный withdrawal с новым паролем снимает блокировку
    * или explicit admin action "unlock withdrawals"
* Security логика:
  * если попытка withdrawal с ПРАВИЛЬНЫМ старым паролем → auto-reject finpass recovery
  * если с НЕПРАВИЛЬНЫМ → увеличиваем risk score (возможная атака)
* Уведомления:
  * при попытке вывода → уведомление админам о suspicious activity
  * пользователю → напоминание о активном recovery запросе

#### R15-4: Админ, который одновременно является инвестором и попадает в blacklist

* Конфликт ролей при санкциях:
  * user имеет флаг `is_admin=True` (или даже `is_super_admin`)
  * одновременно является активным инвестором (депозиты $10k, баланс $5k)
  * нарушил правила (злоупотребление полномочиями, fraud, etc.)
  * другой super_admin блокирует его
* Обработка админских прав:
  * **Немедленное снятие всех административных прав**:
    * `is_admin=False`, `is_extended_admin=False`, `is_super_admin=False`
    * инвалидация всех активных `AdminSession`
    * revoke доступа к админ-панели
  * **Логирование и аудит**:
    * детальная запись в `admin_actions`: кто заблокировал, когда, причина
    * список всех действий заблокированного админа за последние 30 дней
    * проверка подозрительных операций (массовые approvals, изменения балансов)
* Обработка пользовательской части:
  * **Финансовые операции**:
    * политика зависит от причины блокировки:
      * fraud → полная заморозка, возможная конфискация
      * policy violation → возможность вывода существующего баланса
      * technical breach → полный доступ к средствам
  * **Доступ к истории**:
    * показываем историю транзакций как обычному пользователю
    * но скрываем историю административных действий
  * **Реферальная система**:
    * если был рефералом других пользователей → продолжает получать earnings
    * свои рефералы → продолжают работать (это их деньги)
* Целевая политика (документировать в коде):
  * админские права снимаются мгновенно и безусловно
  * финансовые права зависят от severity нарушения
  * полное разделение admin context и user context в коде

#### R15-5: Смена роли админа во время активной сессии

* Dynamic role change без перезапуска:
  * `user_123` является `extended_admin` с активной сессией
  * super_admin понижает его до обычного `admin`
  * или вообще удаляет из админов (`is_admin=False`)
  * сессия еще активна (не истек таймаут 15 минут)
* Что должно произойти:
  * **Активная AdminSession**:
    * при следующем action проверка прав
    * обнаружение несоответствия: session.role > user.actual_role
    * форсированный logout с сообщением "Ваши права были изменены"
    * требование повторной аутентификации
  * **Кнопки в админ-панели**:
    * должны динамически перестраиваться на основе текущих прав
    * extended-only кнопки исчезают для downgraded админа
    * при попытке использовать старую inline-кнопку → error "Недостаточно прав"
  * **Уже начатые критические операции**:
    * если админ начал approve крупного вывода, но в процессе потерял права:
      * операция должна быть rollback
      * или требовать повторного подтверждения с новыми правами
    * все изменения должны проверять права на момент commit, не на момент start
* Реализация:
  * middleware проверяет права на каждый request
  * кэш прав в Redis с TTL 60 секунд (компромисс между производительностью и security)
  * при downgrade/удалении админа → explicit invalidation кэша
* Уведомления:
  * downgraded админу: "Ваши административные права были изменены"
  * super_admin who made change: confirmation с логом предыдущих действий

#### R15-6: Пользователь → BLOCKED во время обработки вывода

* Timing attack scenario:
  * пользователь создал withdrawal request в 10:00:00
  * запрос находится в статусе `PENDING`
  * в 10:05:00 fraud detection поднимает alarm
  * в 10:10:00 админ блокирует пользователя
  * в 10:15:00 другой админ (не зная о блокировке) одобряет withdrawal
* Проблема: race condition между ban и approval
* Решение через atomic checks:
  * при `approve_withdrawal` ОБЯЗАТЕЛЬНАЯ проверка:
    ```python
    # Inside transaction
    user = session.query(User).filter_by(id=withdrawal.user_id).with_for_update().first()
    blacklist = BlacklistService.is_blacklisted(user.telegram_id)
    
    if blacklist and blacklist.action_type != BlacklistActionType.NONE:
        # User is blocked/terminated
        withdrawal.status = WithdrawalStatus.REJECTED
        withdrawal.rejection_reason = f"User blocked: {blacklist.reason}"
        # Return funds to balance
        UserService.adjust_balance(user.id, withdrawal.amount, "withdrawal_rejected_user_blocked")
        return Error("Cannot approve withdrawal for blocked user")
    
    # Proceed with approval
    ```
  * проверка должна быть внутри той же транзакции что и одобрение
  * использование `FOR UPDATE` lock предотвращает concurrent блокировку
* Политика для PENDING withdrawals созданных ДО блокировки:
  * **вариант A (строгий)**: автоматический reject всех pending при блокировке
  * **вариант B (мягкий)**: админ может одобрить если причина блокировки не финансовая
  * **вариант C (recommended)**: freeze в статус `REQUIRES_REVIEW`, explicit решение
* Маркировка в аудите:
  * все withdrawal operations для blocked users логируются с флагом `user_was_blocked`
  * timestamp блокировки vs timestamp withdrawal creation для анализа
  * reject reason должен явно указывать причину: "User blocked on 2025-01-24 10:10:00"
* Уведомления:
  * админу при попытке approve → warning "User is currently blocked"
  * в случае auto-reject → уведомление пользователю (если политика разрешает)

#### R15-7: Партнёр с рефералами, переведённый в TERMINATED

* Каскадное влияние на реферальную структуру:
  * User A (active) → User B (terminated) → User C (active) → User D (active)
  * User B был важным звеном в цепочке
  * его terminat'ят за fraud
* Варианты обработки реферальной цепочки:
  * **Вариант 1: Полное удаление из цепочки**
    * User B "вырезается" из referral chain
    * User C теперь напрямую привязан к User A
    * все будущие earnings от User C идут User A (level 2 → level 1)
    * исторические earnings User B остаются (уже выплачены)
  * **Вариант 2: Перераспределение unpaid earnings**
    * все unpaid earnings User B переводятся на User A (его referrer)
    * как compensation за потерю звена
    * или распределяются по всей цепочке пропорционально
  * **Вариант 3: Заморозка цепочки**
    * User B остается в цепочке технически
    * но все earnings через него = 0%
    * цепочка "разорвана", User C больше не приносит earnings никому выше
  * **Вариант 4: Блокировка только новых earnings**
    * структура цепочки не меняется
    * но от новых депозитов User B ничего не получает
    * существующие его рефералы продолжают приносить earnings как раньше
* Рекомендуемая политика (нужно согласовать с бизнесом):
  * **immediate action при terminate**:
    * stop всех новых earnings для terminated user
    * set флаг `can_earn_referrals=False`
  * **обработка цепочки**:
    * НЕ перестраиваем цепочку автоматически (сложно, риск ошибок)
    * просто skip terminated users в расчетах
    * earnings идут живым звеньям, пропуская terminated
  * **unpaid earnings**:
    * остаются на балансе terminated user
    * возможность withdrawal зависит от причины terminate
    * при fraud → конфискация или legal hold
* Реализация в `ReferralService.process_deposit_referral_earnings()`:
  * получение chain с фильтром `WHERE is_terminated=False`
  * или manual skip в цикле при обнаружении terminated
  * логирование каждого skipped referrer
* Влияние на статистику:
  * terminated user остается в `referral_count` своего referrer'а
  * но не приносит новых earnings
  * метрика "active referrals" vs "total referrals"

---

## 16. R16 — Identity / multi-account сценарии

**Критичность**: Вопросы идентичности напрямую связаны с деньгами и юридическими рисками.

### 16.1. Мультиаккаунтинг и дубликаты

#### R16-1: Один кошелёк на два Telegram-аккаунта (multiaccounting)

* Попытка создания дубликата аккаунта:
  * User A уже зарегистрирован с wallet `0xABC...123`
  * User B (другой telegram_id) пытается зарегистрироваться с тем же wallet `0xABC...123`
* Возможные причины:
  * **легитимные**:
    * потеря доступа к старому Telegram аккаунту
    * смена номера телефона
    * случайная ошибка (пользователь забыл о старом аккаунте)
  * **мошеннические**:
    * попытка получить двойной referral bonus
    * обход блокировки старого аккаунта
    * farming бонусов/акций
* Варианты политики:
  * **Вариант A (жёсткий запрет)**:
    * ВСЕГДА отказ при обнаружении дубликата wallet
    * сообщение: "Этот кошелёк уже привязан к другому аккаунту"
    * единственный путь: обращение в поддержку с KYC
  * **Вариант B (автоматический перенос)**:
    * если старый аккаунт неактивен >90 дней и баланс=0:
      * автоматический перенос привязки на новый telegram_id
      * старый аккаунт помечается как `replaced`
    * если есть активность или баланс → жёсткий отказ
  * **Вариант C (ручной процесс через поддержку)**:
    * любой дубликат → автоматический отказ + создание support ticket
    * админ проверяет:
      * KYC обоих аккаунтов (если есть)
      * активность и балансы
      * признаки fraud
    * принимает решение о переносе или отказе
* Рекомендуемая реализация:
  * проверка в `UserService.create_user()`:
    ```python
    existing_user = UserRepository.get_by_wallet(wallet_address)
    if existing_user:
        if existing_user.telegram_id == new_telegram_id:
            # Same user, just re-registering - allow
            return existing_user
        else:
            # Different telegram_id - potential fraud
            SecurityService.log_multiaccounting_attempt(
                new_telegram_id, 
                existing_user.telegram_id,
                wallet_address
            )
            raise WalletAlreadyInUseError(
                "This wallet is already registered to another account."
                "Please contact support if you believe this is an error."
            )
    ```
  * логирование ВСЕХ попыток в `multiaccounting_attempts` для анализа
  * metrics: daily count multiaccounting attempts
  * periodic review: если >10 attempts от одного telegram → ban

#### R16-2: Один Telegram-аккаунт с несколькими кошельками

* Попытка смены кошелька:
  * User зарегистрирован с wallet A
  * хочет перейти на wallet B (новый, более безопасный, lost access, etc.)
* Текущая политика проекта:
  * один user = один wallet (фиксированная привязка)
  * смена кошелька официально НЕ поддерживается
* Что нужно зафиксировать явно:
  * **Допускаем ли смену вообще?**
    * если YES → через какой процесс?
    * если NO → как обрабатываем запросы?
  * **Процесс смены кошелька (если разрешено)**:
    * создание wallet change request через support
    * requirements:
      * KYC verification обязательна
      * подтверждение владения ОБОИМИ кошельками:
        * signature от старого кошелька
        * signature от нового кошелька
      * финансовый пароль для подтверждения
      * waiting period 7-14 дней (security cooldown)
    * admin approval обязательна
  * **Что происходит при смене**:
    * все депозиты остаются привязаны к user_id (не к wallet)
    * история транзакций сохраняется полностью
    * реферальная структура НЕ меняется
    * новые депозиты должны идти на новый wallet
    * старый wallet добавляется в таблицу `historical_wallets` для audit
  * **Риски и ограничения**:
    * максимум N смен за lifetime (например, 2)
    * нельзя менять если есть pending withdrawals
    * нельзя менять если wallet в blacklist
    * новый wallet не должен быть привязан к другому user
* Альтернатива (если смена запрещена):
  * пользователь может вывести все средства
  * зарегистрировать новый аккаунт с новым wallet
  * реферальная цепочка НЕ переносится
  * это считается как completely new user
* Документация:
  * FAQ: "Могу ли я сменить кошелёк?"
  * четкое объяснение причин ограничения (security, fraud prevention)
  * альтернативные решения

#### R16-3: Потеря Telegram-аккаунта (номер умер, телега недоступна)

* Критический сценарий восстановления доступа:
  * пользователь потерял доступ к Telegram:
    * номер телефона аннулирован провайдером
    * Telegram аккаунт удален самим пользователем
    * взлом и потеря контроля над аккаунтом
    * телефон утерян без возможности восстановления 2FA
  * имеет значительный баланс и активные депозиты
  * создает новый Telegram аккаунт
  * пишет в поддержку: "Я потерял доступ к аккаунту, но это мои деньги!"
* Процесс верификации и восстановления:
  * **Шаг 1: Proof of wallet ownership**
    * пользователь должен подписать сообщение приватным ключом кошелька:
      ```
      Message: "I am recovering my SigmaTradeBot account. New Telegram ID: 123456789. Timestamp: 2025-01-24T12:00:00Z"
      Signature: 0x...
      ```
    * или сделать тестовую транзакцию с известной memo
  * **Шаг 2: Дополнительная верификация**
    * если был KYC → сверка документов
    * если были контакты (email/phone) → подтверждение через них
    * security questions (если реализованы)
    * предоставление деталей:
      * даты депозитов
      * суммы депозитов
      * имена рефералов
  * **Шаг 3: Admin review**
    * проверка suspicious activity на старом аккаунте перед блокировкой
    * проверка что не было недавних выводов (возможный theft)
    * scoring риска: low/medium/high
  * **Шаг 4: Перенос аккаунта**
    * блокировка старого telegram_id (set в blacklist с типом `ACCOUNT_MIGRATED`)
    * обновление `users.telegram_id` на новый
    * обновление `users.username` если изменился
    * генерация нового финпароля (обязательно!)
    * уведомление на все контакты о смене доступа
* Что переносится:
  * полная история транзакций
  * все депозиты и их статусы
  * баланс (available + pending)
  * реферальная структура (и как referrer, и как referee)
  * verification status
  * settings и preferences
* Что НЕ переносится:
  * старый финансовый пароль (генерируем новый!)
  * активные FSM states
  * админские права (если были - требуют отдельного подтверждения)
* Security measures:
  * waiting period 48-72 часа между request и migration
  * уведомление на ВСЕ известные контакты (email, старый telegram если доступен)
  * возможность отмены миграции в течение waiting period
  * permanent log в audit trail
* Edge case: что если ОБОИМИ аккаунтами владеют разные люди?
  * крайне сложная ситуация требующая legal involvement
  * wallet signature подтверждает владение средствами
  * но telegram может быть украден
  * solution: KYC становится обязательным для recovery
  * возможна заморозка до выяснения

#### R16-4: Коммерческая продажа аккаунта с балансом

* "Серый" сценарий:
  * User A продает свой аккаунт User B:
    * передает доступ к Telegram
    * передает приватный ключ кошелька
    * передает финансовый пароль
  * технически: telegram_id и wallet остаются прежними
  * но реальный владелец сменился
* Позиция платформы (требует legal approval):
  * **официально**: аккаунты НЕ подлежат продаже (ToS violation)
  * **технически**: система НЕ может обнаружить такую передачу
  * **практически**: не даем специальных гарантий
* Что может выявить смену владельца:
  * резкое изменение поведенческих паттернов:
    * изменение timezone активности
    * изменение language_code в Telegram
    * новый стиль общения в support
  * подозрительные финансовые операции:
    * immediate withdrawal всего баланса после "продажи"
    * изменение wallet address (если разрешено)
  * fraud detection триггеры:
    * login с нового устройства (если трекаем)
    * изменение email/phone контактов
* Автоматическая реакция при подозрении:
  * повышение risk score (R10-1)
  * требование дополнительной верификации для крупных операций
  * возможная временная блокировка выводов для review
* Что делаем при выявлении:
  * если обнаружили факт продажи:
    * warning обоим пользователям о нарушении ToS
    * возможная блокировка аккаунта
    * НО: средства все равно принадлежат владельцу wallet (blockchain truth)
  * legal complexity: кому принадлежат средства - покупателю или продавцу?
* Документация в ToS:
  * явный запрет на продажу/передачу аккаунтов
  * disclaimer: платформа не гарантирует безопасность при нелегальной передаче
  * ответственность за последствия на пользователях

#### R16-5: Сам себе реферал (self-referral schemes)

* Мошеннические схемы самонакрутки:
  * **схема 1: Прямой self-referral**
    * User A создает аккаунт 1
    * создает реферальную ссылку
    * регистрирует аккаунт 2 по своей же ссылке
    * делает депозит с аккаунта 2 → получает бонус на аккаунт 1
  * **схема 2: Кольцевая структура**
    * User A → аккаунт 1 (wallet A)
    * User A → аккаунт 2 (wallet B) referrer = аккаунт 1
    * User A → аккаунт 3 (wallet C) referrer = аккаунт 2
    * аккаунт 1 делает депозит → earnings идут всем трем его же аккаунтам
  * **схема 3: Семейная накрутка**
    * один человек регистрирует аккаунты на всех членов семьи
    * строит длинную цепочку
    * минимальные депозиты для активации
    * фарминг реферальных бонусов
* Частично закрыто существующими мерами:
  * R4-8: циклические цепочки определяются и блокируются
  * R10-1: fraud detection выявляет подозрительные паттерны
* Дополнительные индикаторы для detection:
  * **один контролирующий субъект**:
    * одинаковые или похожие email адреса (если вводят контакты)
    * последовательные wallet addresses (0xAAA001, 0xAAA002, ...)
    * регистрации в короткий период времени (все за 1 день)
    * одинаковый timezone и language_code
  * **минимальная активность**:
    * депозиты только для активации рефералки
    * нет реальной торговой активности
    * withdrawals сразу после minimum ROI period
  * **географические кластеры**:
    * если получится определить general location (через Telegram API metadata)
    * все аккаунты из одного региона/устройства
* Automated detection в `FraudDetectionService`:
  * при создании referral connection:
    * check: есть ли уже N (например 5+) рефералов с похожими характеристиками
    * check: реферальная цепочка глубже 3 уровней с подозрительными метриками
    * check: все депозиты в цепочке минимальные ($10)
  * risk scoring на основе паттернов
  * при score > threshold:
    * автоматическая приостановка реферальных earnings
    * алерт админам для manual review
* Что делаем при выявлении:
  * **confirmed self-referral scheme**:
    * аннулирование всех реферальных earnings в цепочке
    * возврат незаконно полученных бонусов (если возможно)
    * ban всех связанных аккаунтов
    * wallet addresses в blacklist
  * **подозрительная, но не доказанная**:
    * замораживание реферальных earnings до review
    * требование KYC для всех участников цепочки
    * если KYC пройден и различается → разблокировка
* Профилактические меры:
  * минимальные требования для получения реферальных бонусов:
    * депозит должен быть активен >30 дней
    * минимум N транзакций (не только один депозит)
    * верификация рефереров для крупных earnings
  * caps на реферальные earnings:
    * максимум X USDT в месяц с одного реферала
    * максимум Y активных рефералов без KYC
    * при превышении → обязательная верификация

---

## 17. R17 — Product / policy change сценарии

**Важность**: Изменения бизнес-правил требуют тщательной обработки legacy данных и миграций.

### 17.1. Изменения депозитных условий

#### R17-1: Изменение условий депозитных планов

* Сценарий изменения экономики:
  * текущие условия: Level 1 = $10, ROI 2% daily, cap 500%
  * новые условия: Level 1 = $15, ROI 1.5% daily, cap 400%
  * или наоборот: улучшение условий для привлечения пользователей
* **Реализация через:**
  * R17-6–R17-13: Deposit Level Versioning (система версионирования)
  * R6-26: Настройка сумм уровней (UI)
  * R6-19/R6-20: Изменение ROI коридоров (UI)
* Критический принцип: **гранулированность условий**
  * старые депозиты ДОЛЖНЫ жить по старым правилам (защита пользователей)
  * новые депозиты — по новым правилам
  * нельзя просто обновить `deposit_levels.amount` в БД!
* Правильная реализация:
  * **Версионирование deposit conditions**:
    * добавление поля `deposit_levels.version` или `effective_from` date
    * или отдельная таблица `deposit_level_versions`:
      ```sql
      CREATE TABLE deposit_level_versions (
        id SERIAL PRIMARY KEY,
        level_number INT NOT NULL,
        amount DECIMAL(10,2) NOT NULL,
        roi_percent DECIMAL(5,2) NOT NULL,
        roi_cap_percent INT NOT NULL,
        effective_from TIMESTAMP NOT NULL,
        effective_until TIMESTAMP,
        created_by_admin_id INT
      );
      ```
    * каждый депозит ссылается на конкретную версию условий
  * **Миграционный скрипт**:
    * создание новой версии deposit level conditions
    * НЕ трогаем существующие records
    * все новые депозиты используют latest version
  * **ROI distribution логика**:
    * при начислении ROI: `deposit.roi_percent` from deposit's version
    * не берем из текущего `deposit_levels` config
    * каждый deposit хранит snapshot условий на момент создания
* Коммуникация с пользователями:
  * уведомление о изменениях минимум за 7 дней
  * четкое объяснение: "новые условия только для новых депозитов"
  * FAQ: "Изменятся ли условия моих текущих депозитов?" → НЕТ
* UI отображение:
  * в меню депозитов показываем новые условия
  * в "Мои депозиты" каждый депозит показывает свои условия
  * возможно badge "Legacy plan" vs "Current plan"
* Edge case: что если пользователь хочет "upgrade" старый депозит?
  * официально: нельзя, условия зафиксированы
  * альтернатива: закрыть старый (вывести), открыть новый
  * но потеряет прогресс ROI

#### R17-2: Временное отключение конкретного уровня

* Причины отключения level:
  * **риск-менеджмент**: слишком большой спрос, нужна пауза
  * **техническая**: проблемы с обработкой крупных депозитов
  * **регуляторная**: временные ограничения от authorities
* **Реализация:** R6-37 (включение/отключение уровня), R6-34 (управление уровнями), R17-6 (is_active в версии)
  * **ликвидность**: недостаточно средств для поддержки уровня
* Поведение системы:
  * **новые депозиты на уровень блокируются**:
    * флаг `deposit_levels.is_active=False`
    * или `deposit_levels.is_available=False` (более явное название)
    * при попытке создать депозит:
      * проверка `if not level.is_active`
      * ошибка: "Этот уровень временно недоступен"
  * **активные депозиты продолжают работать**:
    * ROI начисления идут по расписанию
    * пользователи могут просматривать статус
    * withdrawals разрешены
  * **UI изменения**:
    * кнопка level показывает замочек 🔒
    * текст: "Level 3 временно недоступен"
    * опционально: estimated time of reopening
    * ссылка на объявление с объяснением
* Административный интерфейс:
  * кнопка "Deactivate Level" в админ-панели
  * обязательное поле "Reason" для логирования
  * опционально: planned reactivation date
  * подтверждение с warning о последствиях
* Реактивация:
  * кнопка "Reactivate Level"
  * проверки перед активацией:
    * достаточно ли ликвидности?
    * нет ли других блокирующих issues?
  * уведомление пользователям: "Level X снова доступен!"
* Логирование:
  * каждое изменение `is_active` в `admin_actions`
  * timestamp, admin_id, reason
  * для compliance и аудита

#### R17-3: Глобальный стоп на выводы (emergency brake)

* Критический сценарий emergency response:
  * **триггеры**:
    * подозрение на взлом системы
    * обнаружение критического бага в withdrawal logic
    * проблемы с ликвидностью (недостаточно средств в hot wallet)
    * атака на платформу (mass withdrawal attack)
    * регуляторное требование (court order, investigation)
  * требуется немедленная остановка ВСЕХ выводов
* Механизм реализации:
  * **глобальный флаг** в system config:
    * `system_settings.withdrawals_enabled=False`
    * или feature flag: `WITHDRAWALS_EMERGENCY_STOP`
    * хранится в Redis для быстрого доступа + DB для persistence
  * **проверка в начале каждого withdrawal flow**:
    ```python
    if not SystemConfig.get('withdrawals_enabled'):
        raise WithdrawalsDisabledError(
            "Withdrawals are temporarily disabled due to maintenance.\n"
            "Your funds are safe. Please try again later or contact support."
        )
    ```
  * **обработка existing PENDING withdrawals**:
    * вариант A: автоматический перевод в статус `ON_HOLD`
    * вариант B: запрет approve/reject, они "замораживаются"
    * вариант C: оставляем PENDING, но админ видит emergency banner
* UI изменения для пользователей:
  * кнопка "Вывод" показывает banner:
    * "⚠️ Выводы временно приостановлены"
    * "Ваши средства в безопасности"
    * "Мы работаем над решением проблемы"
    * ссылка на status page или announcement
  * при попытке начать withdrawal → честное объяснение
  * НЕ показываем панику: "Технические работы" вместо "EMERGENCY!"
* Административный интерфейс:
  * большая красная кнопка "EMERGENCY STOP WITHDRAWALS"
  * требует:
    * admin confirmation (repeat master key)
    * обязательное поле "Reason" (minimum 50 characters)
    * автоматическое уведомление всех super_admins
  * показывает:
    * количество affected pending withdrawals
    * total amount in pending withdrawals
    * estimated impact
* Процесс восстановления:
  * кнопка "Resume Withdrawals"
  * требует:
    * verification что проблема решена
    * review всех ON_HOLD withdrawals
    * возможно manual approval первых N withdrawals после resume
  * постепенное возобновление:
    * первые 10 withdrawals требуют manual approval
    * потом постепенное увеличение лимитов
    * полное восстановление после подтверждения стабильности
* Коммуникация:
  * немедленное уведомление в official channel
  * regular updates каждые 2-4 часа
  * transparent объяснение причины (насколько возможно)
  * estimated time to resolution

#### R17-4: Временный стоп на депозиты

* Причины приостановки депозитов:
  * **технические**: обновление smart contract, migration системы
  * **экономические**: достигнут лимит TVL (Total Value Locked)
  * **маркетинговые**: пауза перед запуском новой кампании
  * **регуляторные**: compliance review, audit period
* Поведение системы:
  * **флаг** `system_settings.deposits_enabled=False`
  * **новые депозиты запрещены**:
    * кнопки "Пополнить Level N" disabled или скрыты
    * при попытке: "Депозиты временно приостановлены"
    * четкое объяснение причины и estimated duration
  * **существующие депозиты не затронуты**:
    * ROI начисления идут по плану
    * withdrawals доступны
    * все функции кроме создания новых депозитов
  * **pending депозиты** (уже созданы, но еще не подтверждены):
    * продолжают обрабатываться
    * blockchain monitor продолжает работу
    * эти депозиты будут подтверждены даже при общем стопе
* UI для пользователей:
  * banner в главном меню: "Новые депозиты временно недоступны"
  * раздел "Депозиты" показывает объяснение
  * countdown до reopening (если известна дата)
  * возможность подписаться на уведомление о возобновлении

#### R17-5: Переезд на новый системный кошелёк / smart contract

* Критическая миграция инфраструктуры:
  * причины смены:
    * security: компрометация старого кошелька
    * upgrade: переход на новую версию контракта
    * scaling: распределение на multiple wallets
    * regulatory: требование использовать custodial solution
* Сложности миграции:
  * **старые депозиты в пути**:
    * транзакции отправлены на старый адрес
    * еще не подтверждены в блокчейне
    * может быть pending несколько часов
  * **двойной мониторинг**:
    * нужно одновременно мониторить оба адреса
    * старый: до полного завершения всех pending
    * новый: для всех новых депозитов
  * **risk потери транзакций**:
    * момент переключения critical
    * транзакция может прийти в "gap"
* Правильный процесс миграции:
  * **Фаза 1: Подготовка (T-7 дней)**
    * создание и проверка нового кошелька
    * deploy нового контракта (если applicable)
    * тестирование на testnet
    * подготовка мониторинга для двух адресов
    * уведомление пользователей о предстоящей смене
  * **Фаза 2: Dual monitoring (T-1 день до T+7 дней)**
    * в день X включаем новый адрес в UI
    * но продолжаем мониторить старый адрес
    * все новые депозиты идут на новый адрес
    * старые pending депозиты обрабатываются со старого
    * duration: достаточно долго чтобы поймать все slow transactions
  * **Фаза 3: Graceful cutoff старого адреса**
    * после T+7 дней проверяем:
      * нет ли pending депозитов на старый адрес
      * последняя транзакция была >48 часов назад
    * переводим остатки со старого кошелька на новый
    * отключаем мониторинг старого адреса
  * **Фаза 4: Cleanup**
    * обновление всей документации
    * архивирование старого адреса
    * post-mortem: были ли проблемы?
* Технические детали:
  * **Config управление**:
    * `DEPOSIT_WALLET_ADDRESS_PRIMARY=0x...NEW`
    * `DEPOSIT_WALLET_ADDRESS_LEGACY=0x...OLD` (temporary)
    * флаг `MONITOR_LEGACY_WALLET=True` (период dual monitoring)
  * **Blockchain monitor обновления**:
    * поддержка multiple адресов
    * приоритизация: новый адрес чаще
    * логирование source address для каждого депозита
  * **UI изменения**:
    * показываем новый адрес пользователям
    * warning: "Используйте новый адрес, старый скоро будет отключен"
    * countdown до отключения старого
* Recovery plan:
  * если что-то пошло не так → можем rollback на старый адрес
  * все конфиги в config files/env, не hardcoded
  * ability to quickly switch back

### 17.2. Deposit Level Versioning (система версионирования условий)

#### R17-6: Система версионирования deposit_level_versions (архитектура)

* **Цель:** Позволить изменять условия депозитов (сумма, ROI, cap) БЕЗ влияния на существующие депозиты
* **Гарантия:** Старые депозиты продолжают работать по старым условиям, новые создаются по новым
* **Таблица:** `deposit_level_versions`
* **Ключевые поля:**
  * `id` (PK, serial)
  * `level_number` (Int, 1-5) - номер уровня
  * `amount` (Decimal) - требуемая сумма для открытия депозита
  * `roi_percent` (Decimal) - базовый процент доходности
  * `roi_cap_percent` (Int, default 500) - cap лимит ROI в процентах
  * `version` (Int) - номер версии (incrementing для каждого level)
  * `is_active` (Boolean, default True) - доступен ли уровень для создания новых депозитов
  * `effective_from` (Timestamp) - дата начала действия версии
  * `effective_until` (Timestamp, nullable) - дата окончания (для истории)
  * `created_by_admin_id` (ForeignKey → admins.id, nullable) - кто создал версию
  * `created_at` (Timestamp, default NOW())
* **Индексы:**
  * уникальный составной индекс на `(level_number, version)` - предотвращает дублирование версий
  * индекс на `(level_number, is_active)` для быстрого поиска активной версии
  * индекс на `effective_from` для временных запросов
* **Связи:**
  * `deposits.deposit_version_id` → `deposit_level_versions.id` (ForeignKey, nullable for legacy)
  * каждый депозит "запоминает" свою версию при создании
* **Примеры:**
  * **Level 1, version 1:** amount=10, roi_percent=2.0, version=1, is_active=True
  * админ меняет amount на 15 (R6-26):
    * создается **Level 1, version 2:** amount=15, roi_percent=2.0, version=2, is_active=True
    * version 1: `is_active` остается True (или можно сделать False), но `effective_until` не обязательно устанавливается
  * новые депозиты требуют 15 USDT
  * старые депозиты (с version_id=1) продолжают работать с 10 USDT conditions
* **Edge cases:**
  * одновременное изменение amount и roi_percent:
    * если изменяется только amount (R6-26): roi_percent копируется из предыдущей версии
    * если изменяется corridor (R6-23): roi_percent обновляется в corridor config, НЕ создается новая версия
  * удаление версии (не поддерживается): версии immutable после создания (для audit trail)
  * manual SQL изменение version: ЗАПРЕЩЕНО (нарушение консистентности)
* **Связанные сценарии:** R17-7 (создание версии), R17-8 (привязка депозита), R6-26 (UI для создания), R17-10 (отображение)
* **Миграция:** При внедрении системы:
  * создать initial версии для всех 5 уровней с текущими параметрами
  * обновить существующие депозиты: установить `deposit_version_id` к initial версиям (опционально, можно оставить NULL для legacy)

#### R17-7: Создание новой версии уровня при изменении суммы

* **Триггер:** `RoiCorridorService.set_level_amount(level, new_amount, admin_id)` (вызывается из R6-26)
* **Процесс:**
  1. **Получение current version:**
     * запрос: `DepositLevelVersionRepository.get_current_version(level_number=level)`
     * фильтр: `level_number=level AND is_active=True ORDER BY version DESC LIMIT 1`
     * если нет версии (первая инициализация):
       * создание с `version=1`
       * использование default параметров: roi_percent=2.0, roi_cap_percent=500
  2. **Расчет new version number:**
     * если есть current: `new_version = current_version.version + 1`
     * если нет: `new_version = 1`
  3. **Копирование настроек из current (если есть):**
     * `roi_percent = current_version.roi_percent` (или получение из corridor config)
     * `roi_cap_percent = current_version.roi_cap_percent` (обычно 500)
     * `is_active = current_version.is_active` (наследуется статус)
  4. **Получение актуального ROI percent:**
     * вызов `RoiCorridorService.get_corridor_config(level)`
     * если режим `mode="equal"`: использование `roi_fixed` как `roi_percent`
     * если режим `mode="custom"`: использование среднего значения `(roi_min + roi_max) / 2` (опционально)
     * fallback: использование roi_percent из current version или default 2.0
  5. **Создание новой версии:**
     * `DepositLevelVersionRepository.create()` с параметрами:
       * `level_number=level`
       * `amount=new_amount` (главное изменение!)
       * `roi_percent=calculated`
       * `roi_cap_percent=copied or 500`
       * `version=new_version`
       * `is_active=True` (новая версия всегда активна при создании)
       * `effective_from=NOW()`
       * `created_by_admin_id=admin_id`
     * commit транзакции
  6. **Опционально: обновление старой версии:**
     * если хочется явно "закрыть" старую версию:
       * `current_version.effective_until = NOW()`
       * `current_version.is_active = False` (если хотим запретить новые депозиты по старой)
     * или оставить как есть (обе версии is_active=True, но новые депозиты выбирают latest)
  7. **Логирование:**
     * INFO: "Created new level version" с параметрами: level, version, amount, admin_id
* **Обновление БД:**
  * `deposit_level_versions` - INSERT новой записи
  * опционально `current_version` - UPDATE (effective_until, is_active)
* **Уведомления:** через R6-26 (админы уведомляются об изменении)
* **Логирование:**
  * INFO: детали создания версии
  * WARNING: если roi_percent не найден в corridor config (fallback на default)
* **Обработка ошибок:**
  * ошибка commit → rollback, exception bubbles up to caller (R6-26)
  * нарушение unique constraint `(level_number, version)` → крайне редко, логирование CRITICAL
  * admin_id не найден → допускается (можно создавать версии программно)
* **Edge cases:**
  * создание версии с той же суммой что и предыдущая → допускается (админ хочет "заморозить" версию)
  * создание нескольких версий подряд за короткое время → все версии сохраняются (полная история)
  * изменение суммы на меньшую (например, с 15 на 10) → допускается (снижение порога входа)
* **Связанные сценарии:** R6-26 (UI trigger), R17-6 (архитектура), R17-8 (привязка депозита к версии)
* **Сервис:** `RoiCorridorService.set_level_amount()`
* **Репозиторий:** `DepositLevelVersionRepository.create()`, `get_current_version()`

#### R17-8: Привязка депозита к версии при создании

* **Триггер:** Создание нового депозита пользователем (R3-2)
* **Процесс:** В момент создания депозита в `DepositService.create_deposit()`
  1. **Получение current version:**
     * вызов `DepositLevelVersionRepository.get_current_version(level_number=deposit.level)`
     * если нет версии:
       * логирование WARNING: "No version found for level X, creating deposit without version"
       * продолжение создания депозита с `deposit_version_id=None` (legacy mode)
     * если есть версия:
       * проверка `version.is_active == True`
       * если `is_active == False`:
         * ошибка "Уровень временно отключен" → блокировка создания депозита
         * связь с R6-37 (отключение уровня)
  2. **Валидация суммы:**
     * проверка `user_sent_amount >= version.amount`
     * если меньше: ошибка "Минимальная сумма для уровня X: Y USDT"
  3. **Создание snapshot параметров:**
     * сохранение в `deposit`:
       * `deposit.deposit_version_id = version.id` (привязка к версии)
       * `deposit.amount = user_sent_amount` (фактическая сумма транзакции)
       * `deposit.roi_percent = version.roi_percent` (snapshot для гарантии)
       * `deposit.roi_cap_percent = version.roi_cap_percent` (snapshot)
       * `deposit.roi_cap_amount = deposit.amount * (version.roi_cap_percent / 100)` (расчет cap)
  4. **Логирование:**
     * INFO: "Deposit created with version" с deposit_id, version_id, level
* **Обновление БД:**
  * `deposits.deposit_version_id` - установка Foreign Key
  * `deposits.amount`, `deposits.roi_percent`, `deposits.roi_cap_percent`, `deposits.roi_cap_amount` - snapshot
* **Гарантии версионирования:**
  * **G1:** Депозит всегда помнит условия своей версии (через deposit_version_id)
  * **G2:** Изменение версии (создание новой) НЕ влияет на существующие депозиты
  * **G3:** Депозит хранит snapshot key параметров (amount, roi_percent, roi_cap_percent) для redundancy
  * **G4:** При расчете rewards используется `deposit.deposit_version.roi_percent` если есть версия
* **Обработка ошибок:**
  * version не найдена → допускается (legacy deposit без версии)
  * version.is_active = False → блокировка создания
  * user_sent_amount < version.amount → ошибка валидации, возврат средств
* **Edge cases:**
  * пользователь отправил транзакцию на старую сумму после изменения версии:
    * если tx подтверждена после создания новой версии:
    * валидация по новой версии → отклонение (amount < new_version.amount)
    * возврат средств или manual admin approval
  * версия изменилась между показом UI и созданием tx:
    * редкий race condition
    * защита: всегда использовать latest version при создании депозита
    * UI может показывать старую сумму, но backend проверяет actual version
* **Связанные сценарии:** R3-2 (создание депозита), R17-7 (создание версии), R17-9 (использование при начислении), R17-11 (гарантия immutability)
* **Сервис:** `DepositService.create_deposit()`
* **Репозиторий:** `DepositLevelVersionRepository.get_current_version()`

#### R17-9: Использование версии при расчете rewards

* **Контекст:** В `RewardService.calculate_rewards_for_session()` и `RewardService.calculate_individual_rewards()`
* **Приоритет источников ROI rate:**
  1. **RewardSession rate** (если active session и rate > 0) - высший приоритет
  2. **deposit.deposit_version.roi_percent** (если есть версия) - средний приоритет
  3. **ROI corridor config** (из GlobalSettings) - низший приоритет (default)
* **Процесс в calculate_rewards_for_session():**
  * для каждого депозита в сессии:
    1. получение `reward_rate` из session для уровня
    2. если `reward_rate == 0`:
       * проверка: `if deposit.deposit_version is not None:`
         * использование `reward_rate = deposit.deposit_version.roi_percent`
       * иначе:
         * fallback: получение corridor config для уровня
         * использование случайного rate из corridor (или fixed)
    3. расчет reward с полученным rate
* **Процесс в calculate_individual_rewards():** (R7-7)
  * для каждого депозита:
    1. проверка: `if deposit.deposit_version is not None:`
       * использование `rate = deposit.deposit_version.roi_percent`
    2. иначе:
       * получение corridor config для уровня
       * генерация rate через `generate_rate_from_corridor()` или fixed
    3. расчет reward
* **Гарантия консистентности:**
  * депозит с версией **всегда** использует roi_percent из своей версии (если нет active session)
  * это гарантирует что изменение corridor не влияет на старые депозиты
  * **пример:**
    * Deposit A создан с version 1: amount=10, roi_percent=2.0
    * админ меняет corridor на 5%-15% и создает version 2: amount=15, roi_percent=10.0 (avg)
    * Deposit A продолжает получать 2.0% (из version 1)
    * Deposit B (новый, version 2) получает случайный 5%-15% (из corridor) или 10.0% (из version 2 при session-based)
* **Обработка edge cases:**
  * deposit.deposit_version = None (legacy deposit):
    * использование corridor config (текущего на момент начисления)
    * может привести к изменению rate для legacy депозитов при изменении corridor
  * deposit.deposit_version.roi_percent = 0:
    * логирование WARNING, использование corridor config как fallback
  * deposit создан с версией, но версия удалена из БД (НЕ должно происходить):
    * Foreign Key constraint предотвращает удаление
    * если происходит (manual SQL) → ошибка, skip депозита
* **Связанные сценарии:** R7-7 (individual accrual), R7-11 (session-based), R17-8 (привязка версии), R17-11 (гарантия immutability)
* **Код:** проверки в `RewardService.calculate_rewards_for_session()`, `RewardService.calculate_individual_rewards()`

#### R17-10: UI отображение версий для пользователей и админов

* **Для пользователей** (R3-7 "Мои депозиты"):
  * каждый депозит показывает **свои условия** (из snapshot или версии):
    * Уровень: X
    * Сумма: Y USDT (deposit.amount)
    * ROI процент: Z% (deposit.roi_percent или deposit.deposit_version.roi_percent)
    * ROI Cap: W% (deposit.roi_cap_percent)
    * Заработано: V USDT
    * Прогресс: P% (roi_paid / roi_cap * 100)
  * **опциональный badge:**
    * "✨ Current Plan" - если deposit.deposit_version_id == latest version
    * "📜 Legacy Plan" - если deposit.deposit_version_id < latest version
    * помогает пользователю понять что у него старые условия
  * **поведение:**
    * пользователь видит свои условия, не путается
    * если у пользователя 2 депозита на разных версиях:
      * Deposit 1 (version 1): Level 1, 10 USDT, 2%
      * Deposit 2 (version 2): Level 1, 15 USDT, 10%
      * оба показываются с правильными условиями
* **Для админов** (R6-34 "Управление уровнями"):
  * показ **только current version** (latest для каждого уровня):
    * Уровень 1: 15 USDT, 10%, ✅ Активен
  * **доступ к истории версий:**
    * опциональная кнопка "📜 История версий" для каждого уровня
    * показ всех версий с датами и created_by
    * пример:
      * Version 3 (current): 15 USDT, 10%, created 2025-01-25 by Admin #123
      * Version 2: 12 USDT, 5%, created 2025-01-20 by Admin #456
      * Version 1: 10 USDT, 2%, created 2025-01-15 by System
* **В меню создания депозита** (R3-2):
  * показ актуальных условий из **current version**:
    * "Для открытия Level 1 требуется: 15 USDT"
    * ROI: до 500% (или детали corridor)
  * пользователь видит новые условия, не старые
* **Edge cases:**
  * пользователь открывает "Мои депозиты" сразу после изменения версии:
    * его старые депозиты показывают старые условия (корректно)
  * пользователь пытается открыть новый депозит со старой суммой:
    * UI предупреждает "Минимальная сумма изменена на X"
    * валидация на backend отклонит (R17-8)
* **Связанные сценарии:** R3-7 (пользовательский UI), R6-34 (админский UI), R17-6 (архитектура версий)

#### R17-11: История изменений сумм и audit trail

* **Способы отслеживания истории:**
  * **через deposit_level_versions:**
    * каждая версия - это snapshot условий на момент effective_from
    * можно построить timeline изменений:
      * 2025-01-15: Level 1 amount = 10 USDT (version 1)
      * 2025-01-20: Level 1 amount = 12 USDT (version 2)
      * 2025-01-25: Level 1 amount = 15 USDT (version 3)
    * запрос: `SELECT * FROM deposit_level_versions WHERE level_number=1 ORDER BY version ASC`
  * **через deposit_corridor_history:** (R6-27)
    * логирует изменения ROI corridors (min/max/fixed)
    * НЕ логирует изменения amount напрямую
    * но можно интегрировать: добавить поле `amount_changed` в deposit_corridor_history
    * или создать отдельную таблицу `deposit_level_version_history` (избыточно)
  * **через admin_actions:** (R6-15)
    * общий audit log всех админских действий
    * запись "Admin #X changed level Y amount to Z" с timestamp
* **UI для просмотра истории:**
  * кнопка "📜 История версий" в R6-34 (управление уровнями)
  * показ для каждой версии:
    * Version number
    * Amount
    * ROI percent
    * Is Active
    * Effective From
    * Created By (admin username + ID)
  * сортировка по version DESC (newest first)
* **Audit trail гарантии:**
  * **immutability:** версии НЕ изменяются после создания (only INSERT, no UPDATE/DELETE)
  * **traceability:** каждая версия знает created_by_admin_id
  * **timeline:** effective_from позволяет построить временную шкалу
  * **integrity:** Foreign Keys предотвращают удаление версий с привязанными депозитами
* **Использование для анализа:**
  * сколько пользователей создали депозиты на старой версии?
    * `SELECT COUNT(*) FROM deposits WHERE deposit_version_id = old_version_id`
  * какая версия самая популярная?
    * `SELECT deposit_version_id, COUNT(*) FROM deposits GROUP BY deposit_version_id ORDER BY COUNT(*) DESC`
  * как изменялись условия Level 1 за последние 6 месяцев?
    * `SELECT * FROM deposit_level_versions WHERE level_number=1 AND effective_from >= NOW() - INTERVAL '6 months' ORDER BY version ASC`
* **Edge cases:**
  * manual SQL изменение version row (НЕ рекомендуется):
    * нарушение audit trail
    * защита: database triggers (опционально)
    * best practice: версии immutable
  * удаление старой версии:
    * Foreign Key constraint от deposits предотвращает
    * если нужно "скрыть" версию: добавить поле `is_visible` (не удалять)
* **Связанные сценарии:** R6-27 (история corridor изменений), R6-15 (audit log), R17-6 (архитектура), R17-7 (создание версии)

#### R17-12: Edge case - обновление roi_percent при изменении corridor

* **Проблема:** Когда админ изменяет corridor (R6-23), должен ли создаваться новый deposit_level_version?
* **Текущая реализация:** НЕТ, версия НЕ создается
  * изменение corridor влияет на GlobalSettings.roi_settings
  * НЕ влияет на deposit_level_versions
  * roi_percent в версии остается прежним
* **Последствия:**
  * **для новых депозитов:**
    * если используется Individual Accrual (R7-7):
      * rate генерируется из нового corridor config
      * deposit.deposit_version.roi_percent может не совпадать с corridor (это OK)
    * если используется Session-based (R7-11):
      * rate из session или из deposit_version.roi_percent
  * **для старых депозитов:**
    * продолжают использовать roi_percent из своей версии (R17-9)
    * НЕ затрагиваются изменением corridor
* **Потенциальная проблема:**
  * админ изменил только amount (R6-26):
    * создается новая версия с roi_percent из старой версии
    * но corridor мог измениться между версиями!
  * решение: при создании новой версии в R17-7:
    * **всегда** получать актуальный roi_percent из corridor config
    * НЕ копировать слепо из старой версии
* **Правильное поведение set_level_amount():**
  ```python
  # R17-7: Создание новой версии
  # Шаг 3: Получение актуального ROI percent
  corridor_config = RoiCorridorService.get_corridor_config(level)
  if corridor_config["mode"] == "equal":
      roi_percent = corridor_config["roi_fixed"]
  else:  # custom
      # Опция 1: использовать среднее
      roi_percent = (corridor_config["roi_min"] + corridor_config["roi_max"]) / 2
      # Опция 2: использовать фиксированное значение (deprecated)
      # roi_percent = current_version.roi_percent if current_version else 2.0
  
  new_version.roi_percent = roi_percent
  ```
* **Рекомендация:**
  * при изменении corridor: НЕ создавать версию (текущее поведение)
  * при изменении amount: создать версию с актуальным roi_percent из corridor
  * это гарантирует что новые депозиты получают актуальные условия
* **Edge case - double change:**
  * админ изменяет corridor на 5%-15%, затем amount на 20:
    * только изменение amount создает версию
    * новая версия получает roi_percent = avg(5,15) = 10%
    * старые депозиты продолжают с roi_percent из их версии (например, 2%)
  * все депозиты сохраняют консистентность благодаря версионированию
* **Связанные сценарии:** R6-23 (изменение corridor), R6-26 (изменение amount), R17-7 (создание версии), R17-9 (использование при rewards)

#### R17-13: Миграция существующих депозитов на систему версий

* **Контекст:** Система версионирования внедряется в уже работающий проект с существующими депозитами
* **Проблема:** Старые депозиты имеют `deposit_version_id = NULL` (legacy)
* **Решения:**
  * **Опция 1: Оставить legacy депозиты без версии**
    * deposit_version_id остается NULL
    * при расчете rewards: используется corridor config (текущий)
    * риск: изменение corridor влияет на legacy депозиты
    * плюс: не требует миграции, простой rollout
  * **Опция 2: Создать initial versions и привязать legacy депозиты**
    * создать version 1 для каждого уровня с текущими параметрами
    * UPDATE: `UPDATE deposits SET deposit_version_id = (SELECT id FROM deposit_level_versions WHERE level_number = deposits.level AND version = 1) WHERE deposit_version_id IS NULL`
    * плюс: все депозиты версионированы, консистентность
    * минус: требует data migration script
* **Рекомендуемый подход: Опция 2 с осторожностью**
  * **Этап 1: Создание initial versions**
    * для каждого level 1-5:
      * получить текущие параметры (amount, roi_percent из конфигурации или defaults)
      * создать deposit_level_version с version=1, is_active=True, effective_from=NOW()
  * **Этап 2: Миграция депозитов (опциональная)**
    * запустить UPDATE query для привязки legacy депозитов к initial versions
    * логировать количество обновленных записей
    * проверить что все обновлено корректно
  * **Этап 3: Валидация**
    * проверка: `SELECT COUNT(*) FROM deposits WHERE deposit_version_id IS NULL`
    * если 0 или приемлемое число → миграция успешна
  * **Этап 4: Deploy**
    * включить использование версий в коде (R17-8, R17-9)
    * мониторинг: нет ли ошибок с version lookup
* **Миграционный скрипт (SQL пример):**
  ```sql
  -- Создание initial versions
  INSERT INTO deposit_level_versions (level_number, amount, roi_percent, roi_cap_percent, version, is_active, effective_from, created_by_admin_id)
  VALUES
    (1, 10.00, 2.0, 500, 1, true, NOW(), NULL),
    (2, 50.00, 2.0, 500, 1, true, NOW(), NULL),
    (3, 100.00, 2.0, 500, 1, true, NOW(), NULL),
    (4, 500.00, 2.0, 500, 1, true, NOW(), NULL),
    (5, 1000.00, 2.0, 500, 1, true, NOW(), NULL);
  
  -- Привязка legacy депозитов (опционально)
  UPDATE deposits
  SET deposit_version_id = (
    SELECT id FROM deposit_level_versions
    WHERE level_number = deposits.level
    AND version = 1
  )
  WHERE deposit_version_id IS NULL;
  ```
* **Rollback plan:**
  * если миграция пошла не так:
    * откат UPDATE: `UPDATE deposits SET deposit_version_id = NULL WHERE ...`
    * удаление initial versions (если не использовались): `DELETE FROM deposit_level_versions WHERE version = 1`
  * важно: тестировать на staging/dev перед production
* **Связанные сценарии:** R17-6 (архитектура), R17-7 (создание версий), R17-8 (привязка депозитов)

---

## 18. R18 — Threat model / атакующие сценарии

**Критичность**: Специализированные атаки требуют проактивной защиты, не только реактивной.

### 18.1. Blockchain и DDoS атаки

#### R18-1: Спам-депозиты минимальными суммами (dust attack)

* Атакующий сценарий:
  * злоумышленник создает тысячи депозитов по $0.01 USDT
  * цели атаки:
    * **забить базу данных** огромным количеством записей
    * **нагрузить blockchain monitor** обработкой микротранзакций
    * **истощить RPC quota** множеством запросов
    * **замедлить UI** для честных пользователей
    * **создать шум** для маскировки реальной атаки
* Защитные меры:
  * **минимальный размер депозита**:
    * установить floor: например $5 или $10
    * отклонять депозиты меньше минимума
    * конфигурируемый параметр `MIN_DEPOSIT_AMOUNT`
  * **dust filter на уровне мониторинга**:
    * blockchain monitor игнорирует транзакции <$1
    * логирует их как `dust_transaction` для статистики
    * не создает записи в БД
    * опционально: собирает dust в отдельную таблицу для анализа
  * **rate limiting по кошельку источнику**:
    * если один wallet отправляет >10 транзакций за час → blacklist
    * добавление в `suspicious_wallets` таблицу
    * блокировка обработки всех депозитов с этого wallet
  * **aggregation и batching**:
    * вместо N отдельных queries при обработке N транзакций
    * batch обработка: собрать за period, обработать одним запросом
    * уменьшает нагрузку на БД и RPC
* Мониторинг и алерты:
  * метрика: `dust_transactions_per_hour`
  * если >100 за час → алерт админам
  * dashboard показывает top источники dust
  * возможность быстро blacklist wallet через UI
* Cost analysis:
  * dust attack стоит атакующему: gas fees * количество транзакций
  * но может стоить нам: RPC costs, DB storage, processing time
  * эффективная защита делает атаку экономически нецелесообразной

#### R18-2: Массовый бот-спам на регистрацию

* Атакующий сценарий:
  * ботнет создает тысячи Telegram аккаунтов
  * массовая регистрация в боте
  * цели:
    * **забить базу** фейковыми пользователями
    * **нагрузить бота** обработкой updates
    * **фармить бонусы** если есть регистрационные rewards
    * **спам реферальных ссылок** для накрутки
* Существующие защиты (описать как сценарий):
  * `OperationRateLimiter.check_registration_limit()`:
    * лимит на количество регистраций за период
    * например: 100 регистраций/час globally
    * per-bot instance или global через Redis
  * `BlacklistService` pre-registration checks:
    * проверка telegram_id перед стартом регистрации
    * мгновенный отказ для blacklisted
* Дополнительные меры:
  * **CAPTCHA / proof-of-work challenges**:
    * при подозрении на бота: простая задачка
    * "Реши пример: 5 + 3 = ?"
    * или "Нажми кнопку с цифрой 7"
    * lightweight, но эффективно против простых ботов
  * **behavioral analysis**:
    * скорость прохождения регистрации (бот = слишком быстро)
    * паттерны ввода (бот = идентичные тайминги)
    * отсутствие "человеческих" ошибок
  * **wallet verification**:
    * требование valid wallet address сразу
    * проверка wallet на blockchain (есть ли транзакции?)
    * новый пустой wallet = подозрительно
  * **gradual onboarding**:
    * новые аккаунты имеют ограничения:
      * нельзя создать депозит первые 24 часа
      * нельзя приглашать рефералов первую неделю
    * снижает value автоматической регистрации
* Мониторинг:
  * spike detection: если регистраций >X в минуту
  * analysis: geographic/temporal clustering
  * dashboard: real-time регистрации с details
* Response:
  * автоматическое включение stricter rate limits
  * требование CAPTCHA для всех новых регистраций
  * временный stop регистраций в крайнем случае

#### R18-3: Атака через Telegram API (rate-limit / flood)

* Атакующий сценарий:
  * ботнет из реальных Telegram аккаунтов
  * массированный поток updates к боту
  * spam сообщений, команд, button clicks
  * цель: исчерпать ресурсы, положить бота
* Существующие защиты (оформить как сценарий):
  * `RateLimitMiddleware`:
    * ограничение actions per user per period
    * например: 30 actions/minute per user
  * `OperationRateLimiter`:
    * специфичные лимиты для критичных операций
    * withdrawal, deposits, registrations
* Telegram-level protection:
  * Telegram Bot API сам имеет rate limiting
  * но недостаточно для sophisticated attack
* Application-level defense:
  * **per-user throttling** (уже есть):
    * Redis counter: `rate_limit:{user_id}:{action}`
    * exponential backoff при превышении
  * **global throttling**:
    * если total load >threshold → degraded mode
    * disable non-essential features
    * priority queue: critical operations first
  * **fallback to read-only mode**:
    * при extreme load отключаем все write operations
    * пользователи могут только:
      * просматривать баланс
      * просматривать историю
      * читать инструкции
    * все финансовые операции queued for later
* Detection:
  * метрики requests per second (RPS)
  * аномалия detection: spike >3σ
  * breakdown по типам operations
* Recovery:
  * gradually restore write operations
  * monitor for repeated attack
  * permanent ban наиболее активных attackers

#### R18-4: Злонамеренный админ (insider threat)

* Сценарий отличается от компрометации (R10-3):
  * это НЕ взлом - админ действует легитимно
  * но с мошенническими намерениями
  * "решил украсть" деньги пользователей
* Возможные действия:
  * массовые approve выводов на свои кошельки:
    * создает фейковые withdrawal requests
    * или одобряет свои собственные
  * прямое изменение балансов:
    * если есть прямой DB access (не должно быть!)
    * или через admin functions
  * создание фейковых депозитов:
    * увеличение своего баланса без реального депозита
  * манипуляция ROI calculations:
    * искусственное увеличение своих earnings
* Защиты против insider threat:
  * **принцип "четырех глаз" (dual control)**:
    * критические операции требуют approval двух админов
    * особенно: withdrawals >$1000, balance adjustments
    * система escrow: первый админ initiate, второй approve
  * **строгие лимиты на админские операции**:
    * максимум $X withdrawals per day per admin
    * максимум N balance adjustments per week
    * любое превышение → automatic block + alert
  * **immutable audit log**:
    * все админские действия в append-only log
    * невозможно удалить или изменить
    * хранится в отдельной системе (не в main DB)
    * регулярное резервное копирование
  * **anomaly detection специфично для админов**:
    * unusual timing (3am operations)
    * geographical anomalies
    * sudden spike в activity
    * pattern deviation от normal behavior
  * **separation of duties**:
    * админы НЕ должны иметь прямой DB access
    * все через application layer с логированием
    * разные админы для разных функций:
      * withdrawal approver
      * user manager
      * config manager
  * **regular audits**:
    * внешний аудитор проверяет admin actions
    * random sampling withdrawals for verification
    * blockchain verification: claimed vs actual
* Detection примеры:
  * админ одобрил 50 withdrawals за час (normal: 5-10)
  * все withdrawals идут на 3 одинаковых wallet
  * balance adjustment без clear reason/ticket
  * approval withdrawn без user request
* Response:
  * immediate suspension админа
  * freeze всех его recent actions (if possible)
  * investigation: какие операции legitimate, какие fraud
  * legal action если confirmed theft

#### R18-5: Long-tail атаки на RPC / node infrastructure

* Атакующий сценарий:
  * цель: истощить RPC quota у QuickNode или подобных
  * способ: заставить систему делать много запросов
  * результат: система сама себя DDoS'ит
* Векторы атаки:
  * **создание множества pending депозитов**:
    * каждый депозит → periodic checks в blockchain
    * 1000 pending депозитов = 1000 RPC calls каждые 5 минут
  * **спам wallet balance checks**:
    * если система проверяет balances слишком часто
  * **inefficient blockchain queries**:
    * запросы без кэширования
    * repeated calls для одного и того же
* Защиты:
  * **жёсткие внутренние лимиты на RPC**:
    * maximum N calls per minute per service
    * hard cap даже если quota выше
    * rate limiter перед каждым RPC call
    * например: `BlockchainRateLimiter.acquire()` before `web3.eth.*`
  * **aggressive caching**:
    * cache block data for 30 seconds
    * cache transaction receipts permanently
    * cache wallet balances for 60 seconds
  * **smart batching**:
    * вместо N отдельных calls: batch request
    * example: проверка multiple tx receipts одним call
    * использование `eth_getLogs` вместо individual queries
  * **degraded mode при приближении к лимиту**:
    * мониторинг RPC usage: current vs quota
    * если >80% quota → degrade:
      * отключить non-critical blockchain operations
      * увеличить cache TTL
      * снизить частоту polling
    * если >95% quota → emergency mode:
      * только critical operations (withdrawals processing)
      * все остальное в queue "до восстановления"
  * **fallback nodes с shared quota**:
    * primary node exhausted → switch to secondary
    * но общий cap на все nodes together
* Мониторинг:
  * dashboard: RPC calls per minute
  * quota usage: current vs limit
  * cost tracking: $ spent on RPC
  * alert если approaching limit
* Автоматическая реакция:
  * gradual degradation как описано
  * уведомление админам
  * automatic switching to backup providers

---

## 8. Дальнейшие шаги

### 7.1. Превращение в матрицу «Роль × Кнопка × Сценарий»

Прямо по ID сценариев R1-1…R6-15 сделать таблицу и потом по коду/тестам отмечать:

* реализовано / не реализовано
* есть ли тесты
* есть ли UX-обратная связь

### 7.2. Критические сценарии безопасности

Отдельно выписать:

* компрометация админа и экстренное отключение
* массовый DDoS через кнопки
* черный список по кошельку до регистрации
* блокировка/разблокировка выплат при восстановлении финпароля

### 7.3. Формат проверки

Из этого каркаса можно создать:

* **Готовый шаблон Markdown-таблицы** для трекинга реализации
* **Чек-лист для ручного прогона** по ролям (что кликнуть, что ожидать)
* **Матрицу покрытия тестами** для каждого сценария

---

**Версия документа**: 2.0  
**Дата создания**: 2025-01-24  
**Последнее обновление**: 2025-01-28  
**Статус**: Расширенная версия с детальными сценариями управления депозитами, ROI corridors и версионирования

**Changelog v2.0:**
* Добавлено 39 новых детальных сценариев
* R6.4 - ROI Corridor Management (R6-16–R6-29): 14 сценариев динамического управления доходностью
* R6.5 - Deposit Management (R6-30–R6-40): 11 сценариев расширенного управления депозитами
* R7.2 - Individual Reward Accrual (R7-7–R7-12): 6 сценариев индивидуального начисления наград
* R17.2 - Deposit Versioning (R17-6–R17-13): 8 сценариев версионирования условий депозитов
* Обновлены перекрестные ссылки между существующими и новыми сценариями
* Добавлено оглавление с указанием новых разделов

